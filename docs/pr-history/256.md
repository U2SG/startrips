# PR #256 - Give Quick Recap a per-tempo travel base duration

- **Source head:** `96994666acd415426428eb30214dea4f91ae5e5b`
- **Scope:** `QUICK_RECAP_PROFILES.travelBaseMs` in `src/journey/narrativeTiming.ts` moves from a flat 1000 ms at every tempo to the owner-decided 420 / 650 / 900 ms, aligned with the base rhythm `FULL_PROFILES` already carries. `arrivalBaseMs` (800), `travelPerRadiansMs`, `travelMaxMs`, the dwell tables, `FULL_PROFILES` and `KEEPSAKE_PROFILE` are unchanged, and the travel formula is unchanged: only the floor moved. No budget arithmetic needed editing, because `buildDeterministicQuickRecapPlan` books each route chapter through the same `resolveNarrativeTiming` call the runtime step reader makes and `plannedDurationMs` is the sum of those bookings. The comment explaining why the value used to be flat is rewritten rather than deleted, so the refactor history stays readable.
- **User-visible change:** A Quick Recap paces a nearby leg by tempo instead of always spending a second on it; the two-point unit fixture's travelled leg falls from 1011 ms to 661 ms at `standard`. Because the fixed per-route-point overhead is smaller at every tempo, the recap's fail-closed budget check refuses fewer journeys and admits more optional media: the nine-stop fixture that previously overran now plays, and the role-aware-dwell fixture's optional photo now fits its 6500 ms budget at 6350 ms.
- **Review fixes:** None yet.
- **Follow-up:** `arrivalBaseMs` is still a flat 800 across all three Quick Recap tempi, and is still the constant `autoEditPlan.ts` carried before the resolver. ST-010 scoped the owner decision to the camera floor alone, so the arrival floor remains an open pacing question.
- **Validation:** Locally `./init.sh smoke` only — `pnpm typecheck`, `git diff --check`, `node scripts/pr-history.mjs validate-all`. Per this repository's workflow no test suite, dev server, browser, database or Docker ran locally; GitHub Actions (`ledger`, `core`, three `browser-qa` lanes, `verify`) is the authority. The four recomputed fixture numbers (4550 / 6350 / ~41.0 s / ~45.6 s) were derived by hand against the fixtures' real coordinates and are confirmed by the `core` lane, since `pnpm typecheck` cannot see fixture arithmetic.
