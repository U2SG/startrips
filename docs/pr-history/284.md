# PR #284 - Cancel stale deferred Story fullscreen entry

- **Source head:** `f488f9df8e923cd8f84be68bbeec225d46f96316`
- **Scope:** Issue #239, ST-033 only. Replace the untracked deferred image-tap fullscreen timeout with a dependency-injected cancellable helper, guard its callback with the current Story scope revision, and make the existing `cancelPendingMediaDragSettle()` boundary cancel both drag-settle and deferred-fullscreen owners. Extend focused unit coverage and the existing `qa-media-controls` Story mobile capture.
- **User-visible change:** A valid current sub-threshold image tap still enters fullscreen after settle, but changing Journey/Route Point/media scope before the settle deadline prevents stale fullscreen from reopening over the newer Story context. Reduced-motion delay 0 remains deferred and cancellable.
- **Review fixes:** The first browser evidence placed the scope-change scenario inside the long-lived mobile Story QA page; the new ST-033 assertion itself passed, but that scope mutation perturbed a later Manage-mode fixture. The capture is now isolated on its own fresh Story page so the evidence cannot change unrelated downstream QA preconditions.
- **Follow-up:** None in this slice; Story compositor/history behavior and Playback remain unchanged.
- **Validation:** Local validation intentionally limited to `git diff --check` and `node --check scripts/qa-media-controls.mjs`; the fresh worktree has no `node_modules`. GitHub CI owns typecheck, unit tests, build and browser QA, including `browser-qa / login-media`.
