# PR #309 - Retire renderer mode chrome

- **Issue relationship:** `Fixes #308` — ST-057 closes the obsolete renderer-mode chrome/mobile standalone-map slice while preserving Semantic Earth Dive as the detailed-geography path.
- **Source head:** `80276e7826bb8ac195fadefb67797efd1f0120b3`
- **Scope:** ST-057 only. Ordinary desktop Atlas no longer exposes `.living-atlas-globe__mode` or renderer/product-mode copy; one renderer-agnostic, keyboard-reachable Semantic Earth Dive intent invokes the existing `requestDive` path. Detail language and ST-043 map-pick utilities remain detail-only. Mobile V2 removes the permanent `真实地图` sheet action, `[data-mobile-map-trigger]`, standalone `.mobile-v2__real-map` dialog/history/focus-trap path, while ordinary compact mobile retains the same semantic Dive intent for non-gesture access. Gesture guidance is decoupled from the retired visible-mode-control assumption, and `src/main.tsx` changes remain limited to QA-preview prop compatibility.
- **User-visible change:** Detailed geography is presented as one continuous Earth Dive instead of a particle-globe versus real-map product-mode choice. Desktop no longer shows the old mode pill/copy, and mobile no longer carries a permanent real-map action/dialog. Keyboard and switch-control users still have a truthful intent-named Dive affordance with a touch-safe focus target. Selected Journey, explicit Route Point, timeline position and browser history survive the particle -> detail -> particle round trip; focus/fullscreen remains the #253 composition, reduced motion changes presentation only, and MapLibre attribution/legal controls remain owned by detailed geography.
- **Review fixes:** The first review found that compact mobile suppressed the semantic Dive intent together with optional detail utilities after the old mobile map action was removed. The repair split `showDiveIntent` from `showDetailControls`, keeping the same `requestDive` affordance on ordinary compact mobile while focus/cinematic composition remains control-free, with focused source and browser evidence for Tab reachability and >=44px exposed geometry. The migrated globe-focus QA initially tried to read the rewind scrubber from ordinary desktop Atlas; it was moved into the established #253 focus/timeline composition. A follow-up review then caught that exiting focus intentionally clears Route Point context, so the QA now deliberately re-selects the same explicit Route Point before starting the actual particle -> detail -> particle preservation boundary rather than grading focus teardown semantics.
- **CFAA / regression family:** The invalidated assumption was that a visible fallback renderer-mode button is part of the product contract. Regression coverage searches desktop Atlas chrome, compact/mobile sheet/dialog removal, keyboard and switch access, Journey/Route Point/timeline/history continuity, reduced-motion reachability, guest/share boundaries, focus/fullscreen composition, detail language/map-pick ownership, and MapLibre attribution/legal controls. No Dive thresholds/calibration/readiness/reverse semantics, `earthDive.ts`, `ParticleEarthScene.tsx`, `DetailedEarthMap.tsx`, Home/ST-056, Playback/Quick Recap, media preview, Keepsake/imprint, coastline/render-budget, or server/shared DTO behavior is retuned by this slice.
- **Follow-up:** No additional ST-057 source work is required after this Source. Issue #233 remains a separate Home activation/context residual and is not owned by ST-057; future Home/context work must stay outside this PR.
- **Validation:** Exact CODE Source `80276e7826bb8ac195fadefb67797efd1f0120b3` passed every product lane in CI run `34465885401`, including `core`, `globe-focus-chrome`, `login-media`, `post-login`, `final-acceptance`, `earth-dive`, `mobile-contract`, route/context, share, city-label, render-budget, and playback-prefetch. The only CI failures on that CODE head were the intentionally missing-ledger `ledger` job and aggregate `verify`. Exact-head automated review completed on `80276e7` at 2026-09-10T10:30:31Z with no new finding; the historical compact-mobile P2 and QA P1 threads were replied to and resolved after exact-head evidence. This final commit adds only `docs/pr-history/309.md`; frozen `docs/pr-history.md` remains unchanged.
