# PR #271 - Add completed Journey Playback transport state

- **Source head:** `31a0c09d05def2a95ac211f813a1d6e21012219b`
- **Scope:** Issue #126 / ST-044. Adds an explicit terminal Journey Playback transport state after the outro is consumed, unifies paused manual navigation around meaningful beats, and keeps completion inside the existing Playback controls without rebuilding timing, plan, trim, prefetch, camera grammar or Story↔Playback handoff.
- **User-visible change:** A finished Journey now truly stops while retaining its final position; Back, exit and replay remain available, paused viewers can move forward or backward without resuming, and replay restarts the soundtrack from the beginning.
- **Review fixes:** The completion replay path now pauses and rewinds the soundtrack before resetting the director, with focused unit coverage for the rewind helper so a second run cannot resume audio from the completion pause point.
- **Follow-up:** The remaining cinematic choreography in #126 and the Story↔Playback boundary owned by #245 remain outside this PR.
- **Validation:** `git diff --check` passed locally. `pnpm typecheck` could not run in this fresh worktree because `node_modules` is intentionally absent; dependency installation and all full unit/browser matrices remain delegated to GitHub CI by repository policy.
