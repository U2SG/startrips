# PR #470 - [Experience] Wire the account-surface UI to the existing password-change and first-password-enrollment routes

- **Source head:** `b198c36ec0a93c7c590e680399d1901d1f22984d`
- **Scope:** On main, server/routes/account-identities.ts (ST-067/#345), server/account-identities/password-change.ts + account-password.ts POST / (ST-092/#412) and password-enrollment.ts + account-password.ts POST /enrollment (ST-102/#446) are implemented and tested; so is Better Auth's native password-reset lifecycle (server/tests/password-reset-lifecycle.integration.test.ts, #425/#426), already used unauthenticated by AuthGateway.tsx's forgot-password mode via authClient.requestPasswordReset. The remaining gap, per the owner's 2026-09-21 option-B decision on #346: the account surface must never route a credential-less identity through POST /api/account-identities/reverify/password or /password/enrollment, because the only reverification issuer calls auth.api.verifyPassword, which a passwordless Account cannot pass. Instead first-password enrollment reuses the account's existing verified-email recovery link as its recent-control proof; the account surface's job is only to offer a 'send a set-password link' action with sent/expired/failed states, not to call the enrollment route itself. Work already committed on feat/issue346-st124-20260920 (5aebb0f, unpushed) covers the change-password flow, the widened AccountSurface, src/auth/accountPassword.ts and refusal text; it currently renders only a bare verify/recover CTA for the credential-less branch and still needs the actual send-link action and its states.
- **User-visible change:** [Experience] Wire the account-surface UI to the existing password-change and first-password-enrollment routes
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35561694223 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
