# PR #436 - Make the compact Place-to-Story handoff aperture deterministic regardless of image/marker click-time readiness

- **Source head:** `e2078271cf844e5ae701c529356b51fb347a8aa1`
- **Scope:** PR #315 (merged, refined by ST-053/#291, ST-081/#377, ST-082/#378) already implements the bounded Place->Story shared-element handoff: openJourneyStory/createPlaceMediaObservationElement in src/journey/LivingAtlasApp.tsx build a short-lived `[data-place-media-observation]` element beside the live projected Route Point marker, and scripts/qa-route-point-context.mjs asserts its geometry for desktop, reduced-motion and compact landscape viewports. The real gap: createPlaceMediaObservationElement requires, synchronously at click time, both routePointRepresentativeVisual() to find an `<img>` with a populated currentSrc/src and liveRoutePointMarker() to find a currently-visible non-zero-opacity globe marker; if either has not settled yet (image not finished rendering/loading, or marker mid-transition/occluded) the function returns null with no retry, so no aperture is ever created and the `landscape-844x390` compact case in scripts/qa-route-point-context.mjs:1066 times out waiting for `[data-place-media-observation]:not([data-shared-element-clone])` to attach - exactly the CI failure in run 35440173849. This is unfixed on current main (4345f35).
- **User-visible change:** Make the compact Place-to-Story handoff aperture deterministic regardless of image/marker click-time readiness
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35456101941 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
