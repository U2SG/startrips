# PR #517 - Prefilter Windows execution snapshots by PID

- **Source head:** `149248e266b7f464858127a95cf7bdf8db03c1e6`
- **Scope:** Bound Windows execution-provider CIM inspection to exact candidate PIDs discovered natively, and keep read-only selector/plan probes observer-only without publishing real carrier identity.
- **User-visible change:** None. This is control-plane reliability work that avoids selector stalls while preserving lane-scoped fail-closed execution exclusion.
- **Review fixes:** Preserved CommandLine plus CreationDate as the process-identity authority, kept real execution carriers identity-gated, and added regressions proving read-only probes survive identity-provider failure only when occupancy itself remains readable.
- **Follow-up:** Runtime installation remains a separate guarded safe-boundary operation; this ledger does not activate the reviewed bytes.
- **Validation:** Independent Hourly Review marked the exact Source CLEAR; control-plane CI passed on Ubuntu and Windows, and core, keepsake-render, and all eight browser product lanes passed on exact-Source CI run 35798250341. Ledger and verify were expected pre-ledger failures before this final-only commit.
