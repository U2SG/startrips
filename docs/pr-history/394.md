# PR #394 - Request cover art from Story management

- **Source head:** `64b370fb8d1c5d704e8c465710db1a29715767ef`
- **Scope:** Add an authenticated cover-reveal enqueue client and a capability-gated Story management action. Related to #367; opening playback in #379 remains separate.
- **User-visible change:** Members with Journey editing permission can request an ink-wash opening image and see the returned queue/processing/ready state or a readable error. The original cover is unchanged.
- **Review fixes:** Static review kept the action inside the scrollable Story content and exposed its mutation client only when Journey editing is allowed. Cover identity keys isolate asynchronous results after navigation or cover changes.
- **Follow-up:** Renderer integration, ordinary-client derivative display access and opening playback remain outside this PR. No background status polling is added. Initial CI also exposes the #325 Journey Rail hidden/actionability signature and a reduced-motion Earth Dive handoff observed as `holding`; these browser failures remain unresolved by this enqueue change.
- **Validation:** Static diff review and `git diff --check` passed. API and Atlas capability tests were added/updated. No local tests, builds or browser QA were run. Exact Source CI run 35081822241 passed core (types/tests/build), keepsake-render and 19 browser lanes. The initial final-acceptance and earth-dive lanes failed; ledger failed because this record was absent. A diagnostic final-acceptance rerun was requested, but a rerun cannot invalidate the initial failure evidence. The ledger commit requires fresh CI; no full green or visual acceptance is claimed.
