# PR #521 - Prove the same-Route-Point image/video hidden handoff in Story with a CI-gated browser QA lane

- **Source head:** `30f51cc52101d4abfb2e9e662cebf73a90ecf115`
- **Scope:** src/journey/StoryMediaPages.tsx already implements the retained-frame cache (frames/rememberLiveFrame), decodedImages readiness, currentReady/targetReady gating and prefersReducedMotion() short-circuit. Verified directly against origin/main@8d3dac4: handlePictureClick (StoryMediaPages.tsx ~L437-447) is bound to the presented video's own onClick and to a `.story-media-pages__hit-surface` overlay (story-media-pages.css z-index 8, above the video's z-index 6) that only clips the bottom ~72px/25% for controls — so a real click on most of the video surface calls step()/onNavigate instead of reaching playback, exactly matching the user's 'click plays the next media instead' report. This is a genuine functional defect in the navigation wiring, not a missing-QA-proof gap. The owner's 2026-09-22 amendment also reports (via a private recording, frame-indexed as V1-V8) blank/black-frame flicker and stale-asset exposure across image/video handoffs with mixed aspect ratios, plus enter/exit asset-continuity breaks (reveal-then-hide-then-reopen-smaller; wrong image flashing near close) that still need root-cause diagnosis and fixing alongside the verified A1/A2 click defect, all gated behind CI-run browser QA that does not exist today.
- **User-visible change:** Prove the same-Route-Point image/video hidden handoff in Story with a CI-gated browser QA lane
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source and delivery contract; resolved thread dispositions remain in the PR review history.
- **Follow-up:** Every registered delivery member retains its original issue scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35911900138 attempt 1 passed the real product lanes; independent Source review and per-member acceptance coverage are recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
