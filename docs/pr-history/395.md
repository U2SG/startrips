# PR #395 - Separate Composer Locate and Add search results

- **Base:** `d6ec05a6036c853e905c1ee5b54b8bae1897abaf`
- **Source head:** `2ff794b0ec71c18f481896bf3c7d294174586cc2`
- **Scope:** Implements ST-076 / issue #372 in the existing Journey Composer search surface. The current draft is searched synchronously by label through `matchRouteDraftPoints(...)`; results stay in current Route order and retain stable `draftId`. Existing Route Points appear in a distinct local group whose Locate action expands, focuses and scrolls the exact draft row without appending or persisting. External provider results remain a separate group and only explicit Add uses the existing `toDraftPoint(...)` / append path, always producing a fresh draft record even when provider label and canonical coordinates match an existing Route Point. No Backend, schema, new provider, Place/Visit entity or fuzzy geographic identity is added.
- **User-visible change:** Typing in the existing Composer search field immediately shows matching Route Points already in this Journey separately from external location results. Local results say Locate and do not add another point; provider results say Add and create a new Route Point. Duplicate local labels remain separate by Route position, and duplicate-label external results remain distinguishable by provider context/country in their accessible names.
- **Identity / freshness:** Local matching normalizes text only (NFKC/case) and never groups by proximity, provider identity or coordinate equality. Reorder/delete recompute from the current draft array. External search owns a monotonic revision: query edits, Composer close/unmount and Journey changes invalidate older responses. Local lookup does not depend on provider availability, and neither Locate nor Add persists until the existing Composer save path runs.
- **Review fixes:** The first Source `f82937c` changed the established accessible search button name from `搜索` to `搜索外部地点`, which correctly failed `browser-qa / final-acceptance`; Source `914b23a` restored the existing `搜索 / 搜索中…` control contract without weakening the test. Automated review then raised two valid P2 findings: same-label local Locate buttons all exposed `定位 Las Vegas`, and same-label external Add buttons all exposed `添加 <label>`. Final Source `2ff794b` includes Route position in Locate names (`定位 02 · Las Vegas`, `定位 07 · Las Vegas`) and provider context/country in Add names. Both threads were answered and resolved. Fresh exact-Source review request `5695786131` completed review-clear in comment `5695829675`; there are no unresolved current threads.
- **Regression-family search:** The existing ST-075 compact-row draftId focus/scroll refs remain the navigation owner; reorder/delete stay whole-record draft operations; reverse geocode and globe pick retain their separate revision/point-existence guards; provider Add still delegates to the pre-existing append path; the original search placeholder remains unchanged so the normal-path post-login/final-acceptance probe continues to target the real control. No sibling issue requiring separate intake was found in this scope.
- **Browser QA:** `browser-qa / composer-route-points` proves separate same-label/same-coordinate Las Vegas records 02/07, keyboard Locate with exact note/Stop/media provenance, no append or persistence on Locate, provider-unavailable while local lookup stays usable, deletion during pending provider search without resurrection, old-query response rejection, and explicit Add of a same-label/same-coordinate provider result as a fresh draftId. Locate/Add action targets are at least 44px and their distinguishing accessible names are asserted. Existing 320/360/390/430, compact-landscape, reorder/delete identity, keyboard More/Escape, reduced-motion and sticky-footer coverage remains in the same lane.
- **Follow-up:** None required for this slice. The existing coordinate validation and globe-pick paths remain available and unchanged; broader Place/Visit inference and provider-side deduplication remain explicit non-goals of #372.
- **Validation:** Local validation remained lightweight: `git diff --check` and `node --check scripts/qa-composer-route-points.mjs` succeeded; no dependency install, local build, full test suite, browser run, database or Docker was used. GitHub CI is authoritative. Exact Source CI run `35083524331` attempt 2 on `2ff794b0ec71c18f481896bf3c7d294174586cc2` has `core`, dedicated `browser-qa / composer-route-points`, `browser-qa / final-acceptance`, `post-login`, `mobile-contract`, `login-media`, `earth-dive` and every other real product/browser lane SUCCESS. The first attempt's unrelated Earth-Dive normal-motion reveal sampling failure matched the repository's established intermittent family and passed on one same-head targeted rerun without source/assertion/wait changes. Pre-seal red is limited to the intentionally absent current-PR `ledger` and downstream `verify`. This commit adds only `docs/pr-history/395.md`; exact final-head CI remains authoritative before handoff.
- **Boundary:** No merge-ready signing, merge, tag, release or deployment authority is granted. Experience Worker stops at `HANDOFF_REVIEW` after exact final-head CI and merge-readiness succeed.
