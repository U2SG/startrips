# PR #536 - Return Story to the current Route Point

- **Source head:** `0f5da4af8dd41218f0c8da15782ad21f0ca4ba72`
- **Scope:** Make Story's logical Route Point observation control the return detail and selected map marker even without a return media frame. Use a shared-element source only when its asset belongs to that Route Point. Clear Route Point detail when Composer enters globe picking, and yield Detailed Earth Route Point clicks to MapLibre's coordinate picker while that request is active.
- **User-visible change:** Closing Story after moving from a media Route Point to a text-only Route Point returns to the current point. Globe picking remains usable after that return on compact screens and when the user clicks an existing Route Point in Detailed Earth.
- **Review fixes:** The browser check grades the selected marker in the production renderer and clicks an existing Detailed Earth Route Point through real browser hit testing during Composer picking. Source CI exposed a compact-screen detail overlay blocking globe picking; the pick entry closes that detail through its existing cleanup path. Independent review of the final Source found no remaining defect.
- **Follow-up:** None.
- **Validation:** GitHub CI run `35955862351` passed `core`, `keepsake-render`, and all ten `browser-qa` jobs for the exact Source head. The route-point-context lane verified real A-to-text-only-B selection, visible B marker and detail, reopening B, and a real Detailed Earth click on an existing Route Point yielding a new Composer draft without reopening detail. The final-acceptance lane recorded real canvas pick hits at mobile widths 320, 360, 390, and 430. `ledger` and aggregate `verify` were the expected pre-ledger failures; this ledger-only commit triggers their final validation.
