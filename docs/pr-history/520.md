# PR #520 - Give the Route Point context card a real close affordance and prove hit-to-card identity

- **Source head:** `8c92b529d8de63208da34d387a2c27d5e02ec7cb`
- **Scope:** Confirmed on main (f88cf22 lineage, still current): buildRoutePointContext resolves by stable routePointId (not array index); routePointContext.ts's intent/revision model (requestRoutePointContextSelection/resolveRoutePointContextSelection/clearRoutePointContextSelection) already guards stale async writes; ST-081/#377 shipped the same-coordinate switcher (living-atlas__route-point-context-switcher) and ST-082/#378 shipped source-aware cross-point reading with its own data-cross-point-reading-close button, Escape wiring and backdrop-click close. The real gap: the base living-atlas__route-point-context aside (LivingAtlasApp.tsx ~3060-3160, from ST-053/#291) has no data-*-close control, Escape is only wired to globeFocusMode exit (not this card), and no handler clears it on a blank globe click (grep for onGlobeBlank/onBackgroundClick/onPointerMissed found none) - so a user who opens it has no way to dismiss it short of navigating elsewhere. Whether the reported label/marker hit-target actually resolves to the wrong routePointId (claim A) is unverified by static code and needs runtime pointer-event instrumentation, per the issue's own admission.
- **User-visible change:** Give the Route Point context card a real close affordance and prove hit-to-card identity
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source and delivery contract; resolved thread dispositions remain in the PR review history.
- **Follow-up:** Every registered delivery member retains its original issue scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35899116490 attempt 1 passed the real product lanes; independent Source review and per-member acceptance coverage are recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
