# PR #354 - Open Home Base context from the Atlas anchor

- **Source head:** `3d2cc1a8a753f0a84b38ca44114c3a0a48f0f682`
- **Scope:** Implements ST-065 / issue #233 as the residual on-demand interaction slice after the shipped Home presence, temporal resolver, motion-locked geographic anchor and initial camera seed. The existing projected Home anchor is the only Home target: pointer activation is arbitrated inside `ParticleEarthScene`, keyboard activation remains on the accessible Home button, and one compact non-modal owner-private context surface resolves the exact confirmed Home period. This PR does not add a permanent Home tab/tool, a second raycaster/projection/camera owner, a Home inference/suggestion rewrite, guest/share Home access, or Home Prelude/Epilogue behavior.
- **User-visible change:** A visible current or historical Home can now be opened directly from the globe into a restrained period-aware context card on desktop and compact mobile. The surface is memory/context first, does not expose an exact residential address, and yields immediately to higher-priority Story, Playback/menu, timeline, globe coordinate-pick and eligible Journey/Route Point ownership. Home context and the separate Home suggestion do not stack, and hidden/off-canvas Home remains unavailable.
- **Review fixes:** The bounded interaction family was repaired under one renderer authority. City-label contacts delegate pointer/drag/pinch/wheel through the existing renderer arbiter and inherit its touch-action policy; globe-pick retains the original full-precision `CityPoint` coordinates rather than rounded DOM serialization. Home is suppressed while the Playback menu owns presentation. Route Point arbitration ignores non-active Journey siblings and, on final Source, also ignores Journey/Route Point candidates whose temporal reveal is zero, so invisible future points cannot consume a Home click during rewind/compact timeline states. Overlapping Home periods resolve in reverse paint/current visual priority. Existing Story/Playback/timeline yielding, keyboard activation, compact accessibility, suggestion/context exclusivity and guest/share isolation remain intact. Fresh exact-head automated review on Source `3d2cc1a` reported no major issues; all pull-request review threads were resolved only after exact-head CI/review evidence.
- **Follow-up:** This PR closes only ST-065's residual #233 activation/context slice. It deliberately does not add management-first Home UI, a permanent mobile toolbar/tab, new camera recenter behavior, shared/guest Home semantics, Home Prelude/Epilogue, or unrelated release-candidate P1 work (#355/#356). Those require separately owned scope.
- **Validation:** The owner worktree intentionally has no local `node_modules`; no dependency install or heavy local validation was performed. `git diff --check` passed before Source commit. Exact Source-head CI run `34778092142` on `3d2cc1a8a753f0a84b38ca44114c3a0a48f0f682` passed `core` (including Typecheck, Test and Build), `keepsake-render`, `browser-qa / home-base-context`, `city-label-anchoring`, `route-point-context`, `final-acceptance`, `mobile-contract`, `guest-share`, `owner-share`, `earth-dive`, `home-base-suggestion`, `globe-render-budget`, `login-media` and every other product/browser lane; only the expected pre-seal `ledger` and downstream aggregate `verify` failed because this file did not yet exist. The final temporal-arbitration node regression is part of `ParticleEarthScene.test.tsx` and passed in `core`. This ledger commit is documentation-only; exact final-head GitHub CI is authoritative before handoff.