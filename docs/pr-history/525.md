# PR #525 - Reuse prepared Journey read projections

- **Source head:** `ef14706bacf58fa8faeac40cfc792366b001b0fd`
- **Scope:** Reuse the canonical media arrays on Playback stop steps while building Full Playback plans and Keepsake manifests, removing per-media Journey scans and chapter sorts. Return the explicit Recorded Track repository DTO through JSON serialization without a second object-tree copy. Compute client track-summary counts and lexical timestamp bounds in one pass without temporary timestamp arrays or sorting.
- **User-visible change:** None intended. Media ordering, duration overrides and lazy fallbacks, Home/fractional timing, Keepsake revision checks, Recorded Track response fields/dates, tenant authorization and import replay behavior remain unchanged. The optimization reduces redundant computation and allocations; it adds no persistent cache or public API.
- **Review fixes:** Independent static review covered all nine Source files and found no blocker. Review checked real duration callbacks, field allowlists, JSON date/null equivalence and lexical summary semantics. The performance claim covers the planners' own work; arbitrary duration callbacks retain their existing cost.
- **Follow-up:** None required for this scope. No measured latency or frame-rate improvement is claimed.
- **Validation:** Exact Source [CI run 35815740715, attempt 1](https://github.com/U2SG/startrips/actions/runs/35815740715) passed core, Keepsake render and all eight browser shards. Core passed 175 test files / 2,487 tests, typecheck and production build, including linear media-read budgets, stable ordering, fresh rebuilds, lazy overrides and HTTP serialization boundaries. Only the expected missing-ledger check and dependent verify failed before this final commit. Static diff checks passed; no local tests, builds, browser or database execution occurred. This final commit adds only this ledger; final-head verify and fresh merge-readiness remain authoritative before merge.
