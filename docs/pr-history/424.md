# PR #424 - [Backend] Expose owner-only recorded-track withdrawal through the existing Journey recorded-track API

- **Source head:** `46eeb36fa91d6b69e416e46b30a5dc5074ac9606`
- **Scope:** Already on main from #419 / PR #421 (merge e3007ba): the Journey-scoped recorded-track tables, normalization, idempotent writes, owner-only GET readback in server/routes/journey-recorded-tracks.ts, and the repository operation deleteRecordedTrackForAtlas(atlasId, journeyId, operationKey) with repository-level tests. The real gap is that deleteRecordedTrackForAtlas has no HTTP caller anywhere in server/ (git grep on origin/main finds only its definition and the integration test), so an owner cannot withdraw one persisted recorded-track operation through the product boundary. This feature adds exactly that bounded owner-only mutation to the existing /api/journey-recorded-tracks route family, taking operationKey from a validated JSON body, deriving authority only through requireAtlasAccess. It introduces no schema change, no file/GPX parser, no import or upload endpoint, no client or map work, and does not touch the first external input format still gated in #341.
- **User-visible change:** [Backend] Expose owner-only recorded-track withdrawal through the existing Journey recorded-track API
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35431245056 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
