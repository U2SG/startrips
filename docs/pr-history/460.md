# PR #460 - Sparse Route Point chapter continuity: explicit empty/single/few density and one continuous arrival→media beat

- **Source head:** `b7107d4c3cede84d90e6c8a7a4d506dc137266fc`
- **Scope:** Verified against src/journey/journeyPlayback.ts, journeyPlaybackPlan.ts, journeyPlaybackNavigation.test.ts and JourneyPlaybackOverlay.tsx on main. Already implemented: deterministic elapsed-time plan (ST-021), completed/paused-navigation rules (ST-044), Home Prelude/Epilogue (ST-030), and meaningful Next/Previous that already skips the 'travel' beat (#126, tested in journeyPlaybackNavigation.test.ts). Real gap: there is no explicit per-Route-Point media density (empty/single/few) anywhere in the codebase, and buildPlaybackSteps/meaningfulPlaybackStepIndexes still expose the arrival 'stop' beat as its own separate manual Next/Previous destination even when the point has media — journeyPlaybackNavigation.test.ts's own fixture shows Next landing on stop(pointIndex:0) before media(pointIndex:0,mediaIndex:0), which is exactly the extra click #456 asks to remove for populated chapters. JourneyPlaybackOverlay.tsx also still renders arrival and hero media as two sequential full-screen swaps rather than one continuous chapter, and no browser-QA lane exercises a 0/1/3-media fixture.
- **User-visible change:** Sparse Route Point chapter continuity: explicit empty/single/few density and one continuous arrival→media beat
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35528328549 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
