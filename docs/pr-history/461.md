# PR #461 - Route Experience owners through external Codexless execution

- **Source head:** `be43e0a91f7cd8d6b7749ddf9048d7e3bf5739f3`
- **Scope:** Root-cause control-plane repair under #399. Route Experience owners through evidence-only external Codexless receipts while preserving ONE as the sole logical owner registry; bind every receipt mutation to its generation/request id; keep external Experience occupancy visible after the local carrier exits; fail closed on missing provider identity and unknown provider states; preserve two Experience slots without allowing a third owner; keep Backend intake/re-triage active until the Orchestrator has an executable intake path. This PR also hardens CI failure-family parsing so explicit Playwright/browser assertions remain the primary evidence instead of generic workflow footer noise.
- **User-visible change:** No direct Startrips product UI or data-model change. Operationally, Experience work can continue through the external Codexless provider without consuming the LOCAL Backend Claude quota, while Backend queue replenishment remains active and duplicate Experience ownership is fail-closed.
- **Review fixes:** Exact-Source Hourly Review on `be43e0a91f7cd8d6b7749ddf9048d7e3bf5739f3` is CLEAR for seal. Earlier review findings were fixed on the same owner branch: receipt generation binding is mandatory, formal Experience authority was restored to workspace-write, synthetic receipt tests were corrected, external occupancy became canonical selector evidence, Backend intake was retained, and provider status is now an explicit active/terminal enum with unknown states rejected on mutation and observation.
- **Follow-up:** #399 remains the umbrella control-plane reliability issue and is not closed by this PR. The Orchestrator may take over all intake only after a real executable intake implementation exists; until then Backend retains the installed intake/amend path. Existing merge/sign/deploy boundaries remain unchanged.
- **Validation:** Exact CODE Source CI run `35570790083` on `be43e0a91f7cd8d6b7749ddf9048d7e3bf5739f3` passed core, both Ubuntu/Windows control-plane suites, keepsake-render, and every product/browser QA lane. Its only failures are the intentionally absent current-PR ledger and downstream verify before this seal. Hourly Review independently cleared this exact Source after verifying unknown provider states cannot be recorded or free Experience capacity. This final commit changes only `docs/pr-history/461.md`; exact final-head CI remains authoritative before HANDOFF_REVIEW.
