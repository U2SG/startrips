# PR #301 - Add historical Home context to Journey Playback

- **Source head:** `3e688fb5bdc90c181dcde48dc24a51a29686de43`
- **Scope:** Issue #235 / ST-030 only. Add optional owner-only historical Home Prelude and Home Epilogue narrative context to Journey Playback using the existing Home Base period resolver, canonical geographic anchoring and the existing playback plan/step/camera authorities. Home remains camera-only context: it never becomes a persisted Journey Route Point, route arc, measured travel leg, shared DTO field or guest-visible Home datum.
- **User-visible change:** When a confirmed historical Home period is eligible and the Journey is farther than the inclusive 25 km skip radius, Full Playback may begin with a Home prelude and return to Home after the final Journey beat. An eligible Home epilogue remains the final camera/life context through the outro/completion fade; without eligible Home, the existing route-framed intro/outro behavior is unchanged. Atlas Journeys become usable as soon as Journey data loads, while optional private Home history may hydrate later and Playback simply omits Home until it is available.
- **Review fixes:** Owner CFAA found two composition blockers after the initial runtime integration. First, `home-epilogue` was followed by an `outro` that reclaimed route camera ownership; final Source carries the same Home camera target through the outro and proves the camera target key remains Home while the no-Home outro stays route-framed. Second, optional private Home hydration was awaited in the Atlas readiness `Promise.all`; final Source starts Home hydration beside the Journey read but never awaits it on the core readiness path, revision-gates late Home commits, tolerates Home failure, and keeps guest/read-only views free of private Home hydration. A focused test also drains the detached hydration microtasks without making production readiness await Home.
- **Follow-up:** ST-011's global Quick Recap over-budget strategy remains product-gated and untouched. ST-030 only omits its own Home beats when those beats would exceed the existing Quick Recap target. The 25 km radius is narrative redundancy only and never asserts a measured return home. Unknown Journey end dates remain ineligible for Home epilogue rather than borrowing `startedOn`.
- **Validation:** Rebased cleanly onto exact-green `main@db29270849968b04f54f3058552638d4909909df`; local validation intentionally limited to `git diff --check`. Exact rebased CODE-source CI run 34369032203 on `3e688fb5bdc90c181dcde48dc24a51a29686de43` has `core` and every browser lane green. The first playback-prefetch attempt was cancelled by GitHub while installing Chromium before QA executed; the same exact head was rerun and `browser-qa / playback-prefetch` completed successfully without source or timeout changes. The only expected CODE-head failures are the intentionally missing PR ledger and aggregate `verify` before this seal. No current-head review blocker was present at seal time.
