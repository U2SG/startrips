# PR #301 - Add historical Home context to Journey Playback

- **Source head:** `6bde947d6bbfbf4e443cfa27f8c7ce8cb1225e7c`
- **Scope:** Issue #235 / ST-030 only. Add optional owner-only historical Home Prelude and Home Epilogue narrative context to Journey Playback using the existing Home Base period resolver, canonical geographic anchoring and the existing playback plan/step/camera authorities. Home remains camera-only context: it never becomes a persisted Journey Route Point, route arc, measured travel leg, shared DTO field or guest-visible Home datum.
- **User-visible change:** When a confirmed historical Home period is eligible and the Journey is farther than the inclusive 25 km skip radius, Full Playback may begin with a Home prelude and return to Home after the final Journey beat. An eligible Home epilogue remains the final camera/life context through the outro/completion fade; without eligible Home, the existing route-framed intro/outro behavior is unchanged. Atlas Journeys become usable as soon as Journey data loads, while optional private Home history may hydrate later. Home that resolves after Playback has already started is deferred to the next session and cannot change the current visible, camera, timing or prefetch topology.
- **Review fixes:** Owner CFAA review first kept Home camera ownership through outro/completion, removed optional private Home hydration from the Atlas readiness critical path, and froze Home narrative context for each mounted playback session so late hydration cannot replace the current `playbackStepIdentity`, visible media owner, camera target or timer. The final regression-family P2 found prefetch still built a second step list from live Home context. Final Source removes that second topology authority: `JourneyPlaybackOverlay` now plans lookahead and hold targets from the director's session-frozen `steps`, and the regression proves late eligible Home hydration preserves the same semantic beat, prefetch window and hold target; a later new playback may include Home.
- **Follow-up:** ST-011's global Quick Recap over-budget strategy remains product-gated and untouched. ST-030 only omits its own Home beats when those beats would exceed the existing Quick Recap target. The 25 km radius is narrative redundancy only and never asserts a measured return home. Unknown Journey end dates remain ineligible for Home epilogue rather than borrowing `startedOn`.
- **Validation:** Rebasing onto exact-green `main@f0595c19aaba0f9f3ba24e55a9e72490f791a620` produced final CODE Source `6bde947d6bbfbf4e443cfa27f8c7ce8cb1225e7c`. Local validation was intentionally limited to `git diff --check`. Exact CODE-head CI run 34381921935 has `core` and every browser lane green, including `playback-prefetch`; the initially failing unrelated `city-label-anchoring` Hong Kong 3x declutter case passed on a same-head job rerun without source changes. The only remaining CODE-head failures are the expected stale-ledger `ledger` and aggregate `verify` checks before this seal. No newer current-head source blocker was present at seal time.
