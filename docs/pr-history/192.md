# PR #192 - Shard pull request ledger files

- **Source head:** `358ed06baf7227b6538108317d153869410f87ec`
- **Scope:** Replaces the single shared PR ledger write target with one independent `docs/pr-history/<PR_NUMBER>.md` file per PR, adds parser/validator/index tooling, and makes a lightweight ledger CI lane part of the final `verify` gate.
- **User-visible change:** None directly. Parallel development no longer forces unrelated PRs to rebase just because another PR appended to the same ledger file.
- **Review fixes:** The design deliberately leaves `docs/pr-history.md` untouched as a legacy archive and does not commit a generated aggregate index, avoiding both another shared write lock and post-merge bot-commit/CI churn. PR validation automatically enforces that the reviewed Source head is an ancestor of the final head and that only this PR ledger file changes afterward.
- **Follow-up:** After #192 lands, migrate remaining open PR ledgers such as #157 to `docs/pr-history/<PR_NUMBER>.md`; future PRs should never append to the legacy archive.
- **Validation:** `node --test scripts/pr-history.node-test.mjs` 4/4 green; `validate-all`, script syntax, and `git diff --check` green on the code head. This PR's final ledger commit is additionally validated with `validate-pr`; GitHub CI is authoritative for workflow integration.
