# PR #304 - Define Home Base Atlas presence and camera policy

- **Source head:** `f4376eb089035ef7b019bd18eed53c295ff0641d`
- **Scope:** Issue #233 / ST-028 design-first contract slice only. Adds pure Home Base temporal presence, geographic anchor, label/accessibility/motion descriptors and initial-camera policy, plus focused negative source contracts. It does not integrate Home rendering into `LivingAtlasApp`, Playback or `ParticleEarthScene`, and does not add a mobile Home control or guest-share payload.
- **User-visible change:** Establishes the V1 Home semantics that later Atlas rendering must follow: current Home is a quiet stable origin, historical Home can return only in the matching life period, all-time history leaves only restrained traces, labels use `常住地 · <place>`, and Home may seed a fresh untouched Atlas but never compete with selected-Journey camera ownership.
- **Review fixes:** Rebased onto current main after #301, then closed both current-head blockers. The negative-contract guard now matches Home identifier families case-insensitively and pins mixed-case/constant-style forms such as `HomeBasePeriod` and `HOME_BASE_ID`. Accessible Home copy now consumes the same effective-date authority as presence/camera instead of treating `endedOn === null` as current truth; the future-move regression keeps the old effective Home announced current before `2030-01-01`, keeps the scheduled future Home non-current, and switches wording exactly on the move boundary. The earlier effective-date presence/camera repair and no-new-colour guard remain intact.
- **Follow-up:** Runtime Home-field rendering, touch interaction/context surfaces and any `LivingAtlasApp`/`ParticleEarthScene` integration remain separate follow-up work after the active Playback/Home owner is clear. V1 remains static by default, below selected-Journey emphasis, private from guest shares and without a permanent mobile Home tab.
- **Validation:** Exact rebased base is green `main@c6d21c7c13381cffe2c198dfd90d2dbd93a24292` (main push CI 34391430438). Lightweight local `git diff --check` passed. Exact CODE-head CI run 34393792575 on `f4376eb089035ef7b019bd18eed53c295ff0641d` has `core` and every browser QA lane green, including login-media, playback-prefetch, final-acceptance, mobile-contract and globe-focus-chrome; only `ledger` and aggregate `verify` are expected red because this final reseal was not yet present.
