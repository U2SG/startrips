# PR #304 - Define Home Base Atlas presence and camera policy

- **Source head:** `6720155892c23e884bdae180cbb0fac403159571`
- **Scope:** Issue #233 / ST-028 design-first contract slice only. Adds pure Home Base temporal presence, geographic anchor, label/accessibility/motion descriptors and initial-camera policy, plus focused negative source contracts. It does not integrate Home rendering into `LivingAtlasApp`, Playback or `ParticleEarthScene`, and does not add a mobile Home control or guest-share payload.
- **User-visible change:** Establishes the V1 Home semantics that later Atlas rendering must follow: current Home is a quiet stable origin, historical Home can return only in the matching life period, all-time history leaves only restrained traces, labels use `常住地 · <place>`, and Home may seed a fresh untouched Atlas but never compete with selected-Journey camera ownership.
- **Review fixes:** Fixed the current-head CFAA temporal-truth blocker by replacing the `endedOn === null` shortcut with the existing effective-date authority `resolveHomeBaseForDate` (`startedOn <= date < endedOn`) shared by ordinary/all-time presence and initial Home camera seeding. Added a future-move boundary regression: the still-effective old Home owns presence/camera before `2030-01-01`, and ownership switches exactly on that date to the future period. Earlier source cleanup also makes the no-new-colour guard inspect code rather than mistaking issue references in comments for hex colours.
- **Follow-up:** Runtime Home-field rendering, touch interaction/context surfaces and any `LivingAtlasApp`/`ParticleEarthScene` integration remain separate follow-up work after the active Playback/Home owner is clear. V1 remains static by default, below selected-Journey emphasis, private from guest shares and without a permanent mobile Home tab.
- **Validation:** Exact base is green `main@f0595c19aaba0f9f3ba24e55a9e72490f791a620`. Lightweight local `git diff --check` passed. Exact CODE-source CI run 34387813386 on `6720155892c23e884bdae180cbb0fac403159571` has `core` and every browser QA lane green, including login-media, playback-prefetch, final-acceptance, mobile-contract and globe-focus-chrome. The only expected CODE-head failures are `ledger` and aggregate `verify` because this ledger did not yet exist.
