# PR #559 - Catch real WebGLRenderer construction failure in Particle Earth and degrade instead of crashing

- **Source head:** `5f8d733fa53c94c48deca6191f531c8de35c949d`
- **Scope:** Verified against origin/main: src/scene/ParticleEarthScene.tsx constructs `new WebGLRenderer(...)` inside the useThreeScene(host) initializer with no try/catch, and src/scene/useThreeScene.ts calls `factoryRef.current(host)` directly with no error boundary anywhere in the app, so a real context-creation failure is an uncaught exception exactly as the issue describes. By contrast src/reveal/CoverRevealStage.tsx already does the pattern this issue wants: detectBackend() probes WebGL2 in a try/catch and the `new RevealFlow(...)` construction is separately wrapped in try/catch, publishing a settled degraded state on failure. There is no `data-particle-earth-backend` attribute or forced-failure browser-qa coverage anywhere in the repo today (grep confirms). The real gap is exactly what the issue says: give ParticleEarthScene/useThreeScene/LivingAtlasGlobe's PersistentEarthProvider the same real-constructor try/catch plus one stable degraded state and full teardown, auditing that Cover Reveal's existing context lifecycle keeps disposing correctly alongside it.
- **User-visible change:** Catch real WebGLRenderer construction failure in Particle Earth and degrade instead of crashing
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source and delivery contract; resolved thread dispositions remain in the PR review history.
- **Follow-up:** Every registered delivery member retains its original issue scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 36162613769 attempt 1 passed the real product lanes; independent Source review and per-member acceptance coverage are recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
