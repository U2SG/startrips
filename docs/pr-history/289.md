# PR #289 - Keep superseded merge-readiness runs green

- **Source head:** `fb5790297dd84d18782250775e577b3cdcde1911`
- **Scope:** Change only the merge-readiness controller scheduling/authority model and its contributor documentation. Replace `cancel-in-progress` serialization with live head + newer-run freshness checks so superseded controller executions finish successfully as no-ops instead of leaving cancelled check-runs attached to PR heads.
- **User-visible change:** GitHub should no longer show a red aggregate check on an otherwise-green merged PR solely because an older `merge-readiness-controller` execution was superseded. Required `verify`, `merge-readiness`, explicit `merge-ready`, resolved-review and squash-only gates are unchanged.
- **Important review fixes:** None yet. Final review must specifically verify that removing Actions cancellation does not permit an older sign-off or invalidation run to overwrite a newer event, including the pre/post success race window.
- **Known follow-up:** Historical cancelled check-runs on already-merged PR heads cannot be rewritten; the fix applies to controller runs created after this workflow lands.
- **Validation:** GitHub CI is authoritative. Before merge, exercise rapid review/label activity on this PR and confirm superseded controller runs finish success/no-op, the newest event owns the final `merge-readiness` status, and no cancelled `merge-readiness-controller` check is left on the final head.