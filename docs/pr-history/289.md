# PR #289 - Keep superseded merge-readiness runs green

- **Source head:** `e00c581ef6c2f5c91f3883d5d5240956a5c8a18b`
- **Scope:** Change only the merge-readiness controller scheduling/authority model and its contributor documentation. Replace `cancel-in-progress` serialization with live head + newer-run freshness checks so superseded controller executions finish successfully as no-ops instead of leaving cancelled check-runs attached to PR heads.
- **User-visible change:** GitHub should no longer show a red aggregate check on an otherwise-green merged PR solely because an older `merge-readiness-controller` execution was superseded. Required `verify`, `merge-readiness`, explicit `merge-ready`, resolved-review and squash-only gates are unchanged.
- **Review fixes:** Self-review tightened every mutating rejection path so an older run re-checks authority before writing failure/removing the label. Post-success repair now only rewrites `merge-readiness` when this run still owns the latest status and never removes a label that may already belong to a newer controller.
- **Known follow-up:** Historical cancelled check-runs on already-merged PR heads cannot be rewritten; the fix applies to controller runs created after this workflow lands.
- **Validation:** The workflow parsed and executed on Source head `e00c581e...`. Two rapid review submissions produced runs `34310423470` and `34310430163`: the older run detected one newer readiness run and exited success as a no-op, while the newer run completed success and published the authoritative pending status. Together with the preceding synchronize run `34310368551`, all three `merge-readiness-controller` checks on the Source head completed `success`; none was cancelled. Full typecheck/tests/build/browser validation remains delegated to exact-head GitHub CI on the final ledger-only head.