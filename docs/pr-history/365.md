# PR #365 - Preserve Earth Dive camera intent through reveal synchronization

- **Source head:** `6be1ea0f57c8f28699ce6e19f8365f08bd477c05`
- **Scope:** Implements ST-072 / issue #364 on exact-green base `main@2fad078632bac2c8f3e4a0aa84f953e84b706b9d`. The Detailed Earth reveal lifecycle now separates renderer synchronization from geographic camera intent: renderer-only camera drift is restored before readiness can publish, callbacks armed against an older camera-intent revision are stale, particle calibration advances camera intent, active detail focus flights advance intent on every frame, detail-owned DOM-backed gestures remain newer intent, replacement focus flights cannot be retired by a prior flight, and resize-generated `moveend` cannot retire an explicit focus flight while MapLibre still reports movement. Existing stage/readiness ownership, newest-intent semantics, reduced-motion behavior, anchor/local-scale tolerances and strict Earth Dive QA remain unchanged.
- **User-visible change:** A cold or resized Detailed Earth reveal no longer overwrites the newest legitimate camera movement or publishes readiness from renderer-induced camera drift. Keyboard/normal blend presentation remains intact, and replacement focus flights continue to own the camera until their actual completion/interruption.
- **Review fixes:** Successive exact Sources repaired particle calibration ordering (thread `4009874008`), multi-frame focus-flight ownership after reveal re-arm (`4009912743`), replacement-flight completion ordering (`4009987443`), detail-owned gesture intent (`4010062691`) and resize-generated `moveend` incorrectly retiring an active flight (`4010123015`). Final Source `6be1ea0f57c8f28699ce6e19f8365f08bd477c05` uses MapLibre `isMoving()` to distinguish reveal-resize lifecycle events from actual focus-flight completion. Fresh exact-head automated review `5671691296` reported no major issues. All current non-outdated review threads were replied to and resolved with exact-head evidence; only outdated historical threads remain unresolved.
- **Follow-up:** ST-066 / PR #361 remains frozen without ledger until this ST-072 repair is maintainer-merged and the resulting exact-main push CI is green. ST-070 remains pending behind the ST-072 -> ST-066 chain. This PR does not change route geometry, Journey Rail, markers, labels, recorded tracks, Backend work, tags, releases or deployment.
- **Validation:** Local lightweight validation is limited to repository checks; no heavy local build/browser run is used. Exact Source CI `34904098736` on `6be1ea0f57c8f28699ce6e19f8365f08bd477c05` was executed twice on the identical frozen Source. Attempts 1 and 2 both passed `core`, `browser-qa / earth-dive`, `browser-qa / route-anchoring`, `browser-qa / globe-render-budget`, `browser-qa / final-acceptance` and the observed required product/browser matrix; only the intentionally absent-ledger `ledger` and downstream `verify` were red pre-seal. Attempt 1 recorded stable 1920x1080 cold-reveal camera evidence `revealCameraBefore="103.958170,32.338488,6.227385,0.000000,0.000000"` == `revealCameraAfter`, and attempt 2 recorded `revealCameraBefore="101.684614,32.475049,6.260048,0.000000,0.000000"` == `revealCameraAfter`; first-reveal camera pairs were also byte-equal in both attempts. No retry/wait/polling/sleep/timeout inflation/force-click/1px resize/tolerance widening was added. This commit is ledger-only; exact final-head GitHub CI remains authoritative before HANDOFF_REVIEW.
