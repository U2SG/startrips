# PR #280 - Continuous story apertures across image aspect ratios

- **Source head:** `643e6088ae128df32004f3ed422d1158f8f0dab6`
- **Scope:** Story page aperture motion, shared spring inset channels, drag settlement and mixed-aspect browser regression.
- **User-visible change:** Portrait and landscape photographs retain their proportions while their visible stack boundaries change continuously, instead of snapping when the current media identity changes.
- **Review fixes:** Remote review of the aperture implementation completed without findings. Frame review also caught scientific notation in very small computed insets; both interruption reads and the regression trace now retain that precision.
- **Follow-up:** Review CI motion recording, then verify the merged deployment with real mixed-aspect media.
- **Validation:** Initial CI run 34231566718 passed all jobs and recorded mixed-aspect navigation. The final precision correction awaits its own CI run. GitHub CI owns tests, typecheck, build and browser verification; no private media is committed.
