# PR #483 - Gate story-mixed-aspect-aperture-continuity on reached navigation seams, not sampled requestAnimationFrame boundary count

- **Source head:** `7304d1b00e3a1ebaa5b41c1b3ffc78eaa0324b4d`
- **Scope:** Verified on origin/main scripts/qa-media-controls.mjs: the story-mixed-aspect-aperture-continuity check's jump/discontinuity assertion at ~L2524-2531 (clip-path delta bound `delta > Math.max(2, elapsed*.16)`) already correctly enforces the product property -- no aperture jump at a mixed-aspect seam -- and is not the reported problem. The real gap is ~L2579: `apertureFailed = stable.aperture.boundaries < 12 || stable.aperture.jumps.length > 0`, where `aperture.boundaries` only increments when the requestAnimationFrame sampler installed at ~L2517-2540 happens to catch a role transition within an 80ms window; CI scheduling jitter can skip the exact frame a transition occurs on, exactly as evidenced on PR #475 run 35611808543 attempt 1 (`boundaries=11`, `jumps=[]`, lane is actually named `browser-qa / login-media`, not `auth-media` as the issue body states). The eight-photo navigation sequence at ~L2555-2564 already awaits `waitForStoryPicture` per transition, so an explicit reached/settled seam identity is derivable without relying on frame sampling; the fix replaces the `boundaries < 12` proxy with that per-seam evidence while leaving the jump assertion and navigation sequence untouched.
- **User-visible change:** Gate story-mixed-aspect-aperture-continuity on reached navigation seams, not sampled requestAnimationFrame boundary count
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35641090139 attempt 2 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
