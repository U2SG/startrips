# PR #502 - Bound Route Point spline handles by both adjacent legs to remove whisker/hook artifacts

- **Source head:** `8333339b94dcd321841c9320c2f1bc0c2e7029a8`
- **Scope:** PR #361/ST-066 (merged, on main) fixed the hard V-kink at Route Points with a shared per-anchor tangent, but src/scene/geo.ts still derives each handle's magnitude (splineHandleAngle, MAX_ROUTE_SPLINE_HANDLE_RATIO=0.5) from its own leg's angular length alone, and existing geo.test.ts monotonicity checks (e.g. 'keeps asymmetric near-reversal spline progress inside each leg without backward hooks') only assert forward progress-along-one-leg, not that the sampled path never crosses back across the shared anchor tangent half-plane between the incoming and outgoing leg. That is the real residual gap: a long leg can still lend an oversized handle to a short neighbour, producing a small reverse-direction lobe/whisker near an interior Route Point when adjacent leg lengths or turn angles differ sharply, exactly as reported for the Los Angeles / Las Vegas / Kingman / Page / Grand Canyon sequence. scripts/qa-route-anchoring.mjs's existing qa-route-southwest fixture (San Diego/Palm Springs/Las Vegas/Grand Canyon) only asserts marker/line endpoint alignment and leg-bulge fraction, not anchor-adjacent hook detection, so no CI lane currently catches this.
- **User-visible change:** Bound Route Point spline handles by both adjacent legs to remove whisker/hook artifacts
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35704210420 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
