# PR #403 - fix: scope control-plane execution guards by lane

- **Source head:** `70c4dea727807ab27623fbebd4eab5a1d6dc68e6`
- **Scope:** Scope execution exclusion by lane while preserving fail-closed same-lane and same-owner semantics, make Experience one-shot execution independent of the LOCAL Backend lifecycle, and harden Windows/MSYS self-identification with invocation-token plus PID-and-CreationDate evidence.
- **User-visible change:** Experience work can run independently of Backend without allowing duplicate same-lane, same-feature, or same-worktree owners.
- **Review fixes:** Independent Maintainer exact-Source review is CLEAR for `70c4dea727807ab27623fbebd4eab5a1d6dc68e6`; all review threads are resolved; the final unreadable Windows/MSYS carrier P1 is covered by the real direct `launch-experience.sh` path and reused-PID/foreign-carrier fail-closed regression coverage.
- **Follow-up:** Existing issue #399 retains the control-plane root-cause history; this ledger adds no product behavior or new owner authority.
- **Validation:** Exact Source CI `35299830425` passed every real product and control-plane lane against current main baseline `f952907db6aea37da3f37a45a8f8d8b40bce7d93`; only the expected pre-ledger `ledger` and downstream `verify` failed. Fresh real Windows/MSYS direct launch reached the expected bounded rc=3 with `competing_executions=[]` and a successful workspace permission probe. This final commit changes only this ledger; exact final-head CI remains required before HANDOFF_REVIEW.
