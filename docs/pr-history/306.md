# PR #306 - Require explicit Full Playback after Quick Recap overflow

- **Source head:** `42e306ee9deef56ad451231d1d5ce795eae9ef0c`
- **Scope:** ST-011 only. Changes the Living Atlas handling of Quick Recap `over-budget` results so they stop at an explicit choice surface instead of silently transitioning to Full Playback. The Quick Recap planner, timing constants, selected-content rules, Home/session playback topology, same-asset preview layering and the `no-visual-media` fallback remain unchanged.
- **User-visible change:** When required Journey content cannot fit the Quick Recap target, the user sees `当前回顾时长放不下所有必要的旅程点。` and an explicit accessible `完整播放` action. Full Playback does not mount or claim playback-entry/soundtrack preparation until that action is chosen; short and medium Quick Recaps continue through the existing path.
- **Review fixes:** Closed the earlier keyboard-focus and same-ID Journey edit findings, then fixed the newer stale-decision P2 on the final Source head. The over-budget choice is now keyed to a deterministic Quick Recap planning-content fingerprint derived from the planner's normalized visual-media digests, candidate Route Point order and route geometry, so same-revision media identity/order/Route Point assignment/cardinality and planning geometry changes invalidate the Full-only surface immediately. The existing explicit `完整播放` focus transfer remains intact; unrelated `updatedAt` churn does not invalidate the choice.
- **Follow-up:** V1 deliberately does not add automatic compression or silently start Full Playback. Future adaptive compression, if desired, needs a separate product rule for what content or dwell may be reduced. `no-visual-media` remains outside ST-011 and keeps its existing fallback behavior.
- **Validation:** Exact base remains green `main@1ebf01a680ea14c2fc3e32f3d7f2d98ba6819607` (main push CI 34409424496, successful rerun attempt 2). Current repair `git diff --check` passed; local `pnpm typecheck` could not execute because this worktree intentionally has no `node_modules`, so executable validation stayed in GitHub CI. Exact CODE-head CI run 34420946488 on `42e306ee9deef56ad451231d1d5ce795eae9ef0c` has `core` (including typecheck/tests/build) and every browser QA lane green; `ledger` and aggregate `verify` are expected red on the CODE head because this final ledger-only reseal was not present yet.
