# PR #288 - Prove mobile Story continuity across route-point swipes

- **Source head:** `c9c1e47aba67c09e13151eb57ab0d89b6b5c1224`
- **Scope:** Issue #65, ST-012 residual continuity evidence only. Preserve the merged persistent prev/current/next `StoryMediaPages` compositor and add a QA-only two-Route-Point Journey plus real Chromium touch sequences across 320/360/390/430 portrait and phone landscape. The evidence covers within-point, cross-Route-Point and reverse-crossing swipes without introducing another carousel or production media owner.
- **User-visible change:** None intended in this slice; the shipped Story compositor remains authoritative. The PR strengthens regression evidence that current media follows the finger, release continues through the existing spring/semantic handoff, the stable page stack never exposes a legacy incoming/loading owner, and route-boundary swipes settle on the correct logical asset.
- **Review fixes:** P2 review found the first QA version stopped sampling at `touchEnd`. Final Source samples every RAF from release through semantic settlement, measures the first post-release gap, keeps drag visual ownership separate from the release-phase input owner, requires all non-current slots to stay pointer-inert during the handoff, and verifies the settled current page again owns input and highest z-order. This avoids falsely rejecting the intentional visual destination lift during the spring while still catching a second input owner, incoming overlay, hitch or bad settled stack.
- **Follow-up:** Production Story media ownership, mixed-media controls, fullscreen synchronization, deferred-fullscreen cancellation and Story-return focus remain owned by their merged contracts. Real-device/manual feel can continue as product QA without reopening a second compositor authority.
- **Validation:** Lightweight local validation was limited to `git diff --check` and `node --check scripts/qa-media-controls.mjs`. Exact Source-head CI run https://github.com/U2SG/startrips/actions/runs/34308550749 verifies `core`, `browser-qa / login-media`, `final-acceptance`, `playback-prefetch`, route/city/earth/mobile/globe/share lanes all green on `c9c1e47aba67c09e13151eb57ab0d89b6b5c1224`; only the intentionally missing PR ledger and aggregate `verify` failed before this final ledger seal.
