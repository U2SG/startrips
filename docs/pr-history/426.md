# PR #426 - [Backend] Prove and harden the native password-reset server lifecycle and session revocation

- **Source head:** `50abc859cf99f5669f9b6931896eca142b6e4a96`
- **Scope:** Backend-only child of #344 (the parent keeps all AuthGateway/client recovery-state work). Already on main: `server/auth.ts` configures `emailAndPassword.sendResetPassword`, `minPasswordLength: 10`, `revokeSessionsOnPasswordReset: true` and the `/request-password-reset` custom rate rule (3 per 10 min, database storage), and `server/request-log.ts` redacts `/api/auth/reset-password/<token>` with coverage in `server/request-log.test.ts`. The real gap: `git grep` on origin/main finds no test that drives `/api/auth/request-password-reset` or the reset-password endpoint at all, so anti-enumeration, expiry/replay/concurrency, session revocation after reset and stable user/Atlas invariance are unproven, and only a proven gap in pinned Better Auth 1.6.23 justifies a narrow server adapter — no second reset-token table and no custom password hashing.
- **User-visible change:** [Backend] Prove and harden the native password-reset server lifecycle and session revocation
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35436011371 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
