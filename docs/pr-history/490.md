# PR #490 - Simplify upload locks, geometry and Home Base inference

- **Source head:** `9db73a4bb4c8cc216516db6b9bdcb603904e2130`
- **Scope:** Reuse the existing Atlas lock and a private Journey lock query at ten upload call sites; share position/targetPosition geometry construction at twelve scene call sites; replace suffix qualification's discarded evidence-region projection with one support/date scan. Production code is reduced by 85 net lines, with six public inference regression cases added.
- **User-visible change:** None intended. Upload authorization, errors, lease recovery, media move/undo, scene geometry and Home Base suggestions retain their behavior. The continuity-window qualification path no longer calculates an unused quadratic medoid; actual candidate medoids remain unchanged. No whole-algorithm or end-to-end latency improvement is claimed.
- **Review fixes:** Preserve required explicit Atlas scope, including the two existing trusted-upload unscoped queries; retain lock order and sorted cross-Journey locks. Keep slice/burst buffers, bounding spheres, LOD attributes and disposal with their original scene owners. Candidate qualification still uses the requested window span, suggested qualification the actual evidence span; distinct Journey/start/end counts, calendar rules, currentness and digest remain unchanged.
- **Follow-up:** None required for this bounded refactor. Auth/provider, route spline, schema/migrations and external ONE state are outside this delta.
- **Validation:** Exact Source independently reviewed CLEAR across all four files; static diff check passed. [Source CI 35680424293](https://github.com/U2SG/startrips/actions/runs/35680424293) passed core (164 test files / 2,351 tests, including 72 Home Base inference cases; 352 control-plane tests; typecheck and production build), Keepsake rendering and all eight browser QA groups. Source failed only for the not-yet-added current-PR ledger and its dependent verify gate. No local tests, typecheck, build, browser or database execution. The final commit changes only this ledger; exact final CI must prove Source reuse and pass before merge.
