# PR #279 - Extract globe pointer intent decisions without moving authority

- **Source head:** `ae977cd67e16fd1c900b7a39bd7c6d869862ae73`
- **Scope:** Source issue #269. Add `docs/architecture/hotspot-authority-map.md` for the Particle Earth and Story hotspots, then extract exactly one boundary: pure pointer/drag/wheel decision helpers and their constants move from `ParticleEarthScene.tsx` into `src/scene/globePointerIntent.ts`, with their rule tests moving to `globePointerIntent.test.ts`.
- **User-visible change:** None intended. Globe drag, pinch, wheel zoom, activation, inertia, projection, focus and Earth Dive behavior remain owned by the same `ParticleEarthScene` composition root.
- **Review fixes:** None yet.
- **Follow-up:** Later #269 phases may extract other pure/read-only boundaries, including Story adapters or renderer/QA publishers, but this PR deliberately does not move current focus, gesture, media, history, renderer or projection authority.
- **Validation:** Local `git diff --check` and static boundary checks pass; the fresh worktree has no `node_modules`, so typecheck/tests/build/browser validation is delegated to GitHub CI. Required authoritative lanes include `core`, `route-anchoring`, `city-label-anchoring`, `earth-dive`, `globe-focus-chrome`, `ledger` and `verify`.
