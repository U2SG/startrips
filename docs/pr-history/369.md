# PR #369 - Unify recovery surfaces with v12 brand motion

- **Base:** `e2d44349a71a99b382a19e7cf7e4fe976ff825b0`
- **Source head:** `96b039bb08e925a5a20e2a388dc696dbd33c4d36`
- **Scope:** Implements ST-070 / issue #359 as one bounded app-level recovery language for 404/not-found, genuine empty and generic recoverable error/unavailable states. The implementation reuses the canonical v12 signature timeline/geometry/runtime from #302/#329, adds the semantic `recovery` clip without a second authored timeline or animation engine, adds DOM-free recovery descriptors, one reusable recovery surface, and focused browser QA. CTAs are available immediately; interruption/newest-intent wins; stale callbacks cannot restart motion; hidden/offscreen suspension and reduced-motion behavior reuse the existing runtime contract. Auth, share-expiry, media-specific empty states, success/arrival motion, idle companion, Journey/Route Point/Playback ownership and a new general animation framework remain out of scope.
- **User-visible change:** Startrips now presents coherent branded recovery for 404, empty and generic error states without making brand motion a gate. Empty remains non-error, retry is shown only when truthful, 404 retains an immediate Atlas route plus secondary back action where available, reduced-motion resolves to the approved static identity, and desktop/compact-mobile actions preserve stable layout and at least 44px targets.
- **Review fixes:** Review-driven follow-ups preserved the embedded empty-state heading hierarchy, restored the standalone 404 code emphasis, unified generic-error CTA styling, and made interruption terminal even when recovery is currently under Reduced Motion so a later motion-preference change cannot restart animation after a newer CTA/navigation intent. Fresh exact-head Codex review comment `5678934219` reviewed Source `96b039bb08e925a5a20e2a388dc696dbd33c4d36` and reported no major issues; current non-outdated review threads are resolved.
- **Follow-up:** Owner-requested rebase dropped the prior seal and replayed the Experience code stack onto exact-green main `e2d44349a71a99b382a19e7cf7e4fe976ff825b0` after Backend PR #370. The pre-review rebased patch matched the prior Experience product patch by stable patch-id; no #370 Backend implementation was modified. The previous Source/final/review evidence is historical only. No merge-ready, signing, merge, tag, release or deployment authority is granted by this ledger; Experience Worker stops at HANDOFF_REVIEW after exact final-head CI and merge-readiness succeed.
- **Validation:** Exact Source CI run `34959419163` attempt 2 on `96b039bb08e925a5a20e2a388dc696dbd33c4d36` has `core`, focused `browser-qa / recovery-surfaces`, `browser-qa / final-acceptance`, `browser-qa / earth-dive`, `browser-qa / route-anchoring`, `browser-qa / globe-render-budget`, city-label anchoring and all other required observed product/browser lanes SUCCESS. The city-label lane required one same-head targeted rerun after an unrelated island-label reachability intermittent; pre-seal red is limited to the intentionally absent per-PR `ledger` and downstream `verify`. This commit adds only `docs/pr-history/369.md`; exact final-head ledger/verify/core/recovery-surfaces/final-acceptance/earth-dive/route-anchoring/globe-render-budget plus all required CI is authoritative before handoff.
- **Boundary:** No merge-ready, signing, merge, tag, release or deployment authority is granted by this ledger. Experience Worker stops at HANDOFF_REVIEW after exact final-head CI and merge-readiness succeed.
