# PR #283 - Explain Quick Recap media selection

- **Source head:** `bb3a3619577d40bf7aa242f91f38f15d15109471`
- **Scope:** Issue #127, ST-022 explainability slice only. Add the pure `quickRecapSelectionSummary` builder and core tests, pass the existing active Quick Recap Edit Plan plus canonical source Journey into `JourneyPlaybackOverlay`, and render a per-Route-Point disclosure of included and omitted Route Point Media using closed reason-code unions. The deterministic planner, pacing, chapter budget, trims, playback transport, Story media motion and canonical Journey data are unchanged.
- **User-visible change:** Quick Recap exposes a compact `本次整理` disclosure showing which Route Point Media the deterministic recap selected or omitted and why. Full Playback does not render the disclosure.
- **Review fixes:** Automated P2 review `3959540528` found the native `<summary>` disclosure was omitted from Playback's custom Tab focus trap. The trap now includes `summary`, so keyboard-only users can reach `本次整理` without changing the overlay's existing focus ownership.
- **Follow-up:** Production MediaDigest enrichment, duplicate intelligence and optional AI/VLM planning remain later #127 work and are deliberately not broadened into this PR.
- **Validation:** Local validation was limited to `git diff --check` and static scope/boundary inspection in a fresh worktree with no `node_modules`; no local test, build or browser matrix ran. GitHub CI owns typecheck, unit tests, build and browser validation on the submitted ledger head.
