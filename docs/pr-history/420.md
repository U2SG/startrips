# PR #420 - Gate the Earth Dive hard-policy stage fixture on an observable detail construction

- **Source head:** `7adad1922acc4fe259da1bc0acf5f11a6915f2aa`
- **Scope:** Issue #411. In `scripts/qa-earth-dive.mjs`, both iterations of the hard-policy `for (const targetStage of ["prewarm", "blending"])` loop now wait for an observable detail construction - a risen `window.__detailedEarthMapConstructionCount` and a mounted `.detailed-earth-map` node - after their stage predicate and before the pre-switch snapshot, with no further wheel input so the stage under test is held. A snapshot reporting `before.mapConstructionCount < 1` is pushed into `intermediatePolicyFailures`.
- **User-visible change:** None. QA fixture only.
- **Review fixes:** None yet.
- **Follow-up:** None. The residual gap #411 was kept open for - the `prewarm` and `blending` iterations gating on the `data-earth-dive` stage attribute, which flips before a detail map is observable, so the ST-084 particle-only teardown contract could pass vacuously - is closed here. The root-cause repair from 88ecbec is preserved unchanged.
- **Validation:** `./init.sh smoke` locally (`pnpm typecheck`, `git diff --check`, `node --check`, `node scripts/pr-history.mjs validate-all` / `validate-pr`). The `browser-qa / earth-dive`, `core` and `ledger` lanes in GitHub Actions remain authoritative for behaviour.
