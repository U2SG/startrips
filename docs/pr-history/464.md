# PR #464 - Make the portrait-phone first-frame verdict in cover-reveal-opening deterministic against the renderer's own blend identity

- **Source head:** `8bbe50a68bcbbd675293942e7f639a1825a72577`
- **Scope:** Already on main via ST-106/PR #458: src/reveal/CoverRevealStage.tsx exposes data-cover-reveal-composited from coverRevealFrameImage() in src/reveal/coverRevealFlow.ts, and scripts/qa-cover-reveal-opening.mjs grades desktop/first-frame-is-the-derivative and desktop/the-reveal-actually-transitions on that renderer-owned identity instead of a raced pixel probe; the original desktop race from #454/PR #453 is fixed and stable. Real residual gap (Hourly Review recurrence after #458, PR #461 Source e359ef9e9f757f33ef305c0c8a9d96f6eeb0b411 run 35513924574 and PR #460 Source b7107d4c3cede84d90e6c8a7a4d506dc137266fc run 35514145553, both against exact current main 32428d73cd36569f82b6da933d54a0b92d1c8377): coverRevealFrameImage() buckets every progress strictly between 0 and 1 as a single 'blend' identity regardless of per-pixel mask completion, so the check's gradable-sample filter accepts a stable 'blend'-identity sample and still requires its classified colour to be 'derivative'. On portrait-phone's smaller/narrower viewport the mask reaches REVEAL_PROBE's corner (0.1, 0.12) and fully converts it to the original-cover colour while progress is still short of 1 (observed composited identities [generated-first, blend], opened colours [derivative, original-cover, original-cover]), so a correctly-behaving reveal now fails first-frame-is-the-derivative on portrait-phone specifically. This is a new, narrower race than the one #458 fixed, not a repeat of it.
- **User-visible change:** Make the portrait-phone first-frame verdict in cover-reveal-opening deterministic against the renderer's own blend identity
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35522664569 attempt 2 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
