# PR #316 - Reconcile unknown first-Journey create results

- **Issue relationship:** `Fixes #248` — closes the residual first-Journey create/recovery truth gap while preserving the existing empty-Atlas entry and server-owned Journey identity.
- **Source head:** `447a53767d7a4c83362580ae4e0c36a37d5bdbe6`
- **Scope:** Unknown create outcomes are reconciled against authenticated `GET /api/journeys` using the pre-create Journey-id snapshot and canonical submitted metadata/ordered Route Points. Exactly one post-attempt match may be adopted; zero matches is explicitly not-persisted and permits one deliberate resubmission; 2+ matches return `ambiguous` confirmation-only state with no arbitrary identity, media continuation, arrival ownership, or retry. Once create returns a server Journey, later media/assignment failures use `JourneyMediaContinuationError`, preserve that id, and never enter unknown-create reconciliation. Persistent read-back failure keeps the unresolved attempt in `LivingAtlasApp`, but X/Escape may safely leave once the GET settles; reopening the create surface restores the submitted draft, original RouteDraftPoint ids, pending in-memory media assignments, and pre-attempt id snapshot, and remains reconciliation-only until persistence is established. `LOCATION_SEARCH_UNAVAILABLE` now points to globe-pick and existing `精确位置` coordinates, and arrival handoff is initial-save only.
- **User-visible change:** An uncertain first save no longer encourages a blind second create. The Composer recovers one unique persisted Journey, allows one retry only after confirming no persistence, or blocks ambiguous results for Atlas refresh/confirmation. Known-persisted media failures keep the Journey saved and ask the user to reopen it for media continuation; media retry cannot reclaim first-arrival focus. A prolonged Journey-list outage no longer traps the user in Composer: after each failed recheck they may exit safely, while reopening still requires reconciliation and never exposes a blind create action. Pending media and its Route Point assignment intent survive that safe close/reopen for the current app session, so unique adoption resumes upload against the recovered server Journey/Route Point instead of silently dropping the files. Reconciliation-read failure copy now keeps the primary recovery path inside that same app session and explicitly warns that a full page refresh discards pending local media/Route Point intent and requires reselection; it no longer advertises refresh as a preserving action.
- **Review fixes:** P2 `3986293017` found arbitrary adoption when multiple new Journeys matched; final Source requires exactly one and tests the explicit ambiguous state. A second owner P2 found create-result certainty collapsed with post-persist media failure; final Source carries the confirmed Journey in `JourneyMediaContinuationError` and handles it before unknown-create classification, with focused create-success + assignment-failure coverage. The earlier identity P2 was answered and resolved after exact-Source verification. Final-head P2 `3986637609` found that persistent `recheck` disabled every Composer exit; Source `3bececa` lifts the unresolved attempt to `LivingAtlasApp`, enables X/Escape whenever no GET/save is in flight, restores the pending draft on reopen, and clears the lifted marker only after authoritative recovery/success. Focused tests cover repeated reconciliation GET failure and repeated reopen remaining reconciliation-only. That P2 was answered with the fix commit and resolved after exact CODE-head product CI/source verification. A fourth owner P2 `3986999032` found that safe close/reopen preserved unknown-create identity but dropped component-local pending media and Route Point assignment intent. Final Source `5b64fdd` lifts those in-memory values into `UnknownJourneyCreateAttempt`, restores them on reopen, and adds focused coverage for GET failure → safe exit/reopen → unique adoption → media upload against the recovered Journey/Route Point with no second create. The thread was answered by `3987069135` and resolved after exact-Source verification. A fifth owner P2 `3987470177` found that the GET-failure copy still recommended “安全关闭创建器后刷新 Atlas”, contradicting the newly in-memory-only recovery lifetime. Final Source `447a537` changes the recheck path to recheck or close/reopen within the current Atlas session, explicitly warns that full page refresh loses pending local media/Route Point assignment intent and requires reselection, and removes refresh-as-preservation wording from ambiguous recovery. Focused `JourneyComposer` coverage pins the pending-media copy contract. The thread was answered by `3987668342` and resolved after exact CODE-head product CI/source verification. Automated review service was quota-unavailable; owner/source/CFAA evidence is authoritative.
- **CFAA:** Changed dimensions are create-result certainty, persisted identity ownership, media continuation, and save-callback ownership. Recovery stays tenant-scoped through the existing authenticated Journey list; only unique server identity is adoptable. Known-persisted failures retain server identity. `resolveJourneyArrivalHandoff` grants focus only to `initial-save`; `media-retry` has none. The parent-held uncertainty marker changes only Composer create ownership; it now carries the pending in-memory media and original RouteDraftPoint ids for the lifetime of the app session, editing another Journey does not erase it, successful adoption explicitly clears it, and recovery copy does not claim that a destructive full page refresh preserves that in-memory payload. No Story return/Playback handoff, shared-element, globe scene, auth, schema, repository, storage, or route contract changes.
- **Follow-up:** None required for ST-039. Ambiguous recovery intentionally stops at confirmation/refresh rather than auto-deleting or auto-merging duplicates; any duplicate-management UX is separate work. The reported late-save selection-steal remains non-reproducible under the modal save lock and was not fabricated into this change.
- **Validation:** Lightweight local validation on final Source: `git diff --check` and `pnpm typecheck` passed; no local Vitest/full build/browser/dev-server/docker/PostgreSQL ran. Exact CODE-head CI `34583070985` on `447a53767d7a4c83362580ae4e0c36a37d5bdbe6` passed `core` and every browser/product lane: login-media, playback-prefetch, final-acceptance, post-login, route, city-label, earth-dive, mobile-contract, globe-focus, route-point-context, globe-render-budget, guest-share and owner-share. Only `ledger` and aggregate `verify` failed because this Source intentionally preceded the resealed ledger. Final ledger-head CI is authoritative for ledger/verify sign-off.
