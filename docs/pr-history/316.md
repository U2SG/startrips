# PR #316 - Reconcile unknown first-Journey create results

- **Issue relationship:** `Fixes #248` — closes the residual first-Journey create/recovery truth gap while preserving the existing empty-Atlas entry and server-owned Journey identity.
- **Source head:** `0cd3d352ce03a8dd3b1a900855676bcd5c3b727a`
- **Scope:** Unknown create outcomes are reconciled against authenticated `GET /api/journeys` using the pre-create Journey-id snapshot and server-aligned canonical submitted metadata/ordered Route Points. Zero post-snapshot canonical matches is the only `not-persisted` verdict and permits one deliberate resubmission. One canonical match without server-bound attempt provenance is now `confirmation-required`: content equality may show similarity but cannot prove that this specific POST created that Journey, so the Composer neither adopts its id nor uploads pending media, hands it arrival/focus ownership, or retries create. Two or more matches remain `ambiguous` confirmation-only. Once create itself returns a server Journey, later media/assignment failures use `JourneyMediaContinuationError`, preserve that authoritative id, and never enter unknown-create reconciliation. Persistent read-back failure keeps the unresolved attempt in `LivingAtlasApp`; X/Escape may safely leave once the GET settles, and reopening restores the submitted draft, original RouteDraftPoint ids, pending in-memory media assignments, and pre-attempt id snapshot without exposing a blind create path. `LOCATION_SEARCH_UNAVAILABLE` still points to globe-pick and the existing `精确位置` coordinate entry, and arrival handoff remains initial-save only.
- **User-visible change:** An uncertain first save no longer encourages a blind second create or silently claims a content-identical Journey created by another tab/device. If reconciliation finds one unverifiable matching Journey, the Composer explicitly asks the user to close and verify it in Atlas; the current session keeps pending local media/Route Point intent but does not attach them to that Journey or trigger arrival. Zero confirmed matches can reopen one deliberate submit; 2+ matches remain ambiguous. Journey-list outage recovery stays within the current app session, and copy explicitly warns that a full page refresh discards pending local media/Route Point intent. Known-persisted post-create media failures still keep the returned Journey saved and never re-enter unknown-create recovery.
- **Review fixes:** P2 `3986293017` fixed arbitrary adoption when multiple new Journeys matched by making 2+ matches ambiguous. A second owner P2 separated create-result uncertainty from post-persist media failures with `JourneyMediaContinuationError`. P2 `3986637609` restored a safe exit for persistent `recheck` while preserving uncertainty across reopen. P2 `3986999032` preserved pending in-memory media and Route Point assignment intent across safe close/reopen. P2 `3987470177` corrected recovery copy so full page refresh is never advertised as preserving those in-memory inputs. P2 `3988018306` aligned Route Point `note` canonicalization with server write normalization so whitespace-only notes cannot cause a false `not-persisted` verdict. The seventh owner CFAA on historical head `64c49c0` found that pre-create id snapshot plus canonical equality still did not prove create-attempt ownership under concurrent writers. Source `5d62362` fails closed: a sole post-snapshot canonical match returns `confirmation-required` with only the matching id, never a `Journey` object that can flow into media or arrival continuation; zero remains the only resubmittable verdict and 2+ remains ambiguous. Focused pure recovery and Composer coverage models another session creating the sole identical Journey after the snapshot and asserts no automatic create, media continuation, or save/arrival callback. The authenticated integration assertion is updated to the same confirmation-only contract. Repository search found no Journey-create request/idempotency token; unrelated upload `completionAttemptId` is upload-scoped and is not treated as Journey-create provenance. The seventh repair is recorded in PR comment `5633637684` and issue #248 comment `5633641796`; exact-source CI/review grace produced no newer source finding. P2 `3988863503` then found that confirmation-required/ambiguous safe close could return to a stale parent Atlas even though Composer had just read current server truth. Final Source `0cd3d35` preserves the unresolved attempt first, closes Composer, and performs a read-only parent Atlas refresh for confirmation-required/ambiguous exits. That refresh never calls `onSaved`, adopts no candidate, uploads no media, hands no arrival/focus ownership, and leaves the same pending File/Route Point intent available on reopen. Focused LivingAtlasApp coverage proves the candidate becomes Atlas-visible while the unresolved confirmation state remains intact; the refresh-failure notice also stays neutral and does not claim the Journey was saved.
- **CFAA:** Changed dimensions are create-result certainty, persisted identity ownership, media continuation, and save-callback ownership. Canonical equality is no longer treated as ownership. The authenticated Journey listing remains tenant-scoped, but without server-bound attempt provenance no unresolved create path can automatically adopt any matching server id. Recovery canonicalization still mirrors server write normalization for Route Point notes. Known-persisted failures retain the actual server-returned identity. `resolveJourneyArrivalHandoff` still grants focus only to an authoritative `initial-save`; confirmation-required/ambiguous recovery never reaches that callback. The parent-held uncertainty marker remains current-session-only and carries pending media plus original RouteDraftPoint ids without claiming that page refresh preserves them. Confirmation/ambiguous safe close may refresh authenticated Atlas rows only as read-only server truth; that path does not cross the save/arrival/media ownership boundary. No Story return/shared-element/globe scene/playback/auth/schema/storage changes are introduced.
- **Follow-up:** If automatic adoption after an unknown create is desired later, it requires an explicit Journey-create idempotency/request-attempt contract persisted by the server and readable during reconciliation. This PR intentionally does not add schema/migrations merely to preserve auto-adoption. Duplicate-management/confirmation UX beyond the current fail-closed Atlas verification path remains separate work.
- **Validation:** Lightweight local validation on final Source: `pnpm typecheck` and `git diff --check` passed; no local Vitest/full tests/build/browser/dev-server/docker/PostgreSQL ran. Exact CODE-head CI `34598230072` on `0cd3d352ce03a8dd3b1a900855676bcd5c3b727a` passed `core` and every browser/product lane: login-media, playback-prefetch, final-acceptance, post-login, route anchoring, city-label anchoring, earth-dive, mobile-contract, globe-focus, route-point-context, globe-render-budget, guest-share and owner-share. Only `ledger` and aggregate `verify` failed because this Source intentionally still carried the historical ledger. Final ledger-head CI is authoritative for ledger/verify sign-off.
