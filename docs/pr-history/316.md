# PR #316 - Reconcile unknown first-Journey create results

- **Issue relationship:** `Fixes #248` — closes the residual first-Journey create/recovery truth gap while preserving the existing empty-Atlas entry and server-owned Journey identity.
- **Source head:** `abd75f472d14135f1b1feb17940762fe7bc04e9b`
- **Scope:** Unknown create outcomes are reconciled against authenticated `GET /api/journeys` using the pre-create Journey-id snapshot and canonical submitted metadata/ordered Route Points. Exactly one post-attempt match may be adopted; zero matches is explicitly not-persisted and permits one deliberate resubmission; 2+ matches return `ambiguous` confirmation-only state with no arbitrary identity, media continuation, arrival ownership, or retry. Once create returns a server Journey, later media/assignment failures use `JourneyMediaContinuationError`, preserve that id, and never enter unknown-create reconciliation. `LOCATION_SEARCH_UNAVAILABLE` now points to globe-pick and existing `精确位置` coordinates, and arrival handoff is initial-save only.
- **User-visible change:** An uncertain first save no longer encourages a blind second create. The Composer recovers one unique persisted Journey, allows one retry only after confirming no persistence, or blocks ambiguous results for Atlas refresh/confirmation. Known-persisted media failures keep the Journey saved and ask the user to reopen it for media continuation; media retry cannot reclaim first-arrival focus.
- **Review fixes:** P2 `3986293017` found arbitrary adoption when multiple new Journeys matched; final Source requires exactly one and tests the explicit ambiguous state. A second owner P2 found create-result certainty collapsed with post-persist media failure; final Source carries the confirmed Journey in `JourneyMediaContinuationError` and handles it before unknown-create classification, with focused create-success + assignment-failure coverage. The inline P2 was answered and resolved after exact-Source verification. Automated review service was quota-unavailable; owner/source/CFAA evidence is authoritative.
- **CFAA:** Changed dimensions are create-result certainty, persisted identity ownership, media continuation, and save-callback ownership. Recovery stays tenant-scoped through the existing authenticated Journey list; only unique server identity is adoptable. Known-persisted failures retain server identity. `resolveJourneyArrivalHandoff` grants focus only to `initial-save`; `media-retry` has none. No Story/Playback handoff, shared-element, globe scene, auth, schema, repository, storage, or route contract changes.
- **Follow-up:** None required for ST-039. Ambiguous recovery intentionally stops at confirmation/refresh rather than auto-deleting or auto-merging duplicates; any duplicate-management UX is separate work. The reported late-save selection-steal remains non-reproducible under the modal save lock and was not fabricated into this change.
- **Validation:** Lightweight local validation on final Source included `pnpm typecheck` and `git diff --check`; no local Vitest/full build/browser/dev-server/docker/PostgreSQL ran. Exact CODE-head CI `34568105762` on `abd75f472d14135f1b1feb17940762fe7bc04e9b` passed `core` and every browser/product lane: login-media, playback-prefetch, final-acceptance, post-login, route, city-label, earth-dive, mobile-contract, globe-focus, route-point-context, globe-render-budget, guest-share and owner-share. Only the intentionally absent pre-ledger `ledger` and aggregate `verify` checks failed. Final ledger-head CI is authoritative for ledger/verify sign-off.
