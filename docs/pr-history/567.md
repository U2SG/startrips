# PR #567 - Expose committed drag-over state for Story media sort

- **Source head:** `10b4a8f7ad7d5643f38896bf59e5d66a6d47c13d`
- **Scope:** Resolve the residual #427 same-chapter keyboard-sort QA race left after ST-103/PR #431. Story media tiles expose dnd-kit's committed `isOver` target with `data-media-tile-id` and `data-drag-over`; the browser QA waits for seed-2's exact target marker before dropping, within the existing 3-second bound. A focused core regression checks that the marker follows the over tile and remains distinct from destination hover.
- **User-visible change:** None; the organizer's sorting interaction is unchanged, while its committed drop target is observable to QA.
- **Review fixes:** Independent Hourly Review cleared this exact three-file CODE Source with no findings. The ST-103 delete-confirmation focus paths remain untouched.
- **Follow-up:** PR #564 needs fresh final-head auth-media evidence after this repair merges and exact-main push CI passes.
- **Validation:** Exact Source CI run 36243271462 passed core, keepsake-render and every browser QA shard, including auth-media. The independent ST-142 Source review receipt is CLEAR. The pre-SEAL ledger/verify failures are expected; final-head CI and exact-main push CI remain required.
