# PR #397 - Bound route optics and Route Point roles

- **Base:** `9dcf0aee318537968157da54c43f64e9cccfed68`
- **Source head:** `d79ed4c4b181abb39a07f7c02995966f3b10d4c7`
- **Scope:** Implements ST-077 / issue #373 as the optical-only Phase B of #352 on top of merged ST-066 geometry. The existing geodesic sampling, canonical anchors, approximation/limb policy and independent hit targets stay authoritative. This slice replaces sparse flag/star Route Point glyphs with bounded CSS-pixel bead/ring optics, keeps route core/halo weight bounded across Particle-only zoom, and adds the pure `routePresentation.ts` projection for ordinary, selected and narrative-current presentation without creating a second semantic owner, camera owner, Playback owner or recorded-track inference.
- **User-visible change:** Particle-only routes no longer thicken into cable-like strokes as the globe zooms. Passthrough Route Points render as quiet beads while Stops retain a ring treatment; selected and narrative-current records receive stronger but bounded attention. Selection remains browsing only: it does not start travel motion, advance Playback-current, move canonical anchors or offset repeated-coordinate records. Future points and legs remain hidden by the existing temporal reveal authority, including reduced-motion and compact/mobile presentations.
- **Authority / consumer contract:** Narrative route/point identity consumes the existing gated `unknownCreateSemanticOwnership.selection` and is suppressed for explicit browse selection, so raw implicit cursor state or an unproven unknown-create candidate cannot acquire narrative optics. Temporal reveal remains above optical attention for ordinary, selected and narrative-current points. Route Point semantic role (`isStop` vs passthrough) is orthogonal to attention role, and the later label slice is expected to consume the small pure presentation-role output rather than introducing another active boolean/controller.
- **Review fixes:** Exact-Source review cycles identified and this Source resolves: multiple overlapping Journeys independently becoming narrative-current; current Route Point reconstruction from route index instead of cursor-owned identity; missing rendered Stop-ring evidence; narrative attention overriding Stop semantic fill; active-route brightness overriding selected/current drop-shadows; selected and ordinary future points having temporal opacity overridden by more-specific active-route rules; explicit browse selection leaking into narrative-current; route-optics QA proving only one half of the composed brightness/drop-shadow filter; and narrative optics bypassing the existing unknown-create semantic ownership gate. The final evidence-only correction in `d79ed4c` makes the focused source-contract test actually slice from `narrativeSemanticSelection`; product code is unchanged from `2b1777a`. Fresh review request `5706710779` completed clear on exact Source in comment `5706728851`; all current review threads are resolved or outdated.
- **Browser / core evidence:** `browser-qa / route-anchoring` covers overview and deep Particle-only optics, DPR 1/2/3, 844x390 compact landscape, Stop vs passthrough paint, selected vs narrative-current authority, same-coordinate canonical anchors, future selected and ordinary temporal hiding, composed active-route brightness plus attention drop-shadow, overlapping Journey ownership and reduced motion. Core tests cover the pure presentation-role projection and source contracts while existing ParticleEarthScene/LivingAtlasApp geometry and ownership suites remain intact.
- **Follow-up:** This PR intentionally does not implement the later label slice, geo/coastline/city changes, route import, GPS-track inference, global particle/shader retheme, a new renderer/camera owner or Backend work. The label slice should consume the bounded presentation-role projection introduced here. No new follow-up issue is required by this Source.
- **Validation:** Local validation remained lightweight: `git diff --check` and `node --check scripts/qa-route-anchoring.mjs` passed during Source repair; no local full build/browser/database/Docker suite was run. Exact Source CI run `35167986354` attempt 1 on `d79ed4c4b181abb39a07f7c02995966f3b10d4c7` passed `core`, `browser-qa / route-anchoring`, `browser-qa / city-label-anchoring`, `browser-qa / route-point-context`, `browser-qa / earth-dive`, `browser-qa / login-media`, `browser-qa / final-acceptance`, playback-prefetch, mobile/share/composer/attention/globe lanes and every other real product/browser lane observed on that run. Its only failures are the intentionally absent current-PR `ledger` and downstream aggregate `verify`. Exact base `main@9dcf0aee318537968157da54c43f64e9cccfed68` has push CI `35111854316` SUCCESS. This commit adds only `docs/pr-history/397.md`; exact final-head CI and final merge-readiness remain authoritative before handoff.
- **Boundary:** No Backend implementation, merge-ready signing, merge, tag, release or deployment authority is granted. Experience Worker stops at `HANDOFF_REVIEW` after exact final-head CI and merge-readiness succeed.