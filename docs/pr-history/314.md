# PR #314 - Add restrained visited imprint

- **Issue relationship:** `Refs #88` — this is the owner-approved V1 ambient accumulated imprint; it does not close broader Atlas visual-system or Route Point context work.
- **Source head:** `e4a64dc14f4efde4b999e4d29f0abb9bd520d4a5`
- **Scope:** Add one deterministic scene-owned visited-imprint policy and a fixed 60×30 cached contribution field derived only from the `journeyRoutes` already supplied to the viewer plus the existing `temporalReveal` authority. Same-Journey contributions are deduplicated per coarse spatial region; Route Points carry the primary trace and sparse spherical corridor samples contribute a weaker approximation. The existing surface-particle shader samples the cached field to apply only restrained brightness/stability gain using the owner-fixed `0.15 * (1 - exp(-n/3))` policy, with active-context suppression and semantic-zoom attenuation. No second particle layer, route/coastline/geographic-coordinate changes, server/API/storage changes, Home inference, or independent animation clock are introduced. The existing globe render-budget QA fixture exposes bounded field/debug counters and captures first-Journey, mid-timeline, now, and route-strokes-hidden visual evidence.
- **User-visible change:** An Atlas with accumulated Journeys leaves a faint persistent memory texture in the existing globe particles. Repeated Journeys reinforce the trace only toward the fixed cap; rewinding removes future contributions and replay restores them; hiding route strokes leaves the ambient history visible. The imprint recedes at close semantic zoom and around active Journey context so selected routes/points, geography, labels, coastline, Home Base, and #312 Quiet Core remain visually stronger. Guest/share scenes derive the field only from routes already authorized for that viewer.
- **Review fixes:** The final Source centralizes the remaining imprint presentation literals (active-context suppression, twinkle-stability weight, and local-progress reduction) into named `visitedImprint` policy constants, keeping the V1 policy in one scene-owned module. The first CODE-head CI saw a single unrelated `final-acceptance-mobile-360` actionability failure with no console/page error and an aborted session request; no product/QA source was changed for it. On final Source the same `final-acceptance` lane passed. Automated review was quota-unavailable on this PR; an exact-Source manual audit covered field derivation/dedupe, temporal reveal, viewer scope, active-context composition, shader sampling, semantic zoom, #312 Quiet Core separation, canonical projection, and bounded render-budget integration and found no source blocker.
- **Follow-up:** None for ST-020 V1. Route Point contextual content remains #291 and Home activation/context remains separate; no heatmap, GPS history, effect chooser, extra travelling highlight, or generalized effects framework is included here.
- **Validation:** Lightweight local validation on final Source: `git diff --check`, `pnpm typecheck`, and `node --check scripts/qa-globe-render-budget.mjs` passed; no local Vitest/browser/build/server was run. Exact CODE-head CI run `34555565865` passed `core` and every product/browser lane including `globe-render-budget`, `final-acceptance`, `login-media`, `post-login`, shares, route, focus, mobile, city-label and Earth Dive; only the intentionally missing pre-ledger `ledger` and aggregate `verify` were red. CI uploaded real `visited-imprint-1` browser evidence containing the same-camera first/mid/now/route-hidden captures. Final ledger-head CI remains authoritative for ledger/verify sign-off.
