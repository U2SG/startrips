# PR #458 - Wait for a renderer-owned first-frame-painted signal in the cover-reveal-opening QA lane

- **Source head:** `06a366bf7f59c64ada29f81532685cfcdafd2c5d`
- **Scope:** Already on main: src/reveal/coverRevealFlow.ts and CoverRevealStage.tsx correctly run the idle/preparing/revealing/settled lifecycle and already track frameCount/progress internally as the vendored RevealFlow accepts real animation frames; scripts/qa-cover-reveal-opening.mjs exercises the real product surface end-to-end. Real gap (corrected 2026-09-20): the desktop first-frame-is-the-derivative/the-reveal-actually-transitions checks infer the presented frame from a pixel probe raced against the animation; each sample costs a full element screenshot plus an evaluate round-trip, so on a slow runner the whole reveal completes before the first sample returns and the assertion grades the settled/original frame instead of a mid-reveal one (observed on PR #453's Source, run 35495870574). Fix must give the assertion a renderer-owned frame-identity signal it can grade independent of that round-trip timing, not a longer wait before sampling — the same class ST-100/#432 and ST-104/#448 fixed by exposing real readiness instead of a DOM-attribute proxy.
- **User-visible change:** Wait for a renderer-owned first-frame-painted signal in the cover-reveal-opening QA lane
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35503577744 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
