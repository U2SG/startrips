# PR #361 - Smooth route geometry between Route Points

- **Source head:** `9d2ead4eba6e7f95d6e0dbee2a00a3ebec034963`
- **Scope:** Completes ST-066 / issue #352 Phase A only. Route geometry keeps exact canonical Route Point anchors, shared length-weighted tangent-plane intent, bounded spherical handles/deviation, deterministic sampling, the existing #242 hard route vertex/lift budget, and byte-identical whole-route/per-leg geometry.
- **User-visible change:** Multi-point route strokes read as one continuous geographic path rather than independent great-circle legs with hard corners. Near-reversals remain loop-free, and long near-antipodal legs no longer form backward hooks or sharp interior corners from undersampling.
- **Review fixes:** Review first exposed asymmetric near-reversal overshoot/backtracking; Source `558ce48` bounded in-leg progress and added the supplied regression fixture. The next review exposed near-antipodal interior curvature missed by endpoint-only allocation. Final Source `9d2ead4` uses a bounded cross-track spherical Hermite span and validates actual interior samples for adjacent angular step and heading-change tolerance while preserving exact anchors, shared tangent intent, the two-degree deviation bound, determinism and the #242 budget. Both review threads are resolved; the single fresh automated review on `9d2ead4` completed with no additional current-head finding.
- **Boundaries:** Phase A geometry only. No marker styling, route-width/current-leg emphasis, label behavior, recorded/GPS tracks, Detailed Earth behavior, renderer ownership, random wobble, or second route representation.
- **Follow-up:** None inside #352 Phase A. This writer stops at HANDOFF_REVIEW; no merge-ready label, merge, deploy or tag is authorized.
- **Validation:** Exact Source CI `34831612628` on `9d2ead4eba6e7f95d6e0dbee2a00a3ebec034963` passed `core`, `browser-qa / route-anchoring`, owner-share, home-base-context, final-acceptance, mobile-contract, earth-dive, city-label-anchoring and all other observed required product/browser lanes. Only the intentionally absent per-PR `ledger` and downstream aggregate `verify` failed before this seal. Merge-readiness controller `34831612718` succeeded. The recovered worktree had no installed local dependencies, so no dependency install or heavy local validation was run; `git diff --check` passed and GitHub CI is authoritative. This commit adds only `docs/pr-history/361.md`; exact final-head GitHub CI must pass ledger/verify/core/route-anchoring and required browser/product lanes before handoff.
