# PR #286 - Keep focus signal on canonical surface with clip-space depth bias

- **Source head:** `7e4870e9fff580f2b91152c8447ae6be02fed4ee`
- **Scope:** Issue #240, ST-034 only. Remove the personal focus signal's radius-based render lift, keep both initial construction and `applyFocusPoint` on `focusSignalAnchor` / the canonical geographic radius, and add an opt-in clip-space z bias to `createParticleEarthMaterial`. Only the personal signal opts in; cluster, cyan cluster, shell and halo materials remain on the unbiased default. The existing city-label QA additionally records the focus-signal radius beside the geographic surface radius.
- **User-visible change:** The focus glow retains its visual priority above the particle surface without occupying a second geographic shell, so its drawn position and the geographic point it claims stay in the same projection space during zoom/rotation.
- **Review fixes:** None yet.
- **Follow-up:** None in this slice; coastline depth policy, Route Point route lift, camera/focus ownership and decorative particle radii remain unchanged.
- **Validation:** Local validation intentionally limited to `git diff --check` and `node --check scripts/qa-city-label-anchoring.mjs`; full typecheck, unit/build and browser QA, including `browser-qa / city-label-anchoring`, remain authoritative in GitHub CI.
