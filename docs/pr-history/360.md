# PR #360 - Make mobile Story and Share mutually exclusive

- **Source head:** `cc04c9beffebd0460297f5e921425f7cae22f21c`
- **Baseline:** Rebased onto exact-green `main@3451a6d2bbfe34c625e7baa20007c9e7e2a4da4b` after PRs #354, #357 and #358 landed; conflict reconciliation preserves merged Home Base context behavior in `LivingAtlasApp.tsx` and both Home Base context plus owner-share artifact handling in `.github/workflows/ci.yml`.
- **Scope:** Implements ST-069 / issue #356 inside the existing mobile Story/Share ownership and history lifecycle. Collapsed mobile Story now has Escape parity with Browser Back while higher-priority child and mutation states still win first. Story, Journey-detail and Atlas share entry use the existing replacement authority so Story -> Share leaves one active top-level owner rather than stacked Story + Share surfaces. `useMobileSurfaceHistory` reconciles stale predecessor tokens across non-history closes, preserves immediate Back ownership while a reopened Share history write is deferred, keeps create/revoke mutations non-dismissible, distinguishes scheduled stale-owner cleanup from an actual in-flight owned `history.go()` traversal, and lets a newly replacing Share overwrite a merely scheduled stale sheet suffix before reconciliation can self-dismiss it. The owner-share browser lane covers the compact 320/360/390/430 surface matrix, explicit deterministic multi-Journey selection, rapid close/reopen, pending-create Browser Back protection, single-Journey entry, focus/history ownership and screenshot/DOM evidence.
- **User-visible change:** On compact/mobile surfaces, opening Share from Story replaces Story instead of stacking underneath or above it. Escape and Browser Back close the same current owner in the same priority order, while pending share creation/revocation cannot be dismissed out from under the mutation. Rapid close/reopen and sheet -> Share transitions no longer leave ghost Story/sheet history entries or immediately dismiss the newly opened Share. Desktop Story, Journey-detail Share, Playback/fullscreen and the existing Share V1 contract remain unchanged.
- **Review fixes:** Exact-head review drove the bounded history/QA sequence without introducing a second modal/history stack. Source first continued predecessor reconciliation after non-history Share close and blocked Back/Escape/close during create/revoke. It then deferred only token serialization during an owned reconciliation while keeping popstate ownership immediate, tied deferred ownership to real traversal completion, and corrected QA so the actual Chinese accessible names are matched. The regression path explicitly selects the deterministic `海风经过深圳湾 2026-08-20` fixture before create, waits for real `popstate` completion instead of fixed sleeps, scopes the live single-Journey trigger, and records entry diagnostics. Runtime follow-ups retain the owned traversal marker through dispatch and distinguish merely scheduled cleanup from an in-flight `history.go()` so replacement Share registration cannot self-dismiss. Final QA clicks the visible associated custom-checkbox label while still locating/asserting the exact checkbox by accessible name; no force-click, generic selector, retry, timeout inflation or runtime auto-selection was added. Fresh exact-Source Codex review on `cc04c9beffebd0460297f5e921425f7cae22f21c` reported no major issues, and all review threads are resolved.
- **Follow-up:** None inside #356. This writer stops at HANDOFF_REVIEW: no merge-ready label, merge, deploy or tag is authorized. After maintainer merge, the resulting exact main must be green before ST-069 is considered terminal/passed.
- **Validation:** Lightweight local checks on the final Source included `git diff --check` and `node --check scripts/qa-owner-share.mjs`; the focused mobile-history unit suite and typecheck were run on the bounded runtime Sources during repair. Exact Source CI run `34819130472` on `cc04c9beffebd0460297f5e921425f7cae22f21c` passed `core`, `keepsake-render`, `browser-qa / owner-share`, `browser-qa / mobile-contract`, `browser-qa / final-acceptance`, `browser-qa / city-label-anchoring`, `browser-qa / login-media`, `browser-qa / earth-dive`, and all other observed product/browser lanes; only the intentionally absent per-PR `ledger` and downstream aggregate `verify` failed pre-seal. Exact merge-readiness run `34819130471` succeeded. Fresh exact-head Codex review completed with no major issues. This commit adds only `docs/pr-history/360.md`; exact final-head GitHub CI is authoritative before handoff.
