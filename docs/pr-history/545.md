# PR #545 - Keep reviewed itinerary place candidates available

- **Source head:** `5f82275c9a92b245ac6001d0c8fadd097219b330`
- **Scope:** Accept a whole-plan selected candidate when its distinctive established name, country and state agree with provider data and no distant same-name candidate competes. Retain fetched search results and corrected queries for manual review.
- **User-visible change:** More rural and neighboring-town places can be added in one batch; unresolved places open with their already found candidates instead of requiring a repeated search.
- **Review fixes:** Merge corrected-query results with the initially fetched candidates so a failed correction cannot hide choices the member already had.
- **Follow-up:** Some hotels and one rural POI remain ambiguous or absent from the current public map index; they still require a member's choice. Import work continues while the dialog is closed but does not survive a tab refresh.
- **Validation:** GitHub Source CI run 36026209109 at this Source head passed core, keepsake-render and every browser QA shard after one targeted `auth-media` rerun. Ledger and verify remain red only because this required final file has not been committed; final ledger-only CI is pending. A read-only production probe on the pre-fix main found 59 entries, 55 eligible lookups, 51 with initial candidates, and 30 automatically confirmed after whole-plan review. No local tests or build were run.
