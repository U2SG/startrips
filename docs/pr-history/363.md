# PR #363 - Reassert Journey Rail visibility after playback return

- **Source head:** `8681419ff465cef6ecd4a7c5fb71639a18677648`
- **Scope:** Implements the reopened ST-063 / issue #325 lifecycle repair on current main `e6195fdf79c539718d66d2e69fc99fda68d88a2a`. The desktop Journey Rail keeps the existing declarative Playback/globe-pick visibility owner and now re-synchronizes the actual rail DOM visibility in a layout commit across Playback/Story return and compact-layout boundaries, so stale imperative `visibility:hidden` state cannot survive after ownership releases. The ST-062 diagnostic marker and actionability logic are unchanged.
- **User-visible change:** After the compact-mobile to desktop handoff and a Full Playback -> Story -> Atlas return, the Journey Rail is synchronously restored to visible/clickable instead of intermittently remaining hidden over the live globe.
- **Review fixes:** Fresh exact-Source Codex review on `8681419ff465cef6ecd4a7c5fb71639a18677648` completed with no major issues (`5667797452`). Exact Source CI `34872975285` has `core` and all required observed product/browser lanes green; `browser-qa / final-acceptance` recorded `railVisibility:"visible"` at 320, 360, 390 and 430. No retry, wait, polling, timeout inflation, force-click or relaxed actionability was introduced; the QA harness diff is empty.
- **Follow-up:** ST-066 / PR #361 remains frozen cross-lane-blocked without ledger until this ST-063 repair is maintainer-merged and the resulting exact-main push CI is green; only then may ST-066 rebase/revalidate/review and reseal. ST-070 remains pending. No unrelated route geometry, Earth Dive, Backend or release/deploy work is folded into this PR.
- **Validation:** Local lightweight validation: `git diff --check`; targeted Vitest was attempted but this worktree intentionally has no local `node_modules`, so GitHub CI is authoritative. Exact Source CI run `34872975285` on frozen Source `8681419ff465cef6ecd4a7c5fb71639a18677648` was executed twice: attempt 1 and attempt 2 both passed `core` and every required observed product/browser lane, with only the intentionally absent-ledger `ledger` and downstream `verify` red pre-seal. `browser-qa / final-acceptance` succeeded in both executions and the preserved `journey-rail-state` diagnostic logged `railVisibility:"visible"` at 320, 360, 390 and 430 in both. This commit is ledger-only; exact final-head CI must pass `core`, `ledger`, `verify`, final-acceptance and the required product/browser lanes before HANDOFF_REVIEW.
