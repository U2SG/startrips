# PR #421 - [Backend] Persist Journey recorded-track evidence as bounded owner-only segments

- **Source head:** `649d320e145b82a7af1f042c64f50ca05e77d498`
- **Scope:** Server-only slice of #341 that deliberately excludes the gated first input format. Already on main: Journey and Route Point persistence in server/db/app-schema.ts, atlas authority derived from session.session.activeOrganizationId through server/authorization/atlas-access.ts, revision-guarded atomic route writes and soft deletion in server/repositories/journey-repository.ts, and ST-088's media-level evidence separation. The real gap: there is no Journey-scoped store for ordered recorded-track segments and samples at all (no track table, no migration beyond 0024, no symbol matching recorded-track/trackSegment anywhere in server/), so there is no idempotent operation identity, no delete/isolation semantics and no owner-only exposure contract for recording evidence kept structurally apart from Route Points. Out of scope here and left to #341: any file parser, the GPX choice, an import or upload endpoint, background position capture, map or Playback rendering, interpolation or map-matching, and any sharing authorization for precise samples.
- **User-visible change:** [Backend] Persist Journey recorded-track evidence as bounded owner-only segments
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35424244547 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
