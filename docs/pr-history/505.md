# PR #505 - Defer stale intake amendments

- **Source head:** `55bd13f87ef120b8b461f4ccabe6a04ba5df6ad2`
- **Scope:** Related to #399. Classify proven stale model snapshots separately, defer only the affected amendment through existing invocation-local FEATURE_SKIP, guard unchanged/moot snapshot writes and isolate invocation result files.
- **User-visible change:** Concurrent queue updates no longer stall unrelated Backend work through platform backoff. Ownership, gates, attempts and stale-write rejection remain intact.
- **Review fixes:** Full exact-Source diff inspected in this session under explicit user seal/merge authorization, including all four changed paths, caller/selector boundaries and 13 added tests. No blocking finding. External automated review was quota-unavailable and was not treated as approval.
- **Follow-up:** Existing #399 process-provider failures and installed-only inline-Python defect remain separate. Generic commit-time storage conflicts remain fail-closed. Overlapping PR #506 is not included or implicitly approved. Runtime activation requires a safe observed boundary and expected-input hashes.
- **Validation:** Source CI 35713288701 passed core, keepsake-render and all eight browser-QA lanes; ledger/verify were the expected pre-final failures. Control-plane PR CI 35713288797 passed Windows and Ubuntu including the 13 added regressions. Local checks were syntax/AST and git diff only; no local tests/build/browser. Final-head CI and exact-main push CI remain authoritative.
