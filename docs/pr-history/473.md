# PR #473 - Add a restart-safe reference worker client for the shipped cover-reveal-worker protocol

- **Source head:** `4e464e6f72951a6eb227ab4f841f26f2e2e55096`
- **Scope:** Server protocol (ST-074/#368), display read (ST-086/#386) and RevealFlow renderer/opening (ST-073/#367, ST-083/#379) are merged on main; verified no reference client exists anywhere in the repo (no scripts/tools file references cover-reveal-worker or ink-wash-poster). The real gap is exactly the missing local CLI/reference worker client that drives claim->source-read->download->generate->output-upload->complete against the existing `/api/cover-reveal-worker/*` routes using `COVER_REVEAL_WORKER_TOKEN`, with fail-closed lease handling and a deterministic fixture-generator contract test, as scoped by issue #468 (Slice 2 of #367).
- **User-visible change:** Add a restart-safe reference worker client for the shipped cover-reveal-worker protocol
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35586262963 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
