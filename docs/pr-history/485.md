# PR #485 - Enable Apple sign-in with relay-safe identity and credential lifecycle handling

- **Source head:** `0721c2cc0db37996e22bf239800377b1eed8244f`
- **Scope:** On origin/main (297e524), server/auth.ts has no socialProviders and ST-067/#345 already shipped the provider-agnostic identity layer (server/account-identities/{identity-policy,provider-proof,reverification-rate-limit,account-identity-repository}.ts, server/routes/account-identities.ts with generic usableProviderIds/linkableProviderIds, and PR #481's availableLinkProviders read) that #349/ST-132 (still unmerged PR #484) is wiring Google into without needing #348's fuller link-management UI, which is itself unimplemented and not queued (recorded skip on #348). The same generic router is not a real code dependency for Apple either. The gap: configure socialProviders.apple (Better Auth 1.6.23) against pinned Service/App id, Team/Key id and signing key read only from server config, verify audience/issuer/nonce/state via the library's own token verification, route new-signup/existing-login/explicit-bind through the existing link-intent/proof/complete pipeline so a stable Apple subject never gets treated as email-matchable identity (Hide My Email relay addresses and missing/unverified email must not auto-link), keep client/private key material server-only with a documented rotation/revocation procedure that cannot strand a bound account, and cover cancel/expired-secret/relay/duplicate-callback with CI fake-provider tests while recording real Apple Developer authorization as an external gate outside CI.
- **User-visible change:** Enable Apple sign-in with relay-safe identity and credential lifecycle handling
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35726596328 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
