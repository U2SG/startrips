# PR #276 - Keep decoded story media stable during navigation

- **Source head:** `f7e95b757cd96aa6fb039e121c440cd06a078f93`
- **Scope:** Story signed-read reuse, uninterrupted expiry scheduling, keyboard focus across recycled media slots, and eight-photo browser regression.
- **User-visible change:** Warm photos retain their decoded resource while navigating. Repeated direction keys continue after the first handoff; mouse navigation leaves focus on the presented media.
- **Review fixes:** Remote review of the initial source reported no findings. CI exposed duplicate initial reads during StrictMode effect replay and implicit 0.01ms CSS transitions under reduced motion; requests now remain coalesced within their scope, stale completions are ignored, and media transitions are explicitly disabled for reduced motion. Continuous keyboard checks use one initial focus instead of refocusing before every key.
- **Follow-up:** Deploy the reviewed fix, then repeat the same real many-photo story interaction.
- **Evidence:** Authorized browser testing of a deployed many-photo story recorded repeated URL changes for the same image and readiness loss during page motion. Credentials, signed URLs and private media are excluded from this PR.
- **Validation:** Syntax and diff statically checked. Tests, typecheck, build and browser regression run only in GitHub CI. The eight-photo scenario varies each signed-read response and checks forward/backward traversal, resource reuse, physical slots and continuous keyboard focus. Deployed verification of the fix awaits an updated server build.
