# PR #434 - [Backend] Carry an optional recorded media-evidence document through upload start, finalization and recovery

- **Source head:** `dcf0b98f42b7ccbc2e2f5dc73aea68f9d69bed93`
- **Scope:** Already shipped on main (ST-088/#388, PR 391): the `media_asset_evidence` table in `server/db/app-schema.ts`, the normalizer `parseRecordedEvidenceWrite` in `server/media/media-evidence.ts`, `server/repositories/media-evidence-repository.ts` and the owner-only `/api/media-evidence` route. The real gap: `server/routes/uploads.ts` contains no evidence handling at all (grep for `evidence` on `origin/main:server/routes/uploads.ts` returns nothing) and the `media_uploads` table has no column able to retain a normalized evidence document, so an accepted upload cannot carry asset-owned spatial/time evidence through `finalizeUpload`, deduplication, the `completion_unknown` lost-response path or `reconcileStaleUploads`. This slice wires the existing evidence contract into the existing upload lifecycle only: no second evidence model or parser, no EXIF/HEIC/video parser (that stays with #333), no Route Point, placement, cover, sharing or client change. Evidence stays evidence — `contentHashVerified` proves bytes only and never upgrades spatial precision or provenance, and authority stays session-derived through `server/authorization/atlas-access.ts`. Verified at intake that no open PR (416, 422, 426) touches `server/routes/uploads.ts`, `server/media/media-evidence.ts`, `server/repositories/media-evidence-repository.ts` or `server/db/app-schema.ts`.
- **User-visible change:** [Backend] Carry an optional recorded media-evidence document through upload start, finalization and recovery
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35453513563 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
