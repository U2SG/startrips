# PR #475 - [Backend] Gate and, once confirmed, import bounded GPX recorded-track evidence into the existing Journey recorded-track store

- **Source head:** `c8bff0b5abb329719e8c9e26dafb919eeaa9e740`
- **Scope:** Already on main (ST-094/#419, ST-095/#423): the Journey-scoped recorded-track schema, writeRecordedTrackForAtlas normalization/bounds/idempotency, owner-only GET readback and DELETE withdrawal in server/routes/journey-recorded-tracks.ts. The real gap: writeRecordedTrackForAtlas has no HTTP caller anywhere (git grep on origin/main finds only its definition and tests), so no import surface exists. Owner confirmed on the issue (2026-09-21) GPX <trk>/<trkseg>/<trkpt> as the approved first recorded-track input format, and confirmed this one-shot, user-initiated import channel authorizes no continuous/background location capture (scoped to this channel only, not a product-wide prohibition on a future live-recording feature). Both confirmations carry conditions: the route path and the stored model stay format-neutral (no literal 'gpx' in either), byte/point/segment ceilings are declared per-format in a registration structure rather than as constants inlined in the GPX parser, idempotency keys on Journey plus uploaded bytes independent of format, and a wpt/rte-only document must return a distinct 'unsupported-format' error rather than a malformed-file error. This feature adds exactly the GPX parser and a format-neutral import route on top of the existing write path.
- **User-visible change:** [Backend] Gate and, once confirmed, import bounded GPX recorded-track evidence into the existing Journey recorded-track store
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35611808543 attempt 2 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
