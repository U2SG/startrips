# PR #535 - Centralize Story media stage handoff

- **Source head:** `69ed95a1c4b62e8a1fdbe17890d88b7b7d23dbec`
- **Scope:** `StoryMediaPages` owns drag, release, cancel, settle, presentation commit, and transform/clip/z-index cleanup. `JourneyStory` retains semantic selection, fullscreen intent, and the independent Playback clock. The branch includes main's Detailed Earth and current Route Point return changes.
- **User-visible change:** Photos keep their selected picture through rapid reversal, resize, and Back. The presented video owns its native play/seek surface; separate 44px previous/next controls keep fast touch navigation available without stealing video input.
- **Review fixes:** Removed the fixed native-control strip guess. A cancelled horizontal drag cannot turn its release into a photo click. A video step touch activates once on release even when the browser omits `click`, and its optional compatibility click is consumed once. Browser QA checks real image/touch input, native seek events, slow reads, dual videos, compositor pixels, hit targets, rotation, and fullscreen return.
- **Follow-up:** Video fullscreen still needs a separate transport-time transfer and single visible frame through its shared-element morph; this Source does not claim every #489 black-frame path.
- **Validation:** Source CI [35961596456](https://github.com/U2SG/startrips/actions/runs/35961596456) and independent review are authoritative for this code head. This final commit changes only this ledger; exact final-head CI remains required.
