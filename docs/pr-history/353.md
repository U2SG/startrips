# PR #353 - Record the Continuous Journey release candidate

- **Source head:** `7104b6ea65b3ce0a8141231d53c4275634f4a63a`
- **Scope:** Implements ST-040 / issue #249 as documentation-only release truth. Adds `docs/releases/continuous-journey.md` for exact-green candidate `main@39b858634dd71731a10861647581bbce5efbc281` and updates both READMEs so already-shipped guest share viewing, Journey Playback, mobile Viewer mode and Home Base capability are represented as current product truth. The candidate matrix uses only `未实现 / 实现已合待验收 / 已验收 / 明确延期`, ties each claimed promise to merged issue/PR evidence and candidate SHA, and records automated evidence plus known limits. It reconciles Share V1, #245/#246/#247/#248, Home Base foundations, Everyday Fragment, the private Keepsake renderer prototype, regional coastline completion and deferred optional AI without changing runtime code, schema, policy or deployment state.
- **User-visible change:** Repository-facing capability/release documentation now describes what the exact candidate actually contains rather than the older P0 snapshot. The report is explicitly `候选验收` only: it does not claim a tag, deployment or production release, and Natural Earth 10m is described as the 1:10,000,000 source scale rather than 10-meter geographic accuracy.
- **Review fixes:** Exact Source review of the first documentation head found one bounded P2: issue #235 Home Prelude/Epilogue had been incorrectly described as deferred even though PR #301 is merged into the candidate. Final Source `7104b6ea65b3ce0a8141231d53c4275634f4a63a` corrects that row to `已验收`, cites PR #301, preserves its owner/private, historical-date, 25 km narrative-skip, guest-isolation and Quick Recap budget limits, and narrows both README Home bullets so only richer on-demand Home context interaction remains future work. Fresh exact-head release-expression/CFAA review on `7104b6e` reported no major issues and no concrete blocker.
- **Follow-up:** Owner sign-off remains the final product gate: the owner must say Go/No-go on exact candidate `39b858634dd71731a10861647581bbce5efbc281` and decide whether this record stays a `候选验收` report or proceeds toward a tag/release/deployment. This PR does not make that decision and does not tag, deploy or claim production truth. Next-stage issues #331-#351 remain reference-only planning rather than automatic gates for this candidate.
- **Validation:** Candidate baseline push CI `34756019229` is SUCCESS on exact `39b858634dd71731a10861647581bbce5efbc281`. Exact final CODE Source CI run `34757701539` on `7104b6ea65b3ce0a8141231d53c4275634f4a63a` passed `core`, `keepsake-render` and every product/browser lane, including guest-share, owner-share, mobile-contract, playback-prefetch, Home Base suggestion, final-acceptance, earth-dive, city-label-anchoring, globe-render-budget and login-media; only the intentionally pre-seal `ledger` and downstream aggregate `verify` checks failed because this PR ledger did not yet exist. Lightweight local validation passed `git diff --check`, `node scripts/pr-history.mjs validate-all`, fixed-vocabulary/candidate-SHA checks and the newly-added-product-noun guard. This commit is ledger-only; exact final-head GitHub CI remains authoritative before handoff.
