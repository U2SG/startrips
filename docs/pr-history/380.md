# PR #380 - Make PR communication exception-only

- **Source head:** `94626c6c06e2f5cc46cafef731b955b656a76465`
- **Scope:** Establishes the Startrips low-communication, high-evidence pull-request policy in `CONTRIBUTING.md` and `docs/agent-pr-communication.md`. GitHub Conversation is an exception/decision channel rather than a progress log; routine evidence remains in commits, CI/checks, review state, the external ONE control plane, and this per-PR ledger. The policy documents the external ONE location at `D:/startrips/loop-workspace/feature_list.json`, selector at `D:/startrips/loop-workspace/run-loop.sh`, adjacent process rules, and the prohibition on inventing a repository copy or second backlog.
- **User-visible change:** No product/runtime behavior changes. Contributor and agent workflow now defaults to one concise fresh review request per exact CODE Source, one compact final `HANDOFF_REVIEW`, no routine CI/rebase/rerun/mergeability/fix narration, and zero-comment maintainer behavior unless a human decision or exceptional safety/security/data-loss/migration blocker needs explicit visibility.
- **Review fixes:** The initial invalid ledger was removed after Codex P1 `4016068911` identified missing required ledger fields. Codex P2 `4016340669` then identified that ONE was named without a discoverable external location; the docs now define the authoritative external control-plane paths and tell contributors without workspace access not to fabricate ONE state. Both findings are outdated/resolved. Fresh Codex review completed on exact Source `94626c6c06e2f5cc46cafef731b955b656a76465` with no current non-outdated review thread remaining.
- **Follow-up:** This PR is docs/process-only and does not change runtime, tests, CI behavior, deployment, product behavior, or Backend implementation. Final maintainer flow remains exact final-head CI/verify, `merge-ready` as the last mutation, fresh post-sign merge-readiness success, squash merge with exact expected head, then exact-main push CI success. No deployment/tag/release is authorized.
- **Validation:** Exact Source CI run `34978397236` on `94626c6c06e2f5cc46cafef731b955b656a76465` completed with `core`, `keepsake-render`, and all observed browser QA lanes SUCCESS, including `home-base-suggestion`; the only Source-run failures were the intentionally absent per-PR `ledger` and downstream `verify`. This commit adds only `docs/pr-history/380.md`; exact final-head ledger/verify and all required CI checks are authoritative before sign/merge.
