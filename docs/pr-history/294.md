# PR #294 - Close Chinese city localization coverage gaps

- **Source head:** `c8e1da1a14ffbc7b45e130e3a5aadd13b1e206ab`
- **Scope:** Reopened issue #16, ST-014 only. Strengthen the offline city-label build pipeline for CN/HK/MO/TW by preserving Chinese-region country provenance, accepting truthful Chinese/Cantonese/Han GeoNames candidates only inside that region, using an already-Han GeoNames primary name when available, and applying a checked-in Wikidata CC0 fallback by stable GeoNames id for remaining important coverage gaps. Add rank-aware production-data coverage guards and reuse the existing city-label browser lane for rendered zh-CN evidence; no runtime translation/geocoder, hand-maintained English-to-Chinese dictionary, new CI lane, package-script owner, projection change, or Backend work is introduced.
- **User-visible change:** Deeper regional/local zoom around China, Hong Kong, Macau and Taiwan keeps important city/district labels Chinese instead of progressively falling back to romanized English. The generated production asset now localizes required Pearl River Delta anchors such as Shenzhen, Guangzhou, Hong Kong, Bao'an, Luohu District, Tseung Kwan O and Fanling while keeping the payload bounded.
- **Review fixes:** No current-head review thread was raised. Exact-head CI on the first implementation exposed one evidence defect: the regional zoom probe required the exact Shenzhen label even though the existing collision/declutter budget may legitimately render a neighboring PRD anchor instead. Final Source `c8e1da1` grades the actual rendered regional label near the Shenzhen focus for Han text, while local zoom still requires exact `深圳`; the separate Hong Kong focus continues to require exact `香港` at both regional and local zoom. This keeps the QA on rendered text without weakening the existing label-selection/collision contract.
- **Follow-up:** Remaining unlocalized low-rank localities may be expanded through the same licensed offline-source process if product coverage later requires it; do not introduce runtime translation or hide major anchors to manufacture coverage.
- **Validation:** Lightweight local checks: `git diff --check` and `node --check scripts/qa-city-label-anchoring.mjs`. Exact final CODE-source CI run 34330323785 on `c8e1da1a14ffbc7b45e130e3a5aadd13b1e206ab` has `core` and every browser lane green, including `browser-qa / city-label-anchoring`, login-media, post-login, playback-prefetch, final-acceptance, route-anchoring, earth-dive, mobile-contract, globe-focus-chrome, guest-share and owner-share. The run's only expected failures are the missing current-PR ledger and aggregate `verify`; this next/final commit is ledger-only and must receive its own exact-head CI before evaluation. Production `public/earth/cities.json` is 2,377,300 bytes in this Source and the core coverage guard enforces rank 0/1/2 completeness plus at least 65% PRD rank-3 localization.
