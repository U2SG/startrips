# PR #294 - Close Chinese city localization coverage gaps

- **Source head:** `aca51b1d10b769b4d8e84b6eeac35f29b0022edc`
- **Scope:** Reopened issue #16, ST-014 only. Strengthen the offline city-label build pipeline for CN/HK/MO/TW by preserving Chinese-region country provenance, accepting truthful Chinese/Cantonese/Han GeoNames candidates only inside that region, using an already-Han GeoNames primary name when available, and applying a checked-in Wikidata CC0 fallback by stable GeoNames id for remaining important coverage gaps, then normalizing every accepted Han fallback to the single zh-CN Simplified script with checked-in Unicode 17.0 Unihan `kSimplifiedVariant` single-target data and its Unicode-3.0 license. Add rank-aware production-data/script-truth guards and reuse the existing city-label browser lane for rendered zh-CN evidence; no runtime translation/geocoder, hand-maintained English-to-Chinese dictionary, new CI lane, package-script owner, projection change, or Backend work is introduced.
- **User-visible change:** Deeper regional/local zoom around China, Hong Kong, Macau and Taiwan keeps important city/district labels Chinese instead of progressively falling back to romanized English. The generated production asset now localizes required Pearl River Delta anchors such as Shenzhen, Guangzhou, Hong Kong, Bao'an, Luohu District, Tseung Kwan O and Fanling while keeping the payload bounded; Traditional-only fallback forms are normalized before display, including `Tseung Kwan O -> 将军澳新市镇`.
- **Review fixes:** Initial exact-head evidence fixed the regional-probe assumption so regional Shenzhen framing accepts the actual nearby rendered Han PRD anchor while local zoom still requires exact `深圳`, and Hong Kong remains exact `香港` at both tiers. A later owner CFAA blocker found that the single zh-CN `z` field could accept bare `zh`, `yue` or untagged Han Traditional forms unchanged. Final Source `aca51b1` routes primary Han, tagged/generic Chinese, Cantonese, untagged Han and checked-in authoritative fallbacks through the same build-time Unihan Simplified normalizer, corrects bare `zh` so it is not treated as explicitly Simplified, asserts `将军澳新市镇`, and rejects any production Han `z` value that would still change under that deterministic map.

- **Follow-up:** Remaining unlocalized low-rank localities may be expanded through the same licensed offline-source process if product coverage later requires it; do not introduce runtime translation or hide major anchors to manufacture coverage.
- **Validation:** Lightweight local checks: `git diff --check`, `node --check scripts/build-city-data-lib.mjs`, `node --check scripts/build-city-data.mjs`, and the direct production coverage/script guard. Exact final CODE-source CI run 34333523384 on `aca51b1d10b769b4d8e84b6eeac35f29b0022edc` has `core` and every browser lane green, including `browser-qa / city-label-anchoring` and login-media; its only expected failures are the stale current-PR ledger and aggregate `verify` before this seal. Production `public/earth/cities.json` is 2,377,334 bytes; rank 0/1/2 localization remains complete and PRD rank-3 remains above the 65% floor, while the added script-truth guard reports zero non-normalized Han `z` values. This next/final commit is ledger-only and must receive its own exact-head CI before evaluation.
