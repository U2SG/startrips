# PR #513 - Rename the colliding src/brand recovery-surface module and add a same-casing filename CI guard

- **Source head:** `8331ed538841a62ae144ad2b52d466e22802154b`
- **Scope:** Verified real gap on current origin/main (f88cf22): git still tracks both src/brand/StartripsRecoverySurface.tsx (component) and src/brand/startripsRecoverySurface.ts (data, introduced by cc9805a/#369/ST-070) differing only in first-letter case, imported from StartripsNotFound.tsx, StartripsRecoverySurface.tsx, LivingAtlasApp.tsx and main.tsx. tsconfig.json already has forceConsistentCasingInFileNames:true, so this is not a missing-flag fix as the issue's 'consider enabling' suggests; the actual gap is the collision itself plus no CI check for it (Linux core lane is case-sensitive and stays green while Windows/default-macOS pnpm typecheck fails TS1149). Fix: rename the .ts data module to a case-distinct path, update its four importers/tests, and add a lightweight CI check rejecting tracked path pairs that differ only in case.
- **User-visible change:** Rename the colliding src/brand recovery-surface module and add a same-casing filename CI guard
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35735341631 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
