# PR #303 - Preserve EXIF parsing across JPEG TEM markers

- **Source head:** `c70be51e8a1b29d8ac23d1336c1a09459aef65e6`
- **Scope:** Issue #114 / ST-016 only. Correct JPEG marker scanning in `parseJpegExifPlacementSignal()` so standalone TEM (`0xFF01`) is treated as a no-length marker, preserving traversal to later APP1 Exif metadata. No JourneyStory lifecycle, placement scoring/grouping, CI/package or server code changes.
- **User-visible change:** Standards-compliant JPEG uploads that place TEM before APP1 Exif retain the same normalized GPS/capture-time placement signal as the equivalent JPEG without TEM, so they no longer fall through to the no-suggestion/manual path solely because of that marker.
- **Review fixes:** No source review finding was raised on the final Source; Codex PR-open review completed on `c70be51` with no findings. The regression constructs `SOI -> TEM -> APP1 Exif -> EOI` and asserts output equality with the baseline EXIF fixture.
- **Follow-up:** Raw EXIF retention remains unchanged; placement scoring/grouping and async placement-analysis ownership are explicitly out of scope and untouched.
- **Validation:** Base is exact-green `main@db29270849968b04f54f3058552638d4909909df`; lightweight local `git diff --check` passed. Exact CODE-source CI run 34375444355 on `c70be51e8a1b29d8ac23d1336c1a09459aef65e6` has `core` and every browser lane green. The only expected CODE-head failures are `ledger` and aggregate `verify` because this ledger did not yet exist.
