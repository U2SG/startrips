# PR #540 - Allow replacing a Route Point in place

- **Source head:** `9693045a8e3ba78ce3b35b41067ec13e897a341a`
- **Scope:** Add an inline location editor to an existing Route Point row. Search bilingual provider results or enter coordinates, then update the point without changing its identity, order, stop status, notes, or media association.
- **User-visible change:** Members can replace a middle Route Point directly instead of adding a new point, moving it repeatedly, and deleting the old one.
- **Review fixes:** Retain map-data attribution with replacement results and invalidate an outstanding reverse-geocode lookup when its point is replaced.
- **Follow-up:** None.
- **Validation:** GitHub Source CI run 36015503314 passed core, keepsake-render, and every browser QA shard at `9693045a8e3ba78ce3b35b41067ec13e897a341a`, which merges the latest `main` after PR #537. Ledger and verify failed only because this file still named the earlier Source head; the ledger-only final run must validate this exact Source head.
