# PR #564 - Keep Story media intent, gestures and readiness truthful

- **Source head:** `3b099800f58469f470cf968b6b513152be9358ae`
- **Scope:** An interruption from outside a gesture can commit a settle that already landed. Native selection and drag are refused on the inline Story stage, the immersive overlay and the time scrubber. A swipe may start on the presented video's picture above its native control band, and small movements on the video stay with the native transport. A non-covering status notice appears while a video's first frame waits for its live transport. `storyWarmWindow` gives Story browsing bounded, tiered warm reads and decodes anchored on the requested intent, with warm representative video frames and hidden stale physical pages. The story-media lane gains browser QA for all of this.
- **User-visible change:** A swipe that lands keeps its navigation through rotation, app switching, Back, fullscreen or editing. Swipes are not lost to a text drag. A video page can be swiped left, right or down out of fullscreen on a phone, and a click on it still never navigates. A video that cannot play yet says it is preparing instead of looking like a photo. Fast forward browsing starts from warm reads and never flashes a previous photograph.
- **Review fixes:** The painted stack neighbours now count inside the 7-read and 4-decode tier caps, and only the active stage prepares warm frames. A decode that settles after release or reset no longer re-registers an evicted asset. The preparing notice appears without delay and without animation under reduced motion. The native control band is one `--story-video-control-band` token. The unused 1+1 helper and a prohibited term are gone. During a two-step burst, the abandoned in-flight target now keeps its slot, its z-index and its pose until the newer target can be shown, and the burst QA asserts the foreground moves strictly 1 to 2 to 3. QA interrupts from the settling frame, grades every video element on the page and the phone swipe-down exit from a video, and expects Back during a landed settle to keep the new picture.
- **Follow-up:** PR-B covers desktop Story composition (#554) and the cover-opening state (#555). After merge, re-check the shared renewal and held-frame state with #552.
- **Validation:** Local checks: client typecheck, `git diff --check` and `node --check`. Source CI [36176969695](https://github.com/U2SG/startrips/actions/runs/36176969695) passed core, keepsake-render and all ten browser QA lanes. Ledger and verify failed only because this ledger was not updated yet. The final ledger-only head's CI is authoritative.
