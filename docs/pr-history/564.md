# PR #564 - Keep Story media intent, gestures and readiness truthful

- **Source head:** `7ef20f543c763df395151830cac84e2c637ce44d`
- **Scope:** An interruption from outside a gesture can commit a settle that already landed. Native selection and drag are refused on the inline Story stage, the immersive overlay and the time scrubber. A swipe may start on the presented video's picture above its native control band. A non-covering status notice shows while a video's first frame waits for its live transport. `storyWarmWindow` gives Story browsing bounded, tiered warm reads and decodes anchored on the requested intent, with warm representative video frames and hidden stale physical pages. The story-media lane gains browser QA for all of this.
- **User-visible change:** A swipe that lands keeps its navigation through rotation, app switching, fullscreen or editing. Swipes are not lost to a text drag. A video page can be swiped and still never navigates on a click. A video that is not yet playable says it is preparing instead of looking like a photo. Fast forward browsing starts from warm reads and never flashes a previous photograph.
- **Review fixes:** None yet.
- **Follow-up:** PR-B covers desktop Story composition (#554) and the cover-opening state (#555). A two-step burst can briefly return the committed picture to the front while the newest target decodes into a reused page. The lane reports that without failing. The native control band is a documented 84px because Chromium's control boxes are not readable from page script.
- **Validation:** Locally, client typecheck, `git diff --check` and `node --check` passed. `browser-qa / story-media`, `core` and the remaining CI lanes on this PR are authoritative.
