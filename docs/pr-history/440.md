# PR #440 - Preserve accumulated QA check evidence when qa-route-point-context.mjs throws mid-run

- **Source head:** `c8e3ee22055896d6b21852e624156760d9b520af`
- **Scope:** On main, scripts/qa-route-point-context.mjs (lines 268-1089) keeps `checks`/`failed` module-level and runs the whole scenario in a bare `try { ... } finally { await browser.close(); }` (367-1086) with no catch; `console.log(JSON.stringify({ checks }, null, 2))` sits after that block (1088) and only runs on the non-throwing path, so any thrown exception (a locator waitFor/click actionability timeout, as hit in run 35440173849 / job 105889501603) discards every already-recorded check and leaves only the raw Playwright stack in CI. The gap is exactly as reported: this is a harness-evidence defect in one script, not a product defect, and no PR on main addresses it. The fix is to make the checks payload print on the throw path too (e.g. wrap the try body so the existing console.log also runs in a catch before rethrow/non-zero exit), without touching any assertion, timeout or selector.
- **User-visible change:** Preserve accumulated QA check evidence when qa-route-point-context.mjs throws mid-run
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35456698746 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
