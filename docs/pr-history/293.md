# PR #293 - Reveal Route Point context before Story

- **Source head:** `2ea2a57ac04f56e86c28aaf2ad70aba8c904ebfb`
- **Scope:** Issue #291, ST-053 only. Add a pure Route Point context projection and newest-intent resolver, then change Atlas Route Point activation to reveal one restrained context surface before Story. The projection derives label, Journey relationship, date/note presence, scoped visual-media count, at most one representative asset, and explicit route-point location precision only from the Journey already in scope. The single entry opens the existing Story with the same Journey + Route Point identity; no new camera/focus owner, Story viewer, POI/geocoder feed, per-asset GPS inference, or persistent map-mode toolbar is introduced.
- **User-visible change:** Selecting a Route Point in Atlas focus flow first shows one quiet context panel answering what was left there, including truthful date/note/media/location-level metadata. Text-only Route Points remain meaningful without fake media loading, and entering Story preserves the exact selected Journey + Route Point. The context is planet-only and remains bounded/scrollable for long notes.
- **Review fixes:** Automated review found three P2 issues. Final Source `2ea2a57` clears/hides retained context when leaving the planet view so it cannot float over Timeline; rebuilds a retained context from the latest Journey object so Story edits/media changes cannot reveal a stale snapshot on return; and caps the context panel to viewport-relative height with internal scrolling so the entry action remains reachable for long allowed notes. The same Source also replaces the dedicated browser lane's CI-fragile real ParticleEarthScene startup dependency with a minimal `qaRoutePointContext=1` activation fixture under the existing `living-atlas + globe-chrome + qaLite=1` QA surface: it still invokes the production `onJourneyRoutePointActivate` integration and grades focusRevision/identity/context behavior, while existing scene lanes retain raycast and globe-startup ownership.
- **Follow-up:** None for this slice. Scene raycast/startup, Semantic Earth Dive, Story media compositor, Playback return, and broader Atlas attention hierarchy remain owned by their existing merged contracts/issues.
- **Validation:** Local validation was intentionally lightweight: `git diff --check` and `node --check scripts/qa-route-point-context.mjs` passed; this fresh owner worktree has no `node_modules`, so typecheck/tests/build/browser validation stayed CI-first. Exact final CODE-source CI run 34325214980 on `2ea2a57ac04f56e86c28aaf2ad70aba8c904ebfb` has `core` and every browser lane green, including the dedicated `browser-qa / route-point-context`, login-media, post-login, playback-prefetch, final-acceptance, route/city anchoring, earth-dive, mobile-contract, globe-focus-chrome, guest-share and owner-share. Its only expected failures are the missing current-PR ledger and aggregate `verify`; this next/final commit is ledger-only and must receive its own exact-head CI before evaluation/merge sign-off.
