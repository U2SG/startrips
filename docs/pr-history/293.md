# PR #293 - Reveal Route Point context before Story

- **Source head:** `0c39a2b69d7fc42b599043f572d19fb7f4958249`
- **Scope:** Issue #291, ST-053 only. Add a pure Route Point context projection and newest-intent resolver, then change Atlas Route Point activation to reveal one restrained context surface before Story. The projection derives label, Journey relationship, date/note presence, scoped visual-media count, at most one representative asset, and explicit route-point location precision only from the Journey already in scope. The single entry opens the existing Story with the same Journey + Route Point identity; no new camera/focus owner, Story viewer, POI/geocoder feed, per-asset GPS inference, or persistent map-mode toolbar is introduced.
- **User-visible change:** Selecting a Route Point in Atlas focus flow first shows one quiet context panel answering what was left there, including truthful date/note/media/location-level metadata. Text-only Route Points remain meaningful without fake media loading, and entering Story preserves the exact selected Journey + Route Point. The context is planet-only and remains bounded/scrollable for long notes.
- **Review fixes:** Automated review found three P2 issues: retained context now hides outside the planet view, refreshes from the latest Journey object after Story edits/media changes, and stays viewport-bounded/scrollable for long notes. Owner CFAA then found two ownership/posture blockers. Final Source `0c39a2b` makes Route Point context subordinate to the existing active Journey so a visible point on sibling Journey B is ineligible while Journey A owns Atlas semantics, preventing a second semantic owner without moving camera/focusRevision. The context surface also keys compact layout from the product-owned `data-mobile-v2="on"` posture rather than a width-only media query; the dedicated QA adds coarse-pointer 844x390 and 932x430 landscape fixtures and verifies one reachable surface, safe viewport bounds, a >=44px entry target, and unchanged camera/focusRevision ownership.
- **Follow-up:** None for this slice. Scene raycast/startup, Semantic Earth Dive, Story media compositor, Playback return, and broader Atlas attention hierarchy remain owned by their existing merged contracts/issues.
- **Validation:** Local validation remains intentionally lightweight: `git diff --check` and `node --check scripts/qa-route-point-context.mjs`; full validation stays CI-first. Exact final CODE-source CI run 34327699752 on `0c39a2b69d7fc42b599043f572d19fb7f4958249` has `core` and every browser lane green, including `browser-qa / route-point-context`, `mobile-contract`, login-media, post-login, playback-prefetch, final-acceptance, route/city anchoring, earth-dive, globe-focus-chrome, guest-share and owner-share. Its only expected failures are the stale ledger and aggregate `verify`; this next/final commit is ledger-only and must receive its own exact-head CI before evaluation/merge sign-off.
