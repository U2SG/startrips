# PR #551 - Close Detail Route Point context on blank map click

- **Source head:** `779a382e944550c730d5e50bee3bd3d4ae1c1e26`
- **Scope:** Let native Detail Map clicks resolve a Journey Route Point first, an active Composer point pick next, and bare map canvas last. Reuse the existing blank activation callback so the Detail card has the same close intent as the overview globe.
- **User-visible change:** Clicking empty Detail Map space dismisses the selected Route Point card while keeping the Journey, overlay, and Detail camera owner in place. Dragging the map keeps the card open; marker clicks still open the matching card, and Composer can still pick a point.
- **Review fixes:** Independent focused source review found no blocking defect. The browser QA uses native marker, drag, blank-canvas, and Composer clicks and checks the card ID, Journey identity, overlay, owner, and picked coordinates after the camera moves.
- **Follow-up:** Continue #489 fullscreen signed-video expiry and retry in an independent media owner. Keep #514 route summary as a projection over complete route and media facts, with #512 import priority preserved.
- **Validation:** Exact Source CI [36049741594](https://github.com/U2SG/startrips/actions/runs/36049741594) passed core, Keepsake render, and all nine browser QA shards, including globe-context. Ledger and dependent verify failed only because this final ledger was absent. This final commit changes only this ledger; exact final-head CI remains required.