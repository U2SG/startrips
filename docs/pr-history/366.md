# PR #366 - Keep Journey Rail lifecycle visibility owned by Atlas state

- **Source head:** `552fa5cbb8389b0b5d7df125a5d8c8e13b3e7238`
- **Scope:** Implements the third recurrence repair for ST-063 / issue #325 on exact main `2af73c7474a415548db5b90fd846467d2157f6a8`. The fix removes state-dependent inline Journey Rail visibility ownership, makes current Living Atlas lifecycle classes the sole hide authority, explicitly releases the rail with CSS `visibility: visible`, and clears stale inline visibility on each layout commit. The preserved ST-062 `journey-rail-state` diagnostic and visible/enabled/hitOwned actionability contract are unchanged; no retry, wait, polling, timeout inflation, force-click or test-only tolerance change is introduced.
- **User-visible change:** After responsive transitions and Playback/Story return, the desktop Journey Rail can no longer inherit stale imperative `visibility:hidden`; when Atlas lifecycle classes release the rail it is synchronously visible and hit-testable over the globe.
- **Review fixes:** Fresh exact-Source Codex review on `552fa5cbb8389b0b5d7df125a5d8c8e13b3e7238` completed with no major issues (`5672933986`). Exact Source CI `34914208900` has `core` and all required observed product/browser lanes green; `browser-qa / final-acceptance` recorded `railVisibility:"visible"` at 320, 360, 390 and 430. The named node/Vitest regression in `src/journey/LivingAtlasApp.test.ts` covers stale inline rail visibility release ownership.
- **Follow-up:** ST-066 / PR #361 remains frozen cross-lane-blocked without ledger until this ST-063 recurrence repair is maintainer-merged and the resulting exact-main push CI is green; only then may ST-066 rebase/revalidate/review and reseal. ST-070 remains pending. No route geometry, Earth Dive, Backend, tag, release or deployment work is folded into this PR.
- **Validation:** Lightweight local validation is `git diff --check`; GitHub CI is authoritative for product/browser coverage. Exact Source CI run `34914208900` on frozen Source `552fa5cbb8389b0b5d7df125a5d8c8e13b3e7238` was executed twice on the identical head: attempt 1 and attempt 2 both passed `browser-qa / final-acceptance`, and the preserved `[qa-post-login] final:<viewport>:journey-rail-state` markers reported `railVisibility:"visible"` at 320, 360, 390 and 430 in both attempts. Attempt 1 also passed `core` and every observed required product/browser lane; only intentionally absent-ledger `ledger` and downstream `verify` were red pre-seal. This commit is ledger-only; exact final-head CI must pass `core`, `ledger`, `verify`, final-acceptance and required product/browser lanes before HANDOFF_REVIEW.
