# PR #477 - Shard browser QA to reduce CI queue latency

- **Source head:** `97015ff1cbe7c5cf38afc2a9390a7623a02e5073`
- **Scope:** Collapse the 24 independent browser QA jobs into 8 duration-balanced shards while preserving every logical suite, evidence artifact, and fail-fast=false diagnostic breadth; preserve stable logical QA-suite failure identity across physical shard names; retain the original 12-minute timeout per logical suite with shard-level continuation after failure/timeout; and add a fail-closed ledger-only final fast path that reuses exact Source CI only when the final is exactly one ledger-only commit and the Source product/browser lanes are proven green.
- **User-visible change:** None. This changes CI/control-plane execution only and does not alter Startrips runtime behavior.
- **Review fixes:** Independent Hourly Review requested two P1 fixes on the earlier Source: preserve recurring failure-family identity after sharding and preserve per-suite failure isolation/timeout budgets. Exact Source `97015ff1cbe7c5cf38afc2a9390a7623a02e5073` resolves both: `STARTRIPS_QA_SUITE` markers keep logical lanes/fingerprints stable, including city-label routing without keyword dependence; and `browser-qa-shard.mjs` enforces the original 12-minute cap per suite while continuing later suites after timeout/failure. Hourly Review re-reviewed this exact Source and recorded CLEAR for seal with no remaining Source blocker.
- **Follow-up:** Issue #476 is closed by this PR. Exact-main push CI remains full validation; the ledger-only fast path is pull-request-only and fail-closed on code drift, extra final commits, missing/unknown Source CI, or any product/browser failure.
- **Validation:** Exact Source CI run 35596109081 passed `core`, `keepsake-render`, all 8 browser shards, and both control-plane platforms; only the expected pre-SEAL `ledger` (`Validate current PR ledger`) and downstream `verify` failed. All 24 logical browser suites remain represented exactly once, per-suite timeout/failure continuation regressions pass, and the slowest browser shard completed in about 5m35s from workflow start versus the ~19m40s baseline. This final commit changes only `docs/pr-history/477.md`; exact final-head `verify` and merge-readiness remain required before merge.
