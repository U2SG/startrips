# PR #307 - Render Home Base presence in the Living Atlas

- **Issue relationship:** `Refs #233` — intentionally not `Fixes #233`; issue #233 remains open for the residual Home activation/context interaction.
- **Source head:** `fcc9df852d833bb688eb9877de0025261062aaa6`
- **Scope:** ST-056 only. Wires the shipped ST-028 Home Base presence/camera contracts into the ordinary Living Atlas runtime: `LivingAtlasApp` resolves Home presence from the already-loaded periods and live Atlas context, `LivingAtlasGlobe` receives a Home presence descriptor, and the particle scene reuses the canonical scene anchor/projection for Home positioning and the initial orientation seed. Camera ownership explicitly hands off from the one-time Home seed to manual interaction, Playback, explicit Journey selection and Rewind/timeline intent. Guest/shared Atlas keeps Home data out of the runtime, and the Home descriptor remains accessible and touch-safe without introducing a second geographic projection/radius path.
- **User-visible change:** A signed-in user's effective Home can appear as quiet persistent spatial context in the particle Atlas and can orient a fresh unselected Atlas initially. Home remains weaker than an explicitly selected Journey, does not appear for guests, adds no permanent compact-mobile Home control or contextual Home panel, and introduces no new breathing/glow/gold treatment.
- **Review fixes:** The review sequence repaired the asynchronous Home seed revision and marker gesture passthrough; Playback camera ownership; complete latitude/longitude Home seed convergence; explicit Journey-selection ownership plus Rewind timeline context; all-time effective-current Home semantics; suppression of fallback Journey `focusPoint` while Home owns the fresh seed; Earth Dive persistence of the Home geographic anchor; offscreen Home hiding; Home projection updates moved outside React render cycles; and, on the final Source, the hidden non-planet globe subtree is `inert` so its Home marker and other hidden descendants cannot retain keyboard focus.
- **Follow-up:** #233 remains OPEN for the residual on-demand Home activation/context interaction and is deliberately not closed by this PR. #291 is not the owner of that remaining Home slice. No contextual Home panel/content, guest DTO, permanent mobile Home control, Playback topology rewrite, ST-011/ST-049/server/Home-inference/Keepsake work, or particle/coastline/render-budget visual retune is included here.
- **Validation:** Exact CODE Source `fcc9df852d833bb688eb9877de0025261062aaa6` was reviewed by Codex on 2026-09-10 with no major issues. Exact CODE-head CI run `34440018448` completed with every product lane green, including core and all browser QA lanes (city-label-anchoring, final-acceptance, login-media, globe-focus, earth-dive, route/share/render-budget/playback-prefetch/mobile-contract); the earlier city-label coastline wait timeout did not reproduce. On the CODE head, only the intentionally missing-ledger `ledger` failure and aggregate `verify` failure remained. This final commit adds only `docs/pr-history/307.md`; repository lightweight ledger validation is run after creation, with frozen `docs/pr-history.md` unchanged.
