# PR #453 - Wait for the compact-mobile layout commit before measuring Composer viewport-switch action metrics in browser-qa post-login

- **Source head:** `717280632a7850aa1a96e53e14870ecb0ecd0d3f`
- **Scope:** Already on main: JourneyComposer.tsx sets `.journey-composer[data-mobile-layout="true"]` from `useCompactMobileLayout()` (mobileLayout.ts), which resolves `COMPACT_MOBILE_MEDIA_QUERY` asynchronously via a matchMedia change listener/React state commit after a viewport change. The real gap: `verifyComposerMediaActions` in scripts/qa-post-login-controls.mjs (loop at ~lines 671-718) calls `page.setViewportSize()` then immediately reads `.journey-media-fields__actions` with no wait for the attribute to reflect the new viewport, so the tablet sample can observe the stale mobile DOM and report `actionMetrics: []`, as seen in run 35480202788 on PR #447's Source.
- **User-visible change:** Wait for the compact-mobile layout commit before measuring Composer viewport-switch action metrics in browser-qa post-login
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35507426513 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
