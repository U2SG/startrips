# PR #519 - Add coherent multi-issue delivery packages

- **Source head:** `adf043191cb58d75b9011ceda946a2a5ad234d99`
- **Scope:** Add backward-compatible coherent delivery-package support across the sole ONE schema, package membership/scope revision tracking, selector/action/state/review/seal/reconcile helpers, guarded runtime activation, and package regression coverage while preserving legacy single-issue behavior.
- **User-visible change:** None immediately. This control-plane capability allows one validated delivery unit to cover several tightly related existing issues after a separately guarded runtime activation and ONE package registration.
- **Review fixes:** Closed duplicate-member/open-PR ownership gaps, package-wide issue-window and ONE lifecycle races, fail-open atomic-store behavior, and partial-install rollback hazards. Activation now preserves a durable owned STOP plus exact predecessor evidence until either the reviewed runtime verifies exactly or rollback proves every predecessor byte and prior receipt were restored.
- **Follow-up:** Runtime installation and the first pilot package remain separate guarded operations at a proven idle safe boundary; no package is registered or activated by this ledger commit. Existing uncommitted #399 reliability work in this local worktree is deliberately excluded from this PR final.
- **Validation:** Independent Hourly Review marked exact Source `adf043191cb58d75b9011ceda946a2a5ad234d99` CLEAR. Control-plane CI passed on Ubuntu and Windows, and exact-Source product CI run 35799351756 passed core, keepsake-render, and all eight browser lanes; ledger and verify were the expected pre-ledger failures before this final-only commit.
