# PR #499 - Separate core policies and runtime owners

- **Source head:** `4ab373a1f1060549b1775f0bb59cf2f3884cdcd0`
- **Scope:** Extract Story, Composer and Playback policies and public contracts into model owners; move complete multipart-upload, Journey-media and map-cache operations into services; give scene modes/budgets direct owners; resolve DEV preview families before mounting and isolate legacy styles. Add dependency, production-bundle and browser-entry regression guards. Story/Composer/Playback roots expose 12/4/1 declarations instead of 48/15/9; the upload router exposes only its HTTP routes.
- **User-visible change:** No intended change to product flows. Production entry CSS decreases from 232.18 to 226.60 kB (gzip 40.94 to 39.52 kB); reported entry JS remains 1,389.10 kB. Product QA fixtures avoid legacy modules/styles, while desktop/mobile legacy fixtures retain their real content and layout.
- **Review fixes:** Preserve current state, history, signed-read/decode, save/recovery, playback/trim and renderer writers. Preserve complete-upload replay/status checks before request-body parsing, Atlas/lease/Journey lock order, sorted cross-Journey locks and the two existing trusted-upload unscoped lock calls. First CI exposed a widened Buffer type and the stale browser-suite contract; retain the concrete ArrayBuffer-backed result type and update the exact suite inventory to 26 logical suites across eight shards.
- **Follow-up:** None required for this bounded refactor program. Concurrent provider-auth and route-spline work remain with their existing owners; schema, migrations, deployment and external ONE state are unchanged.
- **Validation:** All 63 changed files independently reviewed CLEAR, with static AST/body and test-preservation checks. [Exact Source CI 35685138327](https://github.com/U2SG/startrips/actions/runs/35685138327) passed core (170 test files / 2,376 tests, 352 control-plane regressions, typecheck and production build), Keepsake rendering and all eight browser groups. The five new entry-boundary samples passed, including real desktop/mobile legacy content and computed styles. Source failed only for the not-yet-added current-PR ledger and its dependent verify gate. No local tests, typecheck, build, browser or database execution. The final commit adds only this ledger; exact final CI must prove Source reuse and pass before merge.
