# PR #494 - Accept shipped cover-reveal preset metadata

- **Source head:** `0ef3428303948bc31d93b842812d58c50b20b18e`
- **Scope:** Make the shipped `reveal-flow-ink-wash-v1` derivative metadata consumable by the Journey cover opening without regenerating existing derivatives or changing the deployed worker contract; map that one legacy pipeline id to the vendored `ink-bloom` RevealFlow preset while preserving the five native renderer ids exactly and keeping every other unknown preset fail-closed.
- **User-visible change:** Existing owner Journey covers whose generated first image is already ready can finally play the automatic art-to-memory opening and settle onto the canonical original cover. Existing derivatives require no regeneration or database migration.
- **Review fixes:** Automated review identified inherited `Object.prototype` keys as a fail-closed hole in the legacy alias lookup. Exact Source `0ef3428303948bc31d93b842812d58c50b20b18e` guards the map with `Object.hasOwn(...)` and adds explicit `constructor` / `toString` regressions. The review thread is resolved/outdated, and independent Hourly Review has cleared this exact Source for seal.
- **Follow-up:** Parent #367 remains the broader cover-reveal product/platform tracker. This PR intentionally does not change worker generation metadata, replay policy, Reduced Motion, owner-only authorization, interruption semantics, or canonical cover ownership.
- **Validation:** Exact Source CI run 35684283588 passed `core`, `keepsake-render`, and all 8 browser shards, including `reveal-share` whose real `qa-cover-reveal-opening` fixture now uses the shipped `reveal-flow-ink-wash-v1` metadata. Only the expected pre-SEAL `ledger` and downstream `verify` failed. Local `node --check scripts/qa-cover-reveal-opening.mjs` and `git diff --check` passed. This final commit changes only `docs/pr-history/494.md`; final-head `verify` and merge-readiness remain authoritative before merge.
