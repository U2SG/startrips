# PR #241 - Put the coastline on the geographic surface and project every layer through one frame

- **Source head:** `a5679f5bc989734e107d38534ee6219588499636`
- **Scope:** Fixes #237 Phase A and Phase B. Coastline geometry (far/mid/near builders, detailed coastline loading, and regional refinement defaults) now uses `GEOGRAPHIC_SURFACE_RADIUS` instead of a separate raised shell. Coastline readability is preserved with render ordering plus a clip-space Z-only depth bias that leaves screen X/Y/W unchanged and tapers to zero at the limb/far side. A new shared `src/scene/projection.ts` owns the globe-local-to-clip projection frame, horizon visibility, clip-viewport test, geographic-anchor projection, and candidate globe model composition; Place Labels, Route Points, Journey connector projection, focus-signal instrumentation, the focus rotation solver, city scanning, and coastline QA consume that authority. Browser QA now measures a real active-LOD coastline vertex against nearby place-label motion through continuous drag, with explicit coverage/stall guards. No schema or server data-model change.
- **User-visible change:** Coastlines and geographic annotations now inhabit one geographic surface and one projection space, reducing zoom/drag parallax between the map outline, place labels, route anchors, focus signal, and journey connector. Coastlines remain visually readable above the globe without being physically lifted outward, while far-side/horizon occlusion remains depth-tested. Mobile and desktop share the same projection behavior.
- **Review fixes:** Removed two accidental U+0008 control characters from the source-text contract test so the decorative `1.405` literals are genuinely checked; corrected the explicit regional-radius assertion to Float32-appropriate precision; hardened the map-vs-label browser QA so it waits for rendered animation frames, excludes stalled/sub-pixel samples, requires enough comparable motion pairs and a meaningful widest step, preventing a same-frame `0px/0px` false pass.
- **Follow-up:** #240 tracks the remaining `PERSONAL_SIGNAL_RENDER_LIFT = 1 + 1e-4` visual lift on the focus glow shader. Decorative wire, relief support, atmosphere, archive shell, cluster jitter, and route arc lift remain intentionally outside the geographic-surface invariant; `DetailedEarthMap` remains a separate MapLibre projection.
- **Validation:** Exact Source-head CI #745 on `a5679f5bc989734e107d38534ee6219588499636`: `core` completed successfully (typecheck, 1006-test suite including the new projection/coastline contracts, and build); `browser-qa / city-label-anchoring`, `route-anchoring`, `mobile-contract`, `final-acceptance`, `post-login`, `login-media`, `owner-share`, `guest-share`, and `playback-prefetch` all executed successfully. The workflow conclusion was failure only because the required PR ledger was intentionally absent at the Source head; `ledger` failed at `Validate current PR ledger`, and `verify` consequently failed. Merge-readiness #1436 was success. This ledger commit must be the only change after the Source head before merge.
