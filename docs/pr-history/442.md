# PR #442 - Give browser-qa/city-label-anchoring a semantic coastline-readiness signal and fix its unconditional-checks/gated-wait frame race

- **Source head:** `c78608604b0a0c7fc8f10abc9a95128c1586b0d7`
- **Scope:** Verified on origin/main scripts/qa-city-label-anchoring.mjs: waitForLocalCoastline() (~L387) is called only for the dense-coastline/zoom-3 case (~L857) and blocks on page.waitForFunction over data-coastline-source/data-coastline-refinement with a bare {timeout:5_000} and no diagnostic of which stage is still pending -- the original reported fingerprint, still unaddressed on main. Separately, ~L930-999: measuresCoastline (fixture.key==='dense-coastline') gates await waitForRenderedFrame(page) at L962, but the farthestSeen>=reachable*OUTWARD_COVERAGE check at L999 runs unconditionally for every fixture, so large-us/island/inland-control sample before the scene's rAF anchor write lands -- the 'outward-coverage' race the issue's occurrence 4-6 comments diagnose in detail. That race fix (ungate the wait) is written and CI-verified as commit e2078271 but only exists on the still-OPEN, unmerged PR #436 (a different feature/issue's branch), so it is not yet on main either. Out of scope here: occurrence 6's follow-on hypothesis that the OUTWARD_COVERAGE floor itself should derive from selectCityCandidates/persistentCities admission rather than projection optics is explicitly 'inference, not yet confirmed' by its own author and needs a discriminating experiment before any repair is chosen -- not baked into this feature's acceptance.
- **User-visible change:** Give browser-qa/city-label-anchoring a semantic coastline-readiness signal and fix its unconditional-checks/gated-wait frame race
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35465608787 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
