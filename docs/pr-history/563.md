# PR #563 - Keep Playback media continuous and return to the committed context

- **Source head:** `1f13dde45229311d961e32c88f409e108671aeeb`
- **Scope:** #126 R2 Q1–Q3 Playback presentation and a focused #245 return regression. The opening still shares the stage slot's aperture geometry, and its first media enters at depth 0 with the map-to-media settle starting from identity. The departing slot is preset-clipped to the incoming contain rect and fades in place before removal; alternating slots clear stale clip and opacity. Populated chapters use a content-sized caption row and fold the arrival cue over `--motion-ui` instead of scaling. Reduced Motion keeps the same layout, and a long note folds into a scrolling band during media. Stage padding reserves the transport's footprint. Manual exit returns to the last committed valid narrative position; natural completion returns to the selected Journey's Atlas context.
- **User-visible change:** The first photo of a Route Point no longer jumps in size when its chapter starts. Mixed landscape/portrait swaps no longer expose old side panels or pop out. Settled media is larger, sits higher and is never covered by the transport. A manual exit returns to the last committed media or Route Point; a naturally completed run returns to the still-selected Journey in Atlas.
- **Review fixes:** The first independent review's four findings were addressed: the departing image fades within a fixed crop with a 0.5% exposure tolerance, interrupted swaps restore deterministic poses, long notes use a bounded scrolling band in both motion modes, and the recorder grades retained video canvases. Hourly Review then identified a #245 contract mismatch: natural completion now returns to Atlas, while manual exit preserves the latest committed media or Route Point rather than an earlier entry asset. Browser QA distinguishes manual exit from natural completion.
- **Follow-up:** #498 dense (10+) chapters stay on `few` and are owned by ST-137. Home beats have unit coverage but no separate browser fixture. Quick Recap reuses the presentation path and has no separate dynamic scenario here.
- **Validation:** Source CI [36241400782](https://github.com/U2SG/startrips/actions/runs/36241400782) passed core, keepsake-render and every browser QA shard, including `playback-brand` with Q1–Q3 seam geometry and the two #245 return paths. Ledger and verify failed only because this ledger still named the previous Source. This ledger-only final head's CI is authoritative. Fresh independent review of the current Source remains required before merge.
