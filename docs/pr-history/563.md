# PR #563 - Keep Playback media continuous from opening still to settled stage and return to the last presented media

- **Source head:** `f9bc870287275fe3cae5d4e9990f0dcfbc1856d0`
- **Scope:** #126 R2 Q1–Q3 and #489 V8 in one Playback presentation slice. The opening still shares the stage slot's aperture geometry, and its first media enters at depth 0 with the map-to-media settle starting from identity. The departing slot is preset-clipped to the incoming contain rect and settles at opacity 0 before removal, and alternating slots clear stale clip and opacity. Populated chapters use a `fit-content(34%)` caption row and fold the arrival cue over `--motion-ui` instead of scaling; Reduced Motion keeps the arrival layout. The transport's footprint is reserved as stage padding, and compact padding is tighter. `commitNarrativePlaybackPosition` keeps the last presented media across intro, outro, home, travel and Stop beats. A completed run with presented media returns to Story at that media.
- **User-visible change:** The first photo of a Route Point no longer jumps in size when its chapter starts. Mixed landscape/portrait swaps no longer expose old side panels or pop out. Settled media is larger, sits higher and is never covered by the transport. Leaving or finishing Playback reopens Story on the last media shown; a run that showed no media still returns to Atlas.
- **Review fixes:** Advisor review before push moved the departing clip from a spring-from-zero to a clip preset before motion. The exposure check now measures against the settled incoming aperture, and the arrival cue's overflow clip is confined to the fold.
- **Follow-up:** #498 dense (10+) chapters stay on `few` and are owned by ST-137. Home beats are covered by unit tests only, not by a browser fixture.
- **Validation:** Locally, client `tsc --noEmit`, `git diff --check` and `node --check` on the edited QA scripts passed. The server typecheck in the shared install lacks `@aws-sdk/client-s3`, which is environmental. GitHub CI is authoritative: `core` covers the V8 unit cases, and `browser-qa / playback-brand` runs `qa-playback-continuity` (per-frame seam geometry at 1920×1080 and 390×844, with motion and reduced), `qa-playback-manual-camera` (return after outro/intro and after completion) and `qa-playback-map-bridge`.
