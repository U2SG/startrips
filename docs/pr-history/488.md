# PR #488 - Reuse scoped media reads and geographic projections

- **Source head:** `b3a48fd05d6fe3577b1492dfba31d7707f850182`
- **Scope:** Group Composer media summaries once, reuse Story scoped media indexes and Set membership for optimistic ordering, batch cross-Journey move/undo response reads, combine media-evidence authorization and optional evidence retrieval, and reuse canonical geographic anchors for city labels and each synchronous focus solve.
- **User-visible change:** Preserve media order, names, owner scope, unknown evidence, undo and projected anchors while reducing repeated work. Normal cross-Journey response preparation uses three SELECTs instead of six; evidence reads use one instead of two. Composer grouping and optimistic order append are linear in their inputs. City anchors retain the canonical radius-aware snap conversion and each focus solve converts its geographic anchor once. No end-to-end latency or bundle-size improvement is claimed.
- **Review fixes:** Keep persisted IDs separate from draft IDs, first-match Story lookup distinct from optimistic ordering's existing duplicate-ID behavior, source/destination response matching by ID, and absent evidence distinct from unauthorized assets. Keep transaction-internal reads because concurrent deletion can change READ COMMITTED visibility. Cache canonical city anchors instead of scaling pre-snapped unit directions. Current media/read/decode/history writers and all async scope guards remain at their existing roots.
- **Follow-up:** None required for this bounded refactor. No schema/migration, provider/auth, route-spline or external ONE changes; further refactor batches remain separate work.
- **Validation:** Exact Source independently reviewed CLEAR across all 11 changed files; static diff check passed. [Source CI 35677282632](https://github.com/U2SG/startrips/actions/runs/35677282632) passed core (164 test files / 2,345 tests, 352 control-plane tests, typecheck and production build), Keepsake rendering and all eight browser QA groups. The only Source failures were the missing current-PR ledger and its dependent verify gate. Behavior coverage includes mixed/pending media, scope/order changes, requested Journey subsets and tenant/deletion boundaries, Fragment absent/foreign evidence, and exact projection equivalence including radius/snap and far-side cases. No local test, typecheck, build, browser or database execution. The final commit changes only this ledger; exact final CI must prove the Source reuse relation and pass before merge.
