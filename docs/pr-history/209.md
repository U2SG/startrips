# PR #209 - Resolve Keepsake scene durations through narrative timing

- **Source head:** `81cf9c7442606d757a2c205bc1e39ccae4cef385`
- **Scope:** `sceneForStep()` in `src/journey/journeyKeepsake.ts` stops calling `stepDurationMs()` and resolves every desired scene duration through `resolveNarrativeTiming({ mode: "keepsake", tempo: "standard", ... })`, passing great-circle radians for travel and trimmed note length for arrival; the intro-media literals `6000` / `4500` go through the same call. A local `keepsakeDurationMs()` helper pins mode and tempo in one place (decision D3: fixed 15/30/60 presets, no tempo control). `fitKeepsakeSceneDurations()`, `buildKeepsakeRenderManifest()` and `assertKeepsakeManifestRevision()` are unchanged, and `PLAYBACK_PACING` and its helpers stay in `journeyPlayback.ts` for PR 6. PR 3 of the shared narrative timing plan (#126, #166, #197, #87).
- **User-visible change:** None intended. The `keepsake` resolver profile is seeded from `PLAYBACK_PACING`, so a before/after dump of every scene duration on the test fixture is identical at the 15 s and 30 s presets; at 60 s one travel scene moves 1312 -> 1313 ms and `actualDurationMs` 60000 -> 60001 ms, because the legacy `travelDurationMs()` returned an unrounded float while the resolver contract is an integer and the preset fit redistributes proportionally.
- **Review fixes:** None yet.
- **Follow-up:** `narrativeTiming.test.ts:20, 322-334` imports `PLAYBACK_PACING` deliberately to assert the seeded keepsake profile reproduces it, so the table still has a consumer that the plan's PR 6 paragraph does not list; PR 6 must convert that equivalence assertion to inline literals rather than drop it. The resolver's `intrinsicDurationMs` branch stays unreachable from Keepsake until media assets carry a source duration.
- **Validation:** `pnpm install --frozen-lockfile`, `pnpm typecheck`, `pnpm vitest run src/` (41 files, 528 tests; `journeyKeepsake.test.ts` expectations unedited plus one new test pinning the resolver's note, distance and media-kind sensitivity through the fit), `git diff --check`. No dev server, browser or database checks ran locally; GitHub CI remains the authoritative integration and build verification.
