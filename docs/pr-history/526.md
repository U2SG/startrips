# PR #526 - Keep native Windows smoke coverage off CIM

- **Source head:** `76736914f76428a0e6802f01097ab9698ebfbcf1`
- **Scope:** Remove the obsolete CI compatibility smoke that called Win32_Process CIM after the runtime switched to native Windows process observation, while retaining native own-PID identity coverage and a regression that forbids reintroducing CIM or PowerShell into `execution.snapshot()`. Addresses #399.
- **User-visible change:** None directly; control-plane Windows CI no longer depends on the CIM service that the runtime intentionally removed.
- **Review fixes:** Independent Hourly Review cleared this exact two-file Source and confirmed runtime fail-closed process identity semantics remain unchanged.
- **Follow-up:** Runtime installation remains a separate guarded maintenance operation after all current PRs close. This ledger does not install, sign, merge, dispatch owners or alter STOP state.
- **Validation:** Exact Source control-plane CI passed Ubuntu and Windows, and product Source CI passed core, keepsake-render and every browser shard; pre-ledger ledger/verify failures are expected. Local validation is `git diff --check` plus PR-ledger validators only; no local test suite. Fresh final-head CI is required before Maintainer disposition.
