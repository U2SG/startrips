# PR #481 - Expose a sanitized available-link-providers capability list from the account-identity read

- **Source head:** `d298e20ef08f2a7f97ceeb036abb4479aa1fc82b`
- **Scope:** ST-067 (server/routes/account-identities.ts, merged) shipped GET /api/account-identities returning only redacted `methods` plus the router-local usableProviderIds/linkableProviderIds used by link-intents. It never surfaces linkableProviderIds to the client, so the future #348 UI has no authoritative way to know which social providers are actually configured versus decorative. The real gap is exactly that: add a sanitized `availableLinkProviders` field to the GET response, derived from the same `linkableProviderIds` set the POST /link-intents route already enforces, provider IDs only, no secrets/tokens.
- **User-visible change:** Expose a sanitized available-link-providers capability list from the account-identity read
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35625159312 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
