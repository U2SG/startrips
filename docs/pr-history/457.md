# PR #457 - [Experience] Wire the persisted Earth experience preference into the account menu and Earth Dive policy

- **Source head:** `8e20bc2dd3a669f845251e7d292334f3ff3374b1`
- **Scope:** ST-087/#387 already persists default|particle-only per stable user (GET/PUT /api/account-preferences/earth-experience, server/routes/account-preferences.ts) and ST-084/#331 already enforces the policy inside src/scene/earthDive.ts and LivingAtlasGlobe once given an earthExperiencePolicy prop. The real gap, confirmed on origin/main: docs/architecture/account-earth-experience-preference.md states outright 'the client account-menu surface, the hydration and cache strategy, and wiring the stored value into the Dive are deliberately not part of this contract... Nothing in the product reads this preference yet.' src/auth/AuthGateway.tsx's account menu (desktop account-dock and mobile account-sheet, which already hold invite/edit/sign-out) has no Earth experience entry; src/main.tsx hardcodes earthExperiencePolicy to a local default and never calls the API, so LivingAtlasApp/LivingAtlasGlobe always render 'default' regardless of any saved preference. This feature builds the missing client integration only: menu entry, hydrate-on-load with a safe pending state, write with revision-guarded conflict handling, guest/device fallback, and account-switch cache isolation.
- **User-visible change:** [Experience] Wire the persisted Earth experience preference into the account menu and Earth Dive policy
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35503375753 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
