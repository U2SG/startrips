# PR #329 - Productize the v12 signature loading clip

Addresses #302.

- **Source head:** `94232d8dac9ce8db0277c0d058ae29cfef238e33`
- **Scope:** Implements the narrow ST-055 slice on top of the already-approved v12 identity from #299. `src/brand/startripsV12MotionSource.ts` commits the canonical authored timing/order dataset extracted from the approved standalone `startrips-logo-motion-v12.html` study, recording source SHA-256 `7931733faedeaea68f7999d8c2d95e0ee12f475466cbe9714d0b773987d25b91`. `src/brand/startripsSignatureTimeline.ts` exposes semantic `full` and `loading` clips from that one timing source; consumers request clip names rather than raw seconds, and the 3.6 s loading clip is intentionally remapped instead of uniformly scaling the 25.4 s authored study. `src/brand/startripsSignatureRuntime.ts` provides one interruptible rAF driver; `StartripsSignatureMotion.tsx` renders the existing committed `/brand/startrips-v12-wordmark.svg` geometry through fragment references, so no second path dataset is introduced. The existing `StartripsBrandLoader` uses `clip="loading"` only. Browser QA and core tests cover timing order, geometry consistency, lifecycle cancellation, bounded DOM/listener/driver counts, hidden/offscreen suspension, initial-hidden mounting, terminal non-looping completion and Reduced Motion.
- **User-visible change:** Initial/auth loading uses a condensed v12 character motion instead of the old whole-wordmark breathe loop. The motion notices the star, transfers support through staggered front hooves before the rear legs, then settles back to the approved static v12 lockup in 3.6 seconds. The i-dot star scales around its authored center rather than drifting with the scale transform. Pointer or keyboard intent immediately wins and settles the mark; hidden/offscreen loaders stop their driver; a loader mounted while its tab is already hidden starts suspended and excludes the hidden interval from elapsed time; Reduced Motion shows the static final identity with no timeline scheduled. The loader remains non-blocking and preserves the existing Persistent Earth underneath it.
- **Review fixes:** The first review P2 found that an already-hidden mount could start the runtime before suspension was synchronized; Source `0d63c78aab52d11452487f3038f643bb39149c01` moved `syncSuspension()` before `start()` and hardened pre-suspended startup. A later exact-head review on the prior ledger head found two additional bounded lifecycle/geometry defects. Final Source `94232d8dac9ce8db0277c0d058ae29cfef238e33` now scales the referenced i-dot around the authored `(417.408, -105.264)` center, and records non-looping `full` completion as a terminal runtime state so later visibility or Reduced Motion changes cannot restart a settled clip. Focused node regressions pin both behaviors. Both review threads were resolved only after exact-Source tests/CI and a fresh exact-head automated review reported no major issues.
- **Follow-up:** The owner decision recorded 2026-09-13 clears the ST-055 human gate: the production canonical source is the extracted timeline dataset derived from `startrips-logo-motion-v12.html`; the desktop idle companion is explicitly deferred and not approved by this PR. Also deliberately deferred: 404/empty/error motion, confirmed-result/success excerpts, arrival/chapter excerpts and permanent animated Atlas chrome. `full` exists as the canonical semantic clip but is not placed into a persistent product surface by this slice.
- **Validation:** Final CODE Source `94232d8dac9ce8db0277c0d058ae29cfef238e33` is based on exact-green `origin/main@6fed202e77b87450c6c82f57718351aa963ece19`. Local lightweight validation passed: `pnpm typecheck`; `pnpm exec vitest run src/brand/StartripsSignatureMotion.test.ts src/brand/startripsSignatureRuntime.test.ts src/brand/startripsSignatureTimeline.test.ts src/brand/StartripsBrandMark.test.ts --maxWorkers=1` (20/20); and `git diff --check`. Exact Source CI run `34754797944` passed `core`, `keepsake-render`, `browser-qa / brand-signature-motion`, `browser-qa / final-acceptance`, and every other product/browser lane. `browser-qa / login-media` attempt 1 ended in an unrelated `qa-media-controls.mjs` waitForFunction timeout after its preceding checks had passed; the failed job was rerun on the identical `94232d8` head and succeeded. `ledger` and downstream `verify` were the expected pre-seal failures because this ledger was intentionally absent during Source review. Fresh exact-head review on `94232d8` reported no major issues and both concrete review threads are resolved. Exact final-head CI after this ledger-only commit remains authoritative.

## Cross-feature assumption audit

- **#299 identity boundary:** This PR does not redraw the goat, star, typography or palette. It reuses the committed v12 wordmark/mark geometry and tests path-data consistency between `startripsV12Mark.ts`, `startrips-v12-mark.svg` and the animated wordmark goat. #299 remains the identity-adoption source; #329 adds motion productization only.
- **Owner gate checked:** The owner explicitly approved the extracted canonical timeline dataset and explicitly deferred the desktop idle companion. No persistent Atlas companion is introduced.
- **Auth/Persistent Earth checked:** Only the existing `StartripsBrandLoader` render path changes. The focused login/brand coverage preserves the Atlas Persistent Earth host and transparent background contract.
- **Attention hierarchy checked:** #246/#322 remains authoritative. This slice puts motion only on the existing task-adjacent loader; it adds no idle Atlas companion and no moving chrome while Journey/Story/Playback owns attention.
- **Fullscreen checked:** #253 quiet fullscreen chrome is untouched; no companion is added to fullscreen or Playback.
- **Reduced Motion checked:** the runtime schedules zero drivers and applies the approved final v12 pose; a naturally settled non-looping clip stays terminal instead of being rewritten by later motion-preference changes.
- **Visibility lifecycle checked:** pre-start hidden state is synchronized before `runtime.start()`, the runtime refuses to schedule when already suspended, hidden/offscreen transitions cancel the driver, and a naturally settled non-looping clip cannot be restarted by later visibility transitions.
- **Lifecycle/performance checked:** one runtime owns one rAF; repeated loading cycles keep DOM/listener/driver counts constant; dispose and newer clip ownership cancel stale work; settled `full` remains driver-free.
- **Star identity checked:** motion offset is applied separately from a center-preserving scale around the authored i-dot position, so the source SVG's own translation is not scaled around the global SVG origin.
- **Timeline source checked:** semantic pose sampling reads event boundaries from the committed canonical clip dataset rather than duplicating authored seconds; tests detect uniform playback-rate scaling and require the staggered front-hoof / rear-transfer order plus head/eye/star direction coupling.
- **Latest-main integration checked:** Source `94232d8dac9ce8db0277c0d058ae29cfef238e33` is built directly on merged main `6fed202e77b87450c6c82f57718351aa963ece19`, which includes #328 Journey Rail visibility work. This PR does not change Journey Rail implementation or its QA contract.
