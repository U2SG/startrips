# PR #556 - Bounded Nominatim fallback and residual alias normalization for Chinese foreign-landmark search recall

- **Source head:** `b8ddcbe44f7fb7dbad0c21571c0f891b018aad16`
- **Scope:** Verified gap: server/location/create-location-search.ts instantiates exactly one driver (photon XOR nominatim XOR disabled) with no composite path, and server/location/photon-location-search.ts's ENGLISH_QUERY_ALIASES (added by ST-054/#296, still intact and untouched) only covers ~22 foreign-city exonyms, not POI/landmark queries (大本钟, 圣家堂, 维多利亚港, etc.) and has no simplified/traditional Chinese variant handling (e.g. 鸭川/鴨川) anywhere in the codebase. The issue's 'Preferred production path' (rebuilding a self-hosted Photon index with zh in the import language set) is an operator/deploy action outside repo code, not implementable here; deploy/README.md currently only documents driver selection, not recommended import languages. The issue's focus-dependent regression cases (Chinatown SF vs London, Grand Central+NY focus, national-museum+focus) require the Journey `focus` contract from companion issue #546, which is open and not yet queued as any feature, so this feature is scoped to the non-focus recall/normalization gap only: (1) a bounded Nominatim fallback consulted only when a CJK query's primary Photon result is empty or has no confident Chinese/English label match, and (2) a small generalizing simplified/traditional variant table alongside the existing alias table, not one-off landmark coordinate constants.
- **User-visible change:** Bounded Nominatim fallback and residual alias normalization for Chinese foreign-landmark search recall
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source and delivery contract; resolved thread dispositions remain in the PR review history.
- **Follow-up:** Every registered delivery member retains its original issue scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 36092855063 attempt 1 passed the real product lanes; independent Source review and per-member acceptance coverage are recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
