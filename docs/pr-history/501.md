# PR #501 - Remove duplicate runtime policies and obsolete adapters

- **Source head:** `db69f301c1539e569c1a2c9ffa4cd115d1b27bf2`
- **Scope:** Remove duplicate Full Playback timing tables and obsolete city/brand/motion compatibility adapters; make Playback planning and prefetch consume the canonical narrative timing profile without changing live fractional travel semantics; consolidate particle, regional coastline and local coastline LRU algorithms behind separate `RefinementCache<T>` instances; reuse the existing tenant-scoped media lookup and active-Journey lock for delete-media and Recorded Track writes.
- **User-visible change:** None intended. This is a runtime-authority and duplication reduction refactor; playback timing, renderer cache capacities/lifetimes, media authorization, deletion ordering and Recorded Track replay semantics remain unchanged.
- **Review fixes:** Independent Maintainer review inspected the complete 30-file exact Source and found no blocker. The review confirmed timing precision/override behavior, independent 4/4/12 renderer cache lifetimes, lock/query predicate equivalence, preserved media deletion ordering, and no remaining production references to the removed compatibility exports. Zero review threads were present.
- **Follow-up:** Existing spline, provider-auth and cover-reveal work remains separately owned. This PR does not merge cache contents/lifetimes, alter Journey/media authority, or create a new timing owner.
- **Validation:** Exact Source CI run 35688241482 passed `core`, `keepsake-render`, and all 8 browser shards; only the expected pre-SEAL `ledger` and downstream `verify` failed. `git diff --check` passed, and removed compatibility symbols have no remaining production call sites. This final commit changes only `docs/pr-history/501.md`; final-head `verify` and merge-readiness remain authoritative before merge.
