# PR #287 - Invalidate stale playback prefetch intents

- **Source head:** `5dd651448966858903202e9637d59cc5713b957f`
- **Scope:** Issue #197, ST-045 only. Add one monotonic Playback narrative-intent revision across seek/next/back/tempo/plan-scope changes, guard every signed-read/image-decode prefetch dispatch by planned-vs-live revision, expose the live revision in Playback QA state, and extend the existing playback-prefetch browser evidence across seek, rapid seeks, next/back, tempo and Quick Recap rebuild boundaries. The final repair keeps the Quick Recap rebuild and prefetch gate inside the director's single narrative-intent authority: a tempo intent that awaits a projected-plan commit blocks through its N+1 revision and plan-scope commit N+2 releases the gate. Signed-read dispatch still uses the same planned-vs-live decision boundary; no parallel scheduler or second revision owner was introduced.
- **User-visible change:** Playback keeps the existing tempo-aware lookahead, pacing, hold behavior and Story-return semantics, but rapid navigation can no longer newly dispatch an older prefetch window after a newer narrative intent wins.
- **Review fixes:** Automated P1 review found that QA deduplicated rendered observations by numeric step before selecting the requested intent, which falsely failed next/back and rebuild cases; `8def1908` now selects raw events at/after the requested revision. Exact-head QA then exposed the Quick Recap N+1 -> N+2 rebuild seam. `5ff28b4` moved the rebuild-block state into the director so the plan-scope commit and dispatch share one intent authority. Final Source `5dd6514` corrects the QA contract: Quick Recap must prove the entire pre-tempo read boundary contains no `plannedIntent < finalLiveRevision` reads, but it does not require an artificial suppression event when the shared gate prevents that obsolete dispatch from being queued at all; rapid consecutive seeks still require and observe stale queued-dispatch suppression.
- **Follow-up:** None in this residual invalidation slice. Playback lookahead budgets, asset ceiling, planner arithmetic, Quick Recap pacing, Playback return and Story/focus ownership remain unchanged.
- **Validation:** Local validation was limited to `git diff --check`, `node --check scripts/qa-playback-prefetch.mjs`, and an exact source comparison proving `MAX_PREFETCH_ASSETS` and `readyMsAheadForTempo` are byte-identical to `main@2d38493de9a47f04f4e4d13c921b4d5ced61f846`. Exact Source-head CI run 34307826985 verified `core`, final-acceptance and every browser lane green, including `browser-qa / playback-prefetch`. Quick Recap advanced revision 0 -> 2 and graded every post-tempo read from the pre-action boundary with `staleReads=[]`; rapid seeks retained the required queued stale-dispatch suppression and the live window re-dispatch. The only expected Source-head failures were the stale ledger and aggregate `verify`; this final commit changes only this ledger.
