# PR #287 - Invalidate stale playback prefetch intents

- **Source head:** `f912c2d5e27a194ba1ce86fcbbfb12733ea1d8b1`
- **Scope:** Issue #197, ST-045 only. Add one monotonic Playback narrative-intent revision across seek/next/back/tempo/plan-scope changes, guard every signed-read/image-decode prefetch dispatch by planned-vs-live revision, expose the live revision in Playback QA state, and extend the existing playback-prefetch browser evidence across seek, rapid seeks, next/back, tempo and Quick Recap rebuild boundaries. The final dispatch repair queues the committed window below a frame so a newer synchronous intent can suppress obsolete planned work before its request is actually issued, while the live revision always queues a replacement dispatch.
- **User-visible change:** Playback keeps the existing tempo-aware lookahead, pacing, hold behavior and Story-return semantics, but rapid navigation can no longer newly dispatch an older prefetch window after a newer narrative intent wins.
- **Review fixes:** Automated P1 review found that QA deduplicated rendered observations by numeric step before selecting the requested intent, which falsely failed next/back and rebuild cases; `8def1908` now selects raw events at/after the requested revision. Exact-head QA then exposed that rapid seeks produced no observable stale suppression even though no stale reads escaped; final Source `f912c2d5` moves actual signed-read dispatch to the committed microtask boundary, producing a real `suppress-stale` decision before any obsolete request while re-dispatching the live window normally.
- **Follow-up:** None in this residual invalidation slice. Playback lookahead budgets, asset ceiling, planner arithmetic, Quick Recap pacing, Playback return and Story/focus ownership remain unchanged.
- **Validation:** Local validation was limited to `git diff --check`, `node --check scripts/qa-playback-prefetch.mjs`, and an exact source comparison proving `MAX_PREFETCH_ASSETS` and `readyMsAheadForTempo` are byte-identical to `main@2d38493de9a47f04f4e4d13c921b4d5ced61f846`. Exact Source-head CI run 34304356729 verified `core`, final-acceptance and every browser lane green, including `browser-qa / playback-prefetch`: rapid seeks advanced revision 1 -> 3, recorded `suppressedDispatchCount=1`, emitted `staleReads=[]`, and rendered the new-position media at step 55. The only expected Source-head failures were the missing ledger seal and aggregate `verify`; this final commit changes only this ledger.
