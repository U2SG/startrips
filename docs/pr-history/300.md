# PR #300 - Invalidate stale media placement analysis

- **Source head:** `40a17ec7f14f27e606f853e5fe39d59e2cbc6337`
- **Scope:** Issue #113 only. Add one monotonic JourneyStory placement-analysis revision/scope authority so EXIF/capture-time metadata work can finish computing but may commit only while its captured Journey, selected Route Point, Journey membership, Route Point membership and revision are still current. Keep `mediaPlacement.ts` as the pure suggestion engine; lifecycle correctness stays at its async consumer boundary.
- **User-visible change:** Switching Journey or Route Point, deleting a Journey/Route Point, starting a newer analysis, or leaving Story can no longer let an older media-analysis result reopen placement review, start an obsolete upload, or clear the analyzing state owned by a newer selection. Current-scope behavior, including the #86 single complete-group auto-upload path, is unchanged.
- **Review fixes:** Codex P1 found that React StrictMode effect replay permanently disposed the first authority instance. Final Source adds an explicit setup-side `resume(scope)` that reactivates the same component-owned authority with a fresh revision while cleanup still disposes it; focused coverage proves pre-replay intents remain stale and a new post-replay intent becomes current. The earlier membership repair also invalidates an analysis when any currently addressable suggestion-target Route Point disappears.
- **Follow-up:** CFAA inspected upload completion/refresh, placement retry groups and signed media-read completion. Upload navigation remains under existing `mutationPending` ownership, placement retry is explicit user intent, and signed-read completion already has Story media-read scope/revision ownership; no concrete sibling stale-commit issue was evidenced in this slice.
- **Validation:** Local validation intentionally limited to `git diff --check`; the fresh worktree has no `node_modules`. Exact CODE-source CI run 34359335482 on `40a17ec7f14f27e606f853e5fe39d59e2cbc6337` has `core` and every browser lane green, including login-media, post-login, playback-prefetch, final-acceptance, mobile-contract, route-point-context, globe-render-budget and share/geographic lanes. Its only failures are the intentionally missing current-PR ledger and aggregate `verify` before this seal. The reviewed StrictMode P1 is resolved after this exact-head evidence.
