# PR #310 - Derive Quick Recap duplicate clusters from verified content identity

- **Issue relationship:** `Addresses #127` - ST-058 closes only the exact-content duplicate identity input gap; issue #127 remains open for the broader deterministic media-digest and photo-first exploration.
- **Source head:** `cec48726a505d3af55a4b317cba659d3820d2550`
- **Scope:** ST-058 only. `quickRecapDigestsForJourney` maps persisted owner-media content identity to `MediaDigestV1.similarity.duplicateClusterId` only when `contentHashVerified === true`, consuming the authoritative stored-byte provenance contract landed by #311/#313. Equal but unverified client-declared hashes, null/undefined/empty hashes, and guest-shaped media produce no similarity signal. `JourneyMediaAsset` declares optional `contentHash` plus optional `contentHashVerified`; no server/schema/storage/upload code is changed here.
- **User-visible change:** Quick Recap may explain an already-selected representative as `duplicate-cluster-representative` only for media whose exact-byte identity has been server-verified. Unverified/legacy hash equality no longer becomes planning truth. The included asset-id set, omitted asset ids, chronology, chapter topology, dwell/trim values, planned duration, and guest behavior remain unchanged; raw content identity is not exposed in UI or telemetry.
- **Review fixes:** The original implementation consumed persisted `contentHash` directly and passed its first exact-head review, but owner CFAA on historical head `4291f55e11f93307c9cd1d1dc56ad033cb95d2f9` found that persisted hashes were still client declarations rather than authoritative stored-byte truth. ST-058 was blocked while Backend prerequisite #311 established verified provenance via PR #313. After #313 landed and exact main CI passed, the same #310 owner rebased onto current green main and Source `cec4872` gated duplicate identity on `contentHashVerified === true`; focused fixtures now prove verified-equal hashes cluster while equal unverified hashes do not. The provenance review thread was answered with exact-head evidence and resolved only after the new source passed product CI.
- **CFAA / regression family:** Changed dimension is persisted media identity as a deterministic planner input, now conditioned on authoritative provenance. Family search covers upload start/multipart completion/finalization dedupe owned by #311/#313, owner Journey media transport, unverified/legacy owner media, guest/share hash exclusion, deterministic planner/validator re-derivation, runtime Journey-scoped projection into the first Route Point, selection-summary reason rendering, and invariance of membership/timing. No perceptual/near-duplicate hashing, video-duration discovery, quality scoring, pin/exclude intent, aspect-ratio/framing, Full Playback, Quick Recap tempo/topology, Keepsake, AI/VLM, or cloud media analysis is added.
- **Follow-up:** Issue #127 remains open for the broader deterministic media-digest/photo-first exploration beyond exact byte identity. #311/#313 owns and has already landed the shared verified-hash provenance boundary; this PR only consumes that contract.
- **Validation:** Lightweight local `git diff --check` passed; this rebased worktree had no materialized `node_modules`, so type/test/browser/build/server validation stayed in GitHub CI. Exact CODE-head CI run `34557645588` on Source `cec48726a505d3af55a4b317cba659d3820d2550` passed `core` and every product/browser QA lane, including login-media, post-login, playback-prefetch, final-acceptance, route-anchoring, city-label-anchoring, earth-dive, mobile-contract, globe-focus-chrome, route-point-context, globe-render-budget, guest-share, and owner-share; only the intentionally stale pre-reseal `ledger` and aggregate `verify` were red. Automated review was quota-unavailable on this Source, so an exact-source manual CFAA/source audit was performed and found no remaining provenance or client-scope blocker. This final commit changes only `docs/pr-history/310.md`; frozen `docs/pr-history.md` remains unchanged.
