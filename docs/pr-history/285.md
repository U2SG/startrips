# PR #285 - Restore Story position after playback

- **Source head:** `eca7200634fd97bdd92ab3a9405f1c34079893df`
- **Scope:** Issue #245, ST-036 only. Add the pure Playback return record/resolver and committed playback-position contract; wire manual exit/completion/cancel through Journey Playback and Living Atlas back to the current Story Journey/Route Point/asset observation, with a minimal read-only Story observation hook and focused post-login return evidence. The review repair separates PlaybackMediaStage pending state from its successful presentation commit so only the media that actually owns the visible slot may advance the return commit log.
- **User-visible change:** Exiting Full Playback reopens Story at the last successfully presented logical Route Point/media observation; natural completion keeps the Journey selected at Atlas without reopening Story, and cancelling a pending Playback entry restores the pre-entry Story context. A failed or unseen media request no longer replaces the last visible return position.
- **Review fixes:** Owner/Codex P2 review found that `pending=false` also described failed presentation. PlaybackMediaStage now emits a dedicated successful `onPresented(assetId)` signal, the single Overlay commit ref accepts only a matching current media identity, and focused tests retain the prior committed position across a failed requested media step. The final-acceptance capture also resets and pauses Full Playback before deterministic chapter seeking so the playback clock cannot race the return target during preceding cinematic assertions.
- **Follow-up:** None in this handoff slice; Story compositor, Playback timing/transport, inert ownership and deferred fullscreen cancellation remain owned by their existing contracts.
- **Validation:** Local validation intentionally limited to `git diff --check` and `node --check scripts/qa-post-login-controls.mjs`; full typecheck, unit/build and browser validation remain authoritative in GitHub CI, including the Story -> Full Playback -> seek -> exit return capture.
