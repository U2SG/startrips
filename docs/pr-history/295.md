# PR #295 - Bound particle globe render cost

- **Source head:** `e43984e666870af402ead18284457277c20d42d6`
- **Scope:** Issue #247 / ST-038 only. Bound particle globe rendering with a finite DPR and drawing-buffer pixel budget; initial and resize sizing share one resolver. A product-state visibility resolver reuses the existing hidden-tab/rAF lifecycle, and debug output publishes effective DPR, pixel count, and render state. Add dedicated `browser-qa / globe-render-budget` coverage. No projection, geography, coastline, Route Point, particle optical-size, or Earth Dive camera ownership changes.
- **User-visible change:** Very large/high-DPR viewports stay within budget. The globe renders behind translucent Playback intro/travel/stop/outro and through cover transitions. It may suspend only behind settled opaque media, fullscreen Story, or expanded compact Story that fills the dynamic viewport. Reveal has no catch-up burst.
- **Review fixes:** (1) Codex P1: the whole Playback was incorrectly treated as opaque. Cinematic input isolation remains, but cover ownership now follows the current phase and the existing media presentation-pending handoff; only settled media is opaque. (2) Codex P2: a 0.5 DPR floor could break the pixel cap. The area budget can now go below 0.5, and dimensions floor consistently. (3) Codex P2: expanded mobile Story using `100svh` could expose a strip. Use `100dvh`, while the existing transition flag keeps the globe alive.
- **Follow-up:** GitHub SwiftShader is contract evidence, not an Android GPU benchmark. Real-device frame/battery measurements and future renderer handoff remain separate. No second DPR policy, scheduler, geographic LOD, or device blacklist.
- **Validation:** Lightweight local validation: `git diff --check` and `node --check scripts/qa-globe-render-budget.mjs`. Exact CODE-head CI run 34336212675 on Source `e43984e666870af402ead18284457277c20d42d6` has `core` and every browser lane green, including `browser-qa / globe-render-budget`, `final-acceptance`, `mobile-contract`, `login-media`, `playback-prefetch`, `earth-dive`, and `route-point-context`. Only ledger and aggregate `verify` intentionally fail because the ledger is not sealed. This next/final commit must be ledger-only and receive its own exact-head CI.
