# PR #295 - Bound particle globe render cost

- **Source head:** `0b6e940408a585f90ecf842aa135b94efd4becf2`
- **Scope:** Issue #247 / ST-038 only. Bound particle globe rendering with a finite DPR and drawing-buffer pixel budget; initial and resize sizing share one resolver. A product-state visibility resolver reuses the existing hidden-tab/rAF lifecycle, and debug output publishes effective DPR, pixel count, and render state. Add dedicated `browser-qa / globe-render-budget` coverage. No projection, geography, coastline, Route Point, particle optical-size, or Earth Dive camera ownership changes.
- **User-visible change:** Very large/high-DPR viewports stay within budget. The globe renders behind translucent Playback intro/travel/stop/outro and through cover transitions. It may suspend only behind settled opaque media, fullscreen Story, or expanded compact Story that fills the dynamic viewport. Reveal has no catch-up burst.
- **Review fixes:** (1) Codex P1: the whole Playback was incorrectly treated as opaque. Cinematic input isolation remains, but cover ownership now follows the current phase and the existing media presentation-pending handoff; only settled media is opaque. (2) Codex P2: a 0.5 DPR floor could break the pixel cap. The area budget can now go below 0.5, and dimensions floor consistently. (3) Codex P2: expanded mobile Story using `100svh` could expose a strip. Use `100dvh`, while the existing transition flag keeps the globe alive. (4) Owner P2 requested proof of quality-budget transitions. Production already calls `resize()` from `applyQuality()`, which re-runs `applyRendererBudget()` and `renderer.setSize()`. Final QA explicitly proves `high->low->high` without viewport resize, checking that DPR and drawing-buffer pixels decrease at low quality and return to their original high-quality values.
- **Follow-up:** Issue #247 stays OPEN; this PR uses non-closing `Refs #247.`. Remaining target-device acceptance requires actual renderer DPR, drawing-buffer pixels and frame intervals; a declared frame/long-frame budget; covered-state work reduction; repeated fullscreen/return/resize/hidden->visible/resource-growth checks; and owner-reported Opera/Android regression-family evidence. GitHub SwiftShader is contract evidence, not an Android GPU benchmark. Real-device frame/battery measurements and future renderer handoff remain separate. No second DPR policy, scheduler, geographic LOD, or device blacklist.
- **Validation:** Exact CODE/QA Source CI [34339284216](https://github.com/U2SG/startrips/actions/runs/34339284216) on `0b6e940408a585f90ecf842aa135b94efd4becf2` has `core` and every browser lane green, including `browser-qa / globe-render-budget` with the explicit `high->low->high` proof without viewport resize. Before sealing, only stale `ledger` and aggregate `verify` were red: the outdated Source head classified the final QA changes as code drift. Seal validation is limited to `git diff --check`, `node scripts/pr-history.mjs validate-pr --pr 295 --base 8fe98d0b188d705a1a558c1867fcb0b686e1ec98 --head <ledger-head>`, and `node scripts/pr-history.mjs validate-all`. The final commit changes only this ledger; its own exact-head CI remains authoritative for the seal.
