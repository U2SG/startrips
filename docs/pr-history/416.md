# PR #416 - Make CFAA invariants executable

- **Source head:** `b495721206e685e0db02f0f3a3579a372267f518`
- **Scope:** Deliver CFAA v2 E1 as an executable companion to the existing semantic review policy: stable invariant IDs, machine-readable dimensions/evidence, changed-path impact projection, PR declaration comparison, deterministic validation/tests, and advisory GitHub Actions summary output.
- **Authority boundary:** Path projection remains advisory. A path match means “review this assumption”; no path match is never approval; semantic CLEAR remains reviewer-owned. This PR adds no product runtime, deployment, scheduler, ONE, or owner-registry behavior.
- **Review fixes:** Preserved raw NUL-delimited Git path semantics across Unicode, literal backslashes, surrounding/all-whitespace filenames, CR/LF, Markdown-active punctuation, and invalid UTF-8 bytes; evidence paths now require exact tracked files; stable-ID/share/replay/renderer ownership boundaries were expanded where review found missing projections; merged #413 render evidence is recorded without overstating aggregate multi-renderer proof.
- **Follow-up:** #417 owns exact invariant-to-Source-review evidence binding. #418 owns recent-change graph and regression-family memory. Browser-QA lifecycle/readiness flakes are tracked separately and are not CFAA product regressions.
- **Validation:** Exact Source CI on `b495721206e685e0db02f0f3a3579a372267f518` has CFAA tooling tests, registry validation, impact projection, ledger tooling, and sharded-ledger validation green; the only ledger failure is the expected pre-ledger `Validate current PR ledger`. Prior exact CFAA Sources repeatedly passed all product/browser lanes; the known intermittent `login-media` / `earth-dive` / `city-label-anchoring` families are unrelated lifecycle/readiness QA races and remain separately tracked. All review threads on the Source are resolved. This final commit changes only this ledger.
