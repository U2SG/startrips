# PR #413 - Vendor the two-image Reveal Flows renderer as a bounded, lifecycle-owned Journey cover reveal module

- **Source head:** `eb4de90393215481320aaa6f216b60cad7c51c53`
- **Scope:** Issue #367 is the product/platform contract for an artistic cover-reveal opening and explicitly asks for independent slices. Already on main: the multipart upload pipeline and private signed-read path (server/routes/uploads.ts), the trusted stored-byte content identity contract (#311 / ST-059), and the globe render-budget work (src/scene/renderBudget.ts, ST-038). Slice 1 (derivative/job state, scoped worker credential, lease/idempotency, stale-source protection) has passed as ST-074 / issue #368 / PR #370. It remains outside this pure renderer slice, which accepts an explicit image pair and does not consume derivative APIs, so there is no artificial ST-074 dependency or derivative-data wiring here. Real gap covered here: slice 3 only — nothing of the renderer exists in the client (no RevealFlow, no reveal module, no vendored package), and the reviewed 1.0 package still lives only at the owner's local path, which #367 forbids at runtime. This feature vendors the reviewed renderer into the repository (it is standalone WebGL2 with a Canvas2D fallback and no Three.js dependency, so it does not introduce a second Three instance), wraps it in a pure, testable lifecycle module that takes an arbitrary from/to image pair and a preset as parameters, and proves lifecycle, reduced-motion, stale-intent, budget, disposal and fallback behaviour through a dev-only deterministic preview. It deliberately mounts no product surface: slice 2 (worker reference-client contract) consumes completed #368/ST-074 but remains separate, and slice 4 (Journey opening surface and replay policy) stays a later slice under #367 because the replay policy is a product pacing decision.
- **User-visible change:** Vendor the two-image Reveal Flows renderer as a bounded, lifecycle-owned Journey cover reveal module
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35385566491 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
