# PR #484 - Enable Google sign-in and explicit account linking on the existing identity contract

- **Source head:** `faaf33ecdc8575b29ebb5adf741dd36fbabb17ac`
- **Scope:** main has Better Auth 1.6.23 email/password only (server/auth.ts has no socialProviders) plus ST-067/#345's already-merged provider-agnostic identity safety layer: server/account-identities/{identity-policy,provider-proof,reverification-rate-limit,account-identity-repository}.ts and server/routes/account-identities.ts (list/reverify/link-intents/link-complete/unlink), whose empty usableProviderIds/linkableProviderIds and its own code comment explicitly anticipate #349 constructing the router with configured provider ids. #348 (a fuller multi-provider account-surface UI) is still unimplemented and not queued, but is not a real code dependency here. The gap: configure socialProviders.google against the pinned API; route new-signup vs existing-login vs explicit-bind intents through the existing reverification/proof/link pipeline so a bound subject always returns to the same stable user and same-email-unbound collisions never auto-link or duplicate a user/Atlas; initialize Atlas exactly once on true first creation; validate the OAuth return URL; keep scopes minimal and the client secret server-only; add the minimal explicit link/unlink client affordance against the existing endpoints; and cover cancel/reject/duplicate-callback/mid-flow-switch with CI fake-provider tests, recording the real-credential end-to-end check as an external gate.
- **User-visible change:** Enable Google sign-in and explicit account linking on the existing identity contract
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35689984568 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
