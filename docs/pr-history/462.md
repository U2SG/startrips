# PR #462 - Extend the Story-fullscreen shared-element morph to compact mobile and to video sources

- **Source head:** `68e2f865901e6f96fb920dba10d80a218e133c4c`
- **Scope:** On main, presentFullscreen/enterFullscreen/exitFullscreen in src/journey/JourneyStory.tsx already run Story<->fullscreen through runSharedElementMorph (src/motion/primitives/sharedElement.ts, from PR #266 and later hardened by #429/#436): cancel-on-newer-intent (isTargetCurrent), resize/orientation/scroll/blur/visibility invalidation, and reduced-motion fallback are all implemented and already used for image handoffs on desktop, and snapshotSource already canvas-snapshots a <video> frame for the overview<->stage morphs elsewhere in the same file. The real, verified gap is two explicit bypasses inside presentFullscreen: `if (mobileLayout) { setFullscreen(nextFullscreen); return; }` skips the morph entirely on compact mobile (a hard cut, no shared identity), and `if (source instanceof HTMLVideoElement) { setFullscreen(nextFullscreen); return; }` skips it for any video source on both desktop and mobile. #459's acceptance is exactly closing those two bypasses with the existing primitive (no second state machine/framework), keeping exactly one live playback transport for video and preserving the already-correct Reduced Motion, focus-return and invalidation behavior.
- **User-visible change:** Extend the Story-fullscreen shared-element morph to compact mobile and to video sources
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35517342343 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
