# PR #299 - Adopt the v12 goat-and-star brand identity

- **Source head:** `17d82a1fa2f42680a1fa50dee12ec5636a069f87`
- **Scope:** Issue #297 only. Replace the legacy runtime-font/Tabler-sparkle/simplified-lamb identity with the product-owner-approved v12 authored Startrips wordmark and articulated mountain-goat geometry while preserving the existing `StartripsBrandMark`, `StartripsWordmark`, `StartripsJourneyCue`, and `StartripsBrandLoader` component APIs. Add v12 full/wordmark-only/compact SVG assets plus matching favicon/touch artwork; remove the retired lamb assets and update the existing login brand-loader QA. No Journey, Story, Playback, Atlas selection, focus, history, data, server, or renderer ownership changes.
- **User-visible change:** Startrips now presents the v12 goat-and-star identity throughout Auth, Atlas, Story/Playback/Share/Composer brand cues and loading surfaces, with the authored four-point star retained as the wordmark i-dot. Browser favicon/touch and the file-protocol fallback use the same identity. Legacy loading particles, the Tabler sparkle, and independent CSS limb/letter animation are removed; product state feedback moves the v12 lockup as one quiet unit and reduced motion remains static.
- **Review fixes:** No current-head review blocker was raised. Integration preflight updated the existing `qa-login-v3` brand-loader contract from legacy lamb/star selectors to `data-brand-version=12`, the authored v12 wordmark asset, absence of legacy brand nodes, persistent-Earth continuity, and reduced-motion animation suppression. The compact mark uses one trusted static SVG geometry source with duplicate DOM ids removed and inherits control contrast rather than introducing a second artwork variant.
- **Follow-up:** None required for this identity integration slice. The standalone 25.4-second v12 motion study remains the authored design source; product surfaces deliberately reuse its final vector identity with the existing Startrips state/reduced-motion grammar rather than embedding the review studio UI or adding a parallel animation runtime.
- **Validation:** Lightweight local checks: `git diff --check`; `node --check scripts/qa-login-v3.mjs`; module-syntax validation for the trusted generated compact-mark geometry; XML/identity checks on all v12 SVG assets (single authored star and expected goat presence); PNG dimension checks for the 64px favicon and 180px touch icon. Exact CODE-source CI run 34347328829 on `17d82a1fa2f42680a1fa50dee12ec5636a069f87` has `core` and every product/browser lane green, including `browser-qa / login-media`, `post-login`, `mobile-contract`, `final-acceptance`, Story/Playback-related coverage, globe-render-budget and the geographic/share lanes. Its only failures are the intentionally absent current-PR ledger and aggregate `verify` before this seal. This next/final commit is ledger-only and must receive its own exact-head CI before review handoff.
