# PR #557 - Extend location search with an optional Journey-context focus and wire Composer's last Route Point into it

- **Source head:** `8553e1234fe785478a8469f6ba6ed9381f2f93dd`
- **Scope:** Verified on main: `LocationSearchOptions` (server/location/location-search.ts) and `searchLocations` (src/journey/journeyApi.ts) carry only `query`/`limit`/`signal` — no focus field anywhere, `server/routes/locations.ts` has no lat/lon params or INVALID_LOCATION_FOCUS error, PhotonLocationSearch/NominatimLocationSearch cache purely on query text, and JourneyComposer.tsx's `searchLocations(query)` call (line 536) sends no context. Spike PR #543 (open, unmerged, explicitly 'not for merge yet') prototyped the exact contract this issue asks to productize. The real gap is: add optional `focus:{latitude,longitude}` to the search contract, validate it with a bounded 400 `INVALID_LOCATION_FOCUS` before any provider call, forward rounded (~2 decimal) coordinates as Photon `lat`/`lon` and Nominatim `viewbox`+`bounded=0`, separate cache keys by focus, and have JourneyComposer pass the last Route Point of the current draft (never a centroid, never for itinerary import).
- **User-visible change:** Extend location search with an optional Journey-context focus and wire Composer's last Route Point into it
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source and delivery contract; resolved thread dispositions remain in the PR review history.
- **Follow-up:** Every registered delivery member retains its original issue scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 36150815529 attempt 1 passed the real product lanes; independent Source review and per-member acceptance coverage are recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
