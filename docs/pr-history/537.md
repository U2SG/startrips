# PR #537 - Streamline Chinese itinerary place import

- **Source head:** `1c2d5c821eb6c9fa110a11296347f845c5fd3696`
- **Scope:** Preserve source-backed dates and all itinerary entries, add English place and locality hints with strict geocoder matching, search known Chinese and English names through one endpoint, batch-confirm safe suggestions, keep endpoint-only legs and venue-free events out of location review, surface the import action, and retain render/recognition deployment settings.
- **User-visible change:** Members can paste a trip link, review matched places with Chinese source and English provider names plus city and country context, confirm safe matches together, and search unresolved places in either known language without handling flight legs as locations.
- **Review fixes:** Ground English dates; keep pasted-text city context; resolve named pass-through waypoints; hide closed import content; defer capability lookup until import intent.
- **Follow-up:** After merge and release authorization, sync the tracked Qwen bridge to the external rendering service and verify a real link in the deployed browser. Ambiguous or unindexed POIs remain manual.
- **Validation:** Source CI run 35979684668 passed core, keepsake-render, and every browser QA shard at `1c2d5c821eb6c9fa110a11296347f845c5fd3696`. Its ledger and verify jobs failed only because this ledger still named the preceding Source head; the ledger-only final run must validate this commit against the exact new Source head.
