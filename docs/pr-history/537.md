# PR #537 - Streamline Chinese itinerary place import

- **Source head:** `3f22f37a044693e2f83158ff46ebafca94383882`
- **Scope:** Preserve source-backed dates and all itinerary entries, add English place and locality hints with strict geocoder matching, batch-confirm safe suggestions, keep endpoint-only legs and venue-free events out of location review, surface the import action, and retain render/recognition deployment settings.
- **User-visible change:** Members can paste a trip link, review matched places with city and country context, confirm safe matches together, and search unresolved places manually without handling flight legs as locations.
- **Review fixes:** Ground English dates; keep pasted-text city context; resolve named pass-through waypoints; hide closed import content; defer capability lookup until import intent.
- **Follow-up:** After merge and release authorization, sync the tracked Qwen bridge to the external rendering service and verify a real link in the deployed browser. Ambiguous or unindexed POIs remain manual.
- **Validation:** Source CI run 35971075330 passed core, keepsake-render, and every browser QA shard. Its ledger and verify jobs failed solely because this required PR ledger file was not yet present; the ledger-only final run must validate this commit against the exact Source head.
