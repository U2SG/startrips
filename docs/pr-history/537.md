# PR #537 - Streamline Chinese itinerary place import

- **Source head:** `c98d2dd3bdef62d70075ef8c008b8176f34e90b8`
- **Scope:** Preserve source-backed dates and all itinerary entries, add English place and locality hints, search provider-backed locations, and review the entire itinerary with the configured model before positions are resolved. Search English aliases first to reduce provider calls, accept one distinctive model-corrected rural POI when country and state agree, and keep unresolved or ambiguous points for manual choice. Add a top-level import entry whose work continues while its dialog is closed.
- **User-visible change:** Members can start a link, text, or screenshot import from the Atlas header, browse the current page while it processes, and add reviewed locations together with Chinese source names and bilingual place context. A wrong match can still be changed before adding.
- **Review fixes:** Ground English dates; keep pasted-text city context; resolve named pass-through waypoints; hide closed import content; defer capability lookup until import intent; restore the desktop header layout and the model review import.
- **Follow-up:** After merge and release authorization, sync the tracked Qwen bridge to the external rendering service and verify a real link in the deployed browser. Closing or refreshing the browser tab still ends in-page work; ambiguous or unindexed POIs remain manual.
- **Validation:** Source CI run 35994389684 passed core, keepsake-render, and every browser QA shard at `c98d2dd3bdef62d70075ef8c008b8176f34e90b8`. Ledger and verify failed only because this file still named an earlier Source head; the ledger-only final run must validate this exact Source head.
