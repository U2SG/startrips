# PR #358 - Synchronize Detailed Earth first reveal

- **Source head:** `4707ab25c63e299d6211814c7bb290a33bf2cda3`
- **Scope:** Completes ST-068 / issue #355 inside the existing `DetailedEarthMap` / Earth Dive lifecycle. The repair synchronizes the first Detailed Earth reveal to a current-revision post-sync MapLibre render, closes the missed-load race by observing load immediately after map construction and reconciling already-loaded style truth through the same idempotent settle path, and keeps reverse-release latched until the committed resolver state has actually reached `prewarm` or `particle`. Existing camera/focus/zoom/bearing/pitch authority and renderer gesture ownership remain unchanged; Particle-only mode still owns zero MapLibre detail.
- **User-visible change:** The first desktop Particle -> Detailed Earth reveal no longer depends on a real window resize to finish painting. Reverse -> dive remounts also preserve the same staged handoff without re-entering detail while release is pending. No new renderer chrome, delays, retries, polling, fake resize or secondary camera/readiness authority is introduced.
- **Review fixes:** Review-driven corrections kept the lifecycle bounded and idempotent: post-sync readiness requires a current reveal revision render rather than style load alone; initial load observation cannot be missed when MapLibre renders before later listeners attach; same-stage owner transitions and reverse ownership are recorded without stale teardown callbacks; the reverse-release latch is consumed only after committed `prewarm` / `particle` state. The final QA-only Source commit replaces an invalid globe-projection self-continuity assumption (`2 ** mapZoomChange`) with comparison against the measured particle-camera authority scale change while retaining same-frame alignment, anchor, ownership and actionability gates. Former P1 `4001024776` is resolved on the exact final Source; earlier lifecycle findings are outdated/resolved. Exact `4707ab` Codex review completed 2026-09-13T22:37:57Z with no major issues.
- **Follow-up:** None inside #355. The queued release P1 #356 is separate and is not part of this PR. Existing ST-040 release-candidate reconciliation remains outside this change. This PR does not replace MapLibre, add renderer chrome, introduce 1px/fake-window-resize workarounds, poll readiness, add fixed reveal timeouts, weaken gestures, or add a second camera/readiness authority.
- **Validation:** Full browser/build validation remains authoritative in GitHub CI. Exact Source-head run `34787185057` on `4707ab25c63e299d6211814c7bb290a33bf2cda3` passed `core`, dedicated `browser-qa / earth-dive`, final-acceptance and the required observed product/browser lanes. `city-label-anchoring` had one unrelated first-attempt `hong-kong-localization @3x: rendered label is null` intermittent; the exact-head single-job rerun succeeded on the same Source. Exact-head merge-readiness also succeeded. Pre-seal `ledger` and downstream aggregate `verify` were the only expected failures because this PR ledger did not yet exist. During Source development, lightweight local checks included `pnpm typecheck`, focused node/Vitest lifecycle coverage, `node --check scripts/qa-earth-dive.mjs`, and `git diff --check`. This ledger commit is documentation-only; exact final-head GitHub CI must pass `ledger`, `verify`, `core` and required browser/product lanes before handoff.
