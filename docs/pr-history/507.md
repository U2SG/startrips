# PR #507 - Saved Journey editor: recorded-track import, readback and withdrawal consumer

- **Source head:** `77c66c40a0246c1b62c333ce3b5acbf4119aaae3`
- **Scope:** Verified against origin/main 239a87c1: server/app.ts mounts /api/journey-recorded-tracks (server/routes/journey-recorded-tracks.ts) with POST /:journeyId/imports (import), GET /:journeyId and DELETE /:journeyId (readback/withdrawal), all implemented per ST-094/ST-095/ST-120 (issue 419/423/341, all passed). No src/ module reads or writes these routes -- grep for recordedTrack/recorded-track under src/ returns nothing. The real gap is entirely client-side: the saved Journey editor (src/journey/JourneyComposer.tsx) has no way to import a GPX file, list stored recorded-track batches, or withdraw one. Add a scoped src/journey/JourneyRecordedTracks.tsx + journeyRecordedTracksApi.ts consumer wired into the saved-Journey editor only, consuming the shipped server reason codes and idempotent replay semantics as-is; do not touch the server contract, the GPX parser, or #342's route-provenance rendering. ST-120 is an explicit hard dependency even though already merged to main.
- **User-visible change:** Saved Journey editor: recorded-track import, readback and withdrawal consumer
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source; resolved thread dispositions remain in the PR review history.
- **Follow-up:** The linked issue retains its product scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35741449819 attempt 1 passed the real product lanes; independent Source review is recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
