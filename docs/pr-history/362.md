# PR #362 - Record the owner GO decision in the Continuous Journey candidate record

- **Source head:** `ed6b87484e5dbe7362f60fba03fb77e7daf9afbd`
- **Scope:** Replaces the single stale sentence in the `Release boundary` section of `docs/releases/continuous-journey.md` that still required the owner to sign candidate `2e77a414ddd0647e5b4a0cf268345be3efca5dd6` Go or No-go and to state whether the record stays report-only. The replacement records the owner's OWNER DECISION - GO for exactly that candidate SHA, cites issue comment https://github.com/U2SG/startrips/issues/249#issuecomment-5663312227 and its `2026-09-14T11:37:56Z` timestamp, states that the GO clears candidate acceptance only and authorizes no merge-ready, merge, tag, release or deployment, and states that a changed candidate SHA does not inherit it. Nothing else in the file changes: the pinned candidate SHA, verification time, candidate CI run 34834432003, the vocabulary legend and every capability-matrix row are byte-unchanged, and the document still labels itself a 候选验收 report. `docs/pr-history.md` is untouched.
- **User-visible change:** None in the product. A reader of the release record now sees the recorded owner decision and its explicit limits instead of a pending-decision claim that was false on main.
- **Review fixes:** None. The eight-minute post-push review window passed with no inline comment and no review on PR #362.
- **Follow-up:** None inside this bounded correction. Re-pinning the candidate to a newer main SHA, refreshing the capability matrix, tagging, releasing and deploying stay explicitly out of scope and unauthorized.
- **Validation:** `./init.sh smoke` on the Source head covered both typecheck projects, `git diff --check`, `node --check` on every QA script and `node scripts/pr-history.mjs validate-all`. Exact Source-head CI run 34861818496 passed `core` and every `browser-qa` lane; only the intentionally absent per-PR `ledger` and the downstream aggregate `verify` were red before this ledger commit. Exact final-head GitHub CI is authoritative.
