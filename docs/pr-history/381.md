# PR #381 - Enforce Particle-only Earth Dive policy

- **Base:** `cc9805a3c80eb4fd8ef6b7eb9492611748c0e932`
- **Source head:** `d34bd9fbb6cac4c5f24bb22d85d2b91c4ed79fea`
- **Scope:** Implements ST-084 / issue #331 as an orthogonal Earth experience policy `default | particle-only` inside the existing Semantic Earth Dive authority. The hard particle-only policy is evaluated before renderer preload/mount/readiness/commands, forbids DetailedEarthMap/MapLibre/canvas/worker/detail-network ownership, preserves the existing particle camera unless detail actually owned it, tears down stale detail command/calibration/readiness state, and requires a fresh committed zoom/focus intent before default policy may re-enter detail. No settings/persistence UI, permanent renderer button, zoom-limit workaround, unrelated map bundle, or Backend implementation is included.
- **User-visible change:** When the product selects particle-only Earth behavior, users remain on the particle globe through wheel/pinch, accessible zoom commands, route focus, Journey/Route Point selection, Playback and Story return; the real-map renderer is not preloaded or hidden-mounted. Switching back to default keeps the current particle context and waits for a new legal zoom/focus intent before detail can appear again.
- **Lifecycle semantics:** Switching from an active detail-owned stage to particle-only hands the latest legal geographic observation back to the particle camera; switching from particle/prewarm/blending does not fabricate a camera handback. Returning to default does not replay stale pre-policy focus/zoom requests. Policy/focus refs consumed by the persistent rAF resolver now update in commit-phase layout effect rather than render, so abandoned concurrent renders cannot mutate the live renderer lifecycle. Existing default four-stage, reverse, suspension, playback/story/focus, Reduced Motion and quality semantics remain authoritative.
- **Review fixes:** Review findings corrected unnecessary particle-camera handback, stale pre-policy focus rearming, commit-phase policy-edge bookkeeping, and the source assertion that guards the fresh-intent behavior. Exact Source `d34bd9fbb6cac4c5f24bb22d85d2b91c4ed79fea` received one fresh current-head Codex review result in comment `5683267350`: no major issues. All prior finding threads are resolved after exact-head evidence.
- **Follow-up:** Preference persistence/settings UI and any ordinary user-facing control that selects this policy remain separate future work; this PR only provides the renderer lifecycle contract and QA surface. No Backend follow-up is implemented here.
- **Validation:** Exact Source CI run `34989630317` on `d34bd9fbb6cac4c5f24bb22d85d2b91c4ed79fea` has `core`, `browser-qa / earth-dive`, `browser-qa / final-acceptance`, route/globe/mobile and all other observed real product/browser lanes SUCCESS. Pre-seal failure is limited to intentionally absent PR ledger and downstream `verify`. This commit adds only `docs/pr-history/381.md`; exact final-head ledger/verify/core/earth-dive/final-acceptance and all repository-required real lanes are authoritative before handoff.
- **Boundary:** No merge-ready, signing, merge, tag, release or deployment authority is granted. Experience Worker stops at HANDOFF_REVIEW after exact final-head CI and merge-readiness succeed.
