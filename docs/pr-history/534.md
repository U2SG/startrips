# PR #534 - Close AuthGateway ResetPassword's exception/finally gap and drop the borrowed invitation-expiry copy

- **Source head:** `2a2d8a04504e05f7ea1f84fad7fc62be47e07c94`
- **Scope:** Already on main: server/auth.ts's Better Auth reset lifecycle (anti-enumeration, one-time/expiring token, concurrency, session revocation, stable user/Atlas) is proven end to end by ST-096/PR#426 (issue #425), whose own test file states it exists to give #344 the server guarantees it needs and explicitly leaves 'the missing/expired/used/offline/result-unknown UI recovery states to #344.' src/auth/AuthGateway.tsx's forgot-password submit handler already has full try/catch/finally and correct anti-enumeration copy. The real gap is the sibling ResetPassword() component (src/auth/AuthGateway.tsx ~L284-320): its submit handler awaits authClient.resetPassword with no try/catch, so a thrown/network exception leaves `pending` stuck true forever with the button reading '请稍候…' and no message; and its failure fallback is `result.error?.message || "邀请已失效"`, reusing the invitation-flow copy for a password-reset failure instead of a reset-specific message, with no visible control to re-request a link from a missing/invalid/expired/used-token state.
- **User-visible change:** Close AuthGateway ResetPassword's exception/finally gap and drop the borrowed invitation-expiry copy
- **Review fixes:** Independent Hourly Review cleared this exact CODE Source and delivery contract; resolved thread dispositions remain in the PR review history.
- **Follow-up:** Every registered delivery member retains its original issue scope; this ledger creates no additional product behavior or authority.
- **Validation:** Exact Source CI 35961144123 attempt 1 passed the real product lanes; independent Source review and per-member acceptance coverage are recorded. This final commit changes only this ledger. Exact final-head CI remains required before HANDOFF_REVIEW.
