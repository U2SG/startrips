# PR #479 - Simplify media processing and storage cleanup

- **Source head:** `92bac2931acbfedb4b29a4ca3798e4e87530b2ed`
- **Scope:** Share the Journey/Atlas object-cleanup algorithm, remove unreachable StoryMediaTile management props and CSS, and group whole-Journey playback media once per projection. Source changes contain six implementation/style files and two regression-test files.
- **User-visible change:** Existing media order, chapters, Story controls, layout and deletion behavior are preserved. Whole-Journey projections no longer rescan all media for every Route Point; no measured latency or bundle-size improvement is claimed.
- **Review fixes:** Independent static review cleared the exact Source. A pre-Source review finding removed sorting of unconsumed intro/orphan media; steps now collect only valid Route Point owners, while Story also includes Journey-level intro media. Regression coverage checks stable ties, input identity, empty chapters, unused sort keys, backend-scoped object identity and deletion failure ordering.
- **Follow-up:** Later refactor batches cover route geometry reuse, production/QA dependency isolation, component orchestration, database round trips and renderer lifecycle. This Draft does not claim merge readiness or an external ONE handoff.
- **Validation:** Source whitespace checks and independent static review passed. GitHub Source CI run [35596487624](https://github.com/U2SG/startrips/actions/runs/35596487624) passed core (160 test files, 2,262 tests, typecheck and production build), all 24 browser QA jobs and Keepsake rendering. Only the missing pre-ledger file and downstream verify failed. No local tests, typecheck, build or browser QA were run. The final commit adds only this ledger; exact final-head CI remains authoritative before any merge consideration.
