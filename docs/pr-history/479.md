# PR #479 - Simplify core media and rendering lifecycles

- **Source head:** `6a41a5ce6dc46beaee848866990feeb996055948`
- **Scope:** Group whole-Journey playback media once; share Journey/Atlas storage cleanup and derived-object reconciliation; batch media ordering/move/undo writes; remove unused Story controls and repeated notes guards; separate uploads from Composer UI; reuse route geometry for selection-only changes; wake Earth Dive reconciliation from committed inputs; isolate legacy/QA modules and CSS from the production entry with a build guard.
- **User-visible change:** Preserve media order, placement, undo, cover, guest scope and presentation contracts while reducing repeated database and renderer work. Selection-only route changes retain geometry. CI build comparison 654f94a to 7042a11 reduced entry CSS from 268.38 to 230.51 kB (gzip 48.19 to 40.63 kB), and entry JS from 1385.90 to 1378.20 kB (gzip 411.80 to 408.19 kB); these are artifact sizes, not an end-to-end latency claim.
- **Review fixes:** Preserve sorting only for consumed playback owners and temporal reveal after route-data rebuilds. Count actual superseded rows under concurrent cleanup. Keep renderer-applied snapshots distinct from React refs and preserve committed Dive stages and input wakeups; identical readiness notifications in stable detail no longer schedule work. QA waits for mounted/committed fixtures and the actual published Dive inputs, then retains its strict idle assertion. Playback transport rechecks its current stop atomically before Next and still proves the exact returned media presentation.
- **Follow-up:** None required for this bounded refactor. No schema/data migration, deployment or external ONE state change. Further component redesign and end-to-end performance benchmarking remain separate work.
- **Validation:** Exact Source CI [35603598062](https://github.com/U2SG/startrips/actions/runs/35603598062) passed core (160 files / 2,268 tests, 352 control-plane tests, typecheck and production build), Keepsake render and all eight browser QA groups. Only the expected pre-ledger validation and downstream verify failed. Route selection retained build count 1, while changed/restored route data advanced it to 2/3. Particle, detail, returned particle and reentered detail each passed twelve frames with unchanged Dive tick count and no pending callback. Independent static review cleared production Source 7042a11 and the exact subsequent QA/readiness/input-probe deltas through this Source. No local tests, typecheck, build, browser QA or database execution. This final commit changes only docs/pr-history/479.md; exact final-head verify and merge-readiness remain required before merge.
