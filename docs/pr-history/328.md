# PR #328 - Restore Journey Rail visibility after playback return

- **Source head:** `6f3950a5a437dfb3d95473a2a4a5c01ec6e9e85c`
- **Scope:** Implements ST-063 / issue #325 on top of the merged ST-062 diagnostic. The desktop Journey Rail now derives an explicit `visibility` value from the same Playback / globe-point-picking ownership state that already controls its `inert` contract: those two owners render the rail `hidden`, and the released Atlas state renders it `visible`. The existing responsive mount condition, globe-focus opacity/pointer ownership, ST-062 diagnostic marker, actionability checks and QA timing are unchanged.
- **User-visible change:** After the compact-mobile to desktop handoff and a Full Playback -> Story -> Atlas return, the Journey Rail is reliably visible and clickable instead of intermittently remaining render-hidden over the live globe.
- **Review fixes:** The initial document-transition hypothesis was rejected because the failing acceptance context uses reduced motion and that path is non-animated. The final Source instead makes the normal-state visibility release explicit at the Living Atlas lifecycle boundary. Exact Source-head `browser-qa / final-acceptance` recorded `railVisibility:"visible"` at 320, 360, 390 and 430, `core` passed, and fresh current-head review reported no major issues with zero unresolved threads. No retry, wait, polling loop, timeout inflation or relaxed actionability was introduced.
- **Follow-up:** ST-062 / PR #326 remains the diagnostic predecessor and stays terminal. ST-040 release-candidate documentation remains paused until this fix is merged and the resulting exact main push CI is green. No unrelated Earth Dive or other Experience cleanup is folded into this PR.
- **Validation:** Local lightweight validation: `pnpm typecheck`, focused `src/journey/LivingAtlasApp.test.ts`, and `git diff --check`. GitHub CI is authoritative. ST-063 evidence will capture two separate `browser-qa / final-acceptance` lane logs on the identical final submitted head as `.agent-artifacts/st-063/final-acceptance-run1.log` and `.agent-artifacts/st-063/final-acceptance-run2.log`; each must contain the 320/360/390/430 `journey-rail-state` markers with `railVisibility:"visible"`. The exact final head must also pass `core`, `ledger`, `verify`, `keepsake-render` and the remaining browser lanes.
