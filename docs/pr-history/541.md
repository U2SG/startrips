# PR #541 - Keep Playback camera with the visible Route Point

- **Source head:** `da0f3ed4fb9eb13cba11ab4b617521dd03f6e9d8`
- **Scope:** Full Playback keeps its chapter clock independent of manual Particle Earth and Detail Map camera input. The map renderer acknowledges a destination frame before the arrival chapter begins; explicit chapter selection and “回到当前地点” restore camera follow. Back returns to the last presented Story Route Point or media asset.
- **User-visible change:** Viewers can pan, zoom, drag, or use the keyboard while a journey plays, then return to the current location without skipping a chapter. Long trips keep their arrival cue until the map reaches the destination, and leaving Playback restores the right Story position.
- **Review fixes:** A real first MapLibre wheel gesture now releases follow even when its synthesized zoom event lacks `originalEvent`. Explicit Next at the same Route Point restores follow. Browser QA observes the brief Stop when the director commits it, then checks the visible title and live map hit target. Trusted mobile touch accepts only the particle canvas or its delegated city/route labels, and requires the same pointer to reach the canvas for movement and release with actual camera rotation.
- **Follow-up:** After the video handoff in #538 reaches main, run a joint Story video fullscreen → Atlas Playback → Story video browser path on the combined code. The two changes have no product-file overlap, but their separate CI runs do not prove that full cross-surface sequence.
- **Validation:** Exact Source CI [36028020346](https://github.com/U2SG/startrips/actions/runs/36028020346) passed core, Keepsake render, and every browser QA shard, including the map camera path. Its only failures were the expected missing-ledger check and aggregate verify. This final commit changes only this ledger; exact final-head CI remains required.
